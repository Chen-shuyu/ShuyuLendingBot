# STATUS —— 現在是什麼狀態

> **這是唯一一份「現在」。** 寫下來的當下就取代前一份，舊的整段搬進
> [archive/PLAN-history.md](archive/PLAN-history.md)。
>
> **硬上限 120 行。** 超過就代表有東西該往下層搬，不是把上限調大。
>
> 最後更新：**2026-08-31 22:4x**（main，PR #51～#59 都已合併部署）

## 一句話

機器人**真金運作中**（2026-08-15 上線）。**D056（`assumed_hold_hours: 12`）已上線
並吃到第一筆**：08-30 22:57 部署當場把那張躺了 24.1 小時的 10.91% 換成 **9.00%**，
**33 分鐘後成交**。形狀與回測預測的一致，但 **n=1、而且那一筆還在生息中**。
🔴 **08-31 查出一個量測缺陷（D057）**：`hold_report` 把 `loan` 與 `credit` 當成兩筆
生意，而它們是**同一筆錢的兩個狀態**——17 筆其實是 14 筆，平均持有
16.93h → **20.45h**、完成率 35.3% → **42.6%**。中位數幾乎沒動（11.61 → 11.62h），
**所以 D056 選的那個 12 方向不變，但它其中一條支撐變弱了**。
🟡 **使用者提出一個假說（D058）**：越便宜的單借款人抱越久，也就是 `P` 該是 `r`
的函式而不是常數。**方向與資料一致**（`rho` = −0.29）**但強度遠不足以下結論**。
🟢 **工作已依「會不會改變送到交易所的東西」分成三條車道（D059）**，見下方。

## 資金與部位

| 項目 | 狀態 |
|---|---|
| 部位 | **`464862123`**——345.06 USD @ 年化 **9.00%**，08-30 23:30:46 開倉，**仍在生息**。**已持有 23.2 小時**，是假設值 `P=12h` 的 **1.9 倍**（n=1，只記錄不下結論） |
| 場上掛單 | **無**（錢在部位裡）。前一張 `5096500219` @ 10.91% 已於 08-30 22:57 被 D056 的部署取代——它留下一段 **24.1 小時、有預估值的右設限樣本**，正是 D047 說「樣本數是 1」時缺的那個 |
| **實得年化（帳本）** | **5.34%**——**交易所自己說的錢**（D051），⚠ **這是 08-30 的數字，D056 上線後還沒重算**（要等這一筆收回來，見乙道） |
| 實測完成率 | 🔴 **42.6%**（**14 筆**已結束的 `credit`，中位數 11.62h、平均 20.45h、借到期 4 筆）——**舊的 35.3%／17 筆是錯的，見 D057**。⚠ 42.6% 本身仍是下界（還有 1 筆生息中） |
| 等待估計 | 已成交那半**平均高估 4.25 倍、中位數只有 1.67 倍**（8 樣本）；**沒成交那半反過來被低估**（D047） |

算實得年化：`python3 scripts/sync_earnings.py --since 2026-08-15 --principal 344.31`

## 運行中的東西

- **策略 `expected_value`**（D035／D038）：掛在哪個價位由「利率 × 借出期間 ÷
  (等待 + 借出期間)」最大的那一個決定，等待時間每輪從 1 小時 K 線重估。
  **`P` 已於 D056 與合約天期拆開，目前 12 小時**（合約天期維持 2 天）
- **部署**：Podman ＋ systemd Quadlet，巡檢 600 秒；帳本每 6 小時同步、日結一天一則。
  **健康狀態不寫在這裡**（寫下來的當下就開始過期）——隨時可查：
  `podman ps` 或 `podman exec shuyu-lending-bot python3 scripts/healthcheck.py`
- **三張落地表**（M1 完整了）：`market_snapshots` ＋ `market_candles`（D042）、
  `pricing_decisions`（D043）、`repost_comparisons`（D046）。**列數不寫死在這裡**
- **四份工具**：`hold_report.py`／`wait_report.py`（🔴 **兩份都有 D057 的缺陷待修**）、
  `backtest_report.py`（M2）——**唯讀**；`sync_earnings.py`（D051，**唯一會寫 DB 的**）
- **測試**：**要用時重跑 `pytest tests/`**——手抄的數字會漂
- **已知限制**（都是刻意的，不是疏漏）：🔴 **乾旱會讓 `W` 自己變好看**（D047）、
  **`W` 用平均而中位數明顯較準**（D045 追記，**D057 是同一個統計性質的第四次現身**）、
  **往上調價沒有判準**（D046／D050／D054）、**`P` 仍是常數而它可能該是 `P(r)`**（D058）。
  🔴 **比較政策一律要跨起跑點**（D054）

## 接下來做什麼：三條車道（判準與完整敘述見 D059）

**判準是「這次改動會不會改變機器人送到交易所的東西」，不是風險大小——是可不可歸因。**

🔴 **取樣速度先講**：**錢鎖在部位裡的期間 `pricing_decisions` 一列都不寫**
（實測 23 小時 0 列）。所以觀察 D056 是以**循環**計不是以小時計，
**至今完整循環數是 0**，要看出差別得一週以上。**這正是甲道要現在做的理由。**

### 甲道——現在就做，場上有單也沒關係（行為零變化／唯讀）

| # | 做什麼 | 為什麼排這個順序 |
|---|---|---|
| 1 | **修 D057 的幽靈樣本**（報表濾掉 `kind='loan'`） | 唯讀，且它是後面每一項的**輸入** |
| 2 | **`W` 旋鈕化**：`ev_wait_statistic`（mean｜median）＋ `ev_censoring`（window_tail｜drop｜km），**預設＝現行行為** | D045 追記 ＋ D047，兩個修法都要能被比 |
| 3 | **`rate_dependent_hold()`**（D058） | 純模擬側；`HoldModel` 的接縫早就在了 |
| 4 | **`--wait` 聯合掃描**：`W` 6 種 × `P` 網格，**走 `run_policy_across_starts()`** | D054 的硬規矩；平均／勝率／最差要一起印 |

⚠ **甲道不等於零風險**：它一樣要重啟容器。實測重啟沒動過場上的單（08-30 兩次），
**但那是觀測不是保證**。

### 乙道——要等這一筆收回來

5. **D056 第一筆的驗收**：等待 0.55h、實際持有多久、有沒有借到期。⚠ **n=1，只記錄。**
6. **重算帳本實得年化**（`sync_earnings.py`）——唯一不靠推論的裁判（D051）。

### 丙道——要等乙道，而且是定價路徑，要使用者拍板

7. 🔴 **`W` 換預設值 ＋ `P` 重調，必須一起做**——D056 的 12 有一部分在補 `W` 的偏差，
   單獨改 `W` 會拆掉抵銷的一半。
8. **若 D058 成立，`P` 改成 `P(r)`**——它會**改變第 7 項的形狀**（要找的不是更好的
   常數，是一條曲線），所以先跑第 3 項再決定第 7 項怎麼收。

**不排進三道的**：A2-b 的進場守門（D054 末尾那個想法）、M2 第 3 步／M3。
兩項都獨立可做，但都不該插在丙道前面。完整路線圖見 [PLAN.md](PLAN.md)，
待辦見 [TASKS.md](TASKS.md)。

## 🟡 兩件「還沒被觸發」的驗收

**驗收條件寫在合併當下，而「沒發生」不等於通過**——這是 D044 自己立的規矩。

| 項目 | 狀態 | 還缺什麼 |
|---|---|---|
| **M1-c**（D046） | 條件 1 與條件 3 **✅ 全數已驗**；**條件 2 於 08-30 22:57 第一次被觸發**——D056 上線讓候選價位與場上價差 16.7%，遠超 2% 容差，**那張 10.91% 真的被改掛了**。⚠ **但那是設定改動引發的，不是市場走出來的**，所以還不算自然情境下的驗收 | 一次**由市場而非由部署引發**的改掛 |
| **D044／B5**（餘額不足不停機） | 🟡 仍未觸發 | 日誌自 08-17 起 0 ERROR |

## 三個判讀陷阱（完整敘述在 D057／D058／D047，這裡只留提醒）

- 🔴 **分組前要先數相異值**——14 筆只有 **7 個相異利率**，而浮點雜訊會切出一組
  **看起來支持假說**的假分組（D058）。以「支持」的樣子出現，比以「無法分組」危險。
- **穩健的統計量會掩蓋缺陷**：D057 的假樣本讓平均走 3.5h、中位數只走 0.01h。
- **`W` 的兩半要分開讀**：沒成交那半的偏誤**方向會變**（D047 追記）。

## 文件怎麼讀（三層）

| 層 | 讀什麼 | 什麼時候讀 |
|---|---|---|
| **L1 現在** | **本檔** ＋ [TASKS.md](TASKS.md) | **每次對話開場只讀這兩份** |
| **L2 為什麼** | [DECISIONS.md](DECISIONS.md)（含索引表）、[ARCHITECTURE.md](ARCHITECTURE.md)、[PLAN.md](PLAN.md) | 要改動、要查某個決策的理由時 |
| **L3 當時發生什麼** | [PROGRESS.md](PROGRESS.md)、[CHANGELOG.md](CHANGELOG.md)、**[archive/](archive/)**（入口是它的 README） | 想知道某天發生什麼 |

**程式碼註解裡的 `D0xx` 一律指向 [DECISIONS.md](DECISIONS.md)。**

## 維護規則

1. **本檔硬上限 120 行**，超過就往下層搬
2. **一件事只在一個地方講完整**：DECISIONS.md 是唯一的完整敘述處
3. **PR 合併後只更新三處**：本檔 ＋ CHANGELOG ＋（若有決策）DECISIONS
4. **PROGRESS.md 每月 1 號切檔**，舊月份進 `archive/`
