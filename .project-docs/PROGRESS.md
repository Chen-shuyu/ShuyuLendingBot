# PROGRESS

> **這份是 L3「當時發生了什麼」，不是現況。**
> 現在什麼狀態看 [STATUS.md](STATUS.md)，還沒做的事看 [TASKS.md](TASKS.md)。
>
> **本檔只保留最近 7 個工作日（2026-08-17 起）。**
> 更早的完整紀錄在
> [archive/PROGRESS-2026-07-26~08-16.md](archive/PROGRESS-2026-07-26~08-16.md)
> ——包含 M1～M4 的整個建置期、真金上線當天、以及 08-16 那一天
> （436 行，第一筆成交、D033／D034 都在裡面）。
>
> **往後每月 1 號切檔**，舊月份進 `archive/`。

## 全部紀錄索引（每天一行）

| 日期 | 段落 | 這一天發生了什麼 | 在哪裡 |
|---|---|---|---|
| 2026-07-26 | — | 接手專案、建立 `.project-docs/` 六份文件、M1／M2 起步（D007／D008） | → archive |
| 2026-07-27 | — | M3 資料與可觀測（D013）；修正 git 作者設定 | → archive |
| 2026-08-01 | — | M4 測試套件 227 項、CI 拿掉 `\|\| true`；修好部署的主機端目錄問題 | → archive |
| 2026-08-02 | — | 容器可靠性四項（D016）→ 兩項沒生效 → 改由 systemd Quadlet 接管（D017） | → archive |
| 2026-08-09 | — | 修好 CI 的容器生命週期斷言 | → archive |
| 2026-08-09 | 續 | 程式碼層三項 A3／A4／A5 | → archive |
| 2026-08-09 | 續 | 失效告警 B2 與「不健康就處理」A6 | → archive |
| 2026-08-15 | — | M4 分層搬遷 | → archive |
| 2026-08-15 | 續 | 金鑰檔位置與掛載強化 | → archive |
| 2026-08-15 | 續 | B4：失效告警改三分法（同分支 `deploy/m4-secrets-hardening`） | → archive |
| 2026-08-15 | 續 | LINE Messaging API 接上，M4 完成 | → archive |
| 2026-08-15 | 續 | 首次實單：被拒單、找到金額四捨五入的 bug | → archive |
| 2026-08-15 | 續 | 第一筆真單掛出，隨即抓到取消掛單失效 | → archive |
| 2026-08-15 | 收工 | 實單第一天總結 | → archive |
| 2026-08-16 | — | 掛單 12 小時零成交，查出定價基準用錯 | → archive |
| 2026-08-16 | 續 | 時區統一為 CST | → archive |
| 2026-08-16 | 續 | 盤點待辦並排出工作計畫（直接更新 main，未開分支） | → archive |
| 2026-08-16 | 續 | 新增三項待辦：CI 部署過濾與 LINE 通知規劃 | → archive |
| 2026-08-16 | 續 | P1-3 實作：純文件變更不再觸發部署重啟（同分支） | → archive |
| 2026-08-16 | 續 | P2-3／P2-4：訊息格式統一與交易面通知 | → archive |
| 2026-08-16 | 續 | P1-1／P2-1：訂單簿定價與成交偵測 | → archive |
| 2026-08-16 | 續 | 第一筆成交，以及它險些被自己取消掉 | → archive |
| 2026-08-16 | 夜間第二段 | 用半價借出去了，定價鏈補上成交資料源（D033） | → archive |
| 2026-08-16 | 夜間第三段 | 查證推翻了 P1-4 的原規劃，重掛判準改用推導式（D034） | → archive |
| 2026-08-17 | — | 空轉一整天，查證推翻「市場跌了」的第一印象（D035） | 本檔 |
| 2026-08-17 | 收尾 | PR #31 合併後的盤查與路線圖重排（D036） | 本檔 |
| 2026-08-17 | 深夜 | A1：定價決策進日誌，並修掉一個講錯理由的出口 | 本檔 |
| 2026-08-18 | — | 🎉 期望值策略拿到第一份正面證據；PR #32 與 A1 合併上線 | 本檔 |
| 2026-08-19 | — | 等待估計問錯了問題；閒置時間第一次被量出來（D038） | 本檔 |
| 2026-08-19 | 收尾 | PR #33 合併部署，並補齊 B7／B8 文件對齊 | 本檔 |
| 2026-08-20 | — | 🎉 第二筆正常成交；A2-a／A3 完成 | 本檔 |
| 2026-08-21 | — | PR #34 部署完成；合併後盤查抓到 D4 | 本檔 |
| 2026-08-21 | 續 | PR #35 部署驗證 | 本檔 |
| 2026-08-22 | — | 8/21 起的交易盤點（上線以來最好的一段），以及 D1 的量測落地 | 本檔 |
| 2026-08-23 | — | PR #36 部署驗證；查證下一步時挖出兩個跨輪殘留狀態（D041） | 本檔 |
| 2026-08-23 | 續 | PR #37 合併與部署驗證，以及一次全文件對齊盤查 | 本檔 |
| 2026-08-23 | 續二 | M1-a 市場快照落地（第一次存下市場長什麼樣）；C2 白名單修好 | 本檔 |
| 2026-08-24 | — | 🎉 D040／D041 第一次真實驗收通過（130 輪無殘留）；算出「掃單多久來一次」 | 本檔 |
| 2026-08-24 | 續 | M1-b 決策落地（D043）：DB 第一次存下「我們怎麼想」 | 本檔 |
| 2026-08-24 | 收工 | 盤點文件排出優先序，修掉 B5（D044）：餘額不足不再整夜停機 | 本檔 |
| 2026-08-25 | 深夜 | 市場兩天內翻轉，08-24 的結論失效；第一次去算簿子（掃單 40 次）；`ev_window_hours` 拿到雙向證據 | 本檔 |
| 2026-08-26 | — | **D043 五條驗收全數通過，M1 完成**；查出期望值曲線是平的、等待估計五筆全部高估 3.9 倍（D045） | 本檔 |
| 2026-08-26 | 續 | D045 實作：`wait_report.py` 上線；正式資料抓到一個會靜默吃掉樣本的合併 bug | 本檔 |
| 2026-08-26 | 收工 | 使用者提案帶出 D046：**往上調價從頭到尾沒有判準**；排出 M1-c 反事實落地 | 本檔 |
| 2026-08-28 | — | **M1-c 落地，M1 完成**：新表 `repost_comparisons`；行為零變化用「舊版 vs 新版並排跑」驗出來 | 本檔 |
| 2026-08-29 | — | M1-c 驗收：228 輪 **0 列，而那是正確的**——單子全秒成交，要觀測的狀態一輪沒出現；D045 倍數 3.9 → **5.4** | 本檔 |
| 2026-08-29 | 續 | 部位收回、模型第一次選 **10.95%**；查出等待高估倍數是**駝峰**、樣本全在峰頂；修掉報告誤述；**B6 七支替身實打校正** | 本檔 |
| 2026-08-29 | 續二 | **10.95% 成交了（等 5.19h）**——駝峰被證實；查出 **`median` 8/8 比策略用的 `mean` 準**；hold_report 的分界**不會自己好**；B6 補打 `funding_credits` | 本檔 |
| 2026-08-29 | 續三 | **開獎：掛高輸了**——借 1.98h、實得 **3.02%**（名目最高、實得最低）。誤差約 7 個百分點來自 `P`、1 個來自 `W`，**D045 一路在調小的那一項**；實得年化終於進報告 | 本檔 |
| 2026-08-30 | — | **第三個誤差源被量出來（D047）**：那張 10.91% 掛 13 小時沒成交，而模型算出的實質年化**單調上升 9.03% → 9.46%**——乾旱期會讓等待估計自己變好看；M2 拿到第二條可跑的驗收標準 | 本檔 |
| 2026-08-30 | 續 | **M2 第 1 步：重播器上線**（`core/backtest.py`）。**驗收 25/25 對上正式紀錄**；同時查出 **PLAN 原本的驗收標準比它依賴的那張表早寫了六天、根本跑不起來（D048）**；測試 726 → **756** | 本檔 |
| 2026-08-30 | 續二 | **M2 第 2 步：成交模擬上線（D049）**。成交規則拿 12 筆真實成交驗過**總量比 0.93×**；**D1 第一次有答案**，但方向切半就翻、細掃是雜訊。🔴 **就算 `P` 真的是 48，假設 48 仍不是最好的**——那個 48 在替 `W` 的偏差擦屁股；測試 756 → **791** | 本檔 |

---

## 2026-08-17 —— 空轉一整天，查證推翻「市場跌了」的第一印象（D035）

分支 `fix/market-floor-goes-stale`。這一天沒有任何程式碼變更，機器人**一筆單都沒掛**。
這一段的價值全部來自查證：**第一個結論是錯的，第二個才是真的。**

### 這 21 小時實際發生的事：什麼都沒有

PR #30 於 2026-08-16 23:33 部署，容器 `active (running)` 21 小時、healthy。
但當天的 493 行日誌**全部**是同一句「可放貸金額不足，略過本輪」，
DB 裡 2026-08-17 一筆掛單都沒有，融資錢包只剩 0.016 USD。

原因不是故障：**344.3 USD 仍鎖在 08-16 21:30 那筆年化 5.47% 的部位**
（`funding_positions.closed_at` 仍是 `None`），2 天期，最晚 2026-08-18 21:30 到期。
機器人整天無事可做是正確行為。

### 推演：資金回來的那一刻，會整輪不掛單

用專案自己的 `OrderBookDepthStrategy` 對當下的真實市場跑 `build_offer_plan(344.3)`：

```
排隊定價（前方 ≤ 100 萬）：0.00014996/日（年化 5.47%）
成交價下限（0.85 × 常態）：0.00013779/日（年化 5.03%）
絕對地板 minimum_rate：    0.00021918/日（年化 8.00%）
  → 低於地板，整輪不掛單 → 0 筆
```

簿子最前面是一道 **445 萬 USD、掛在 0.00015、2 天期**的牆——D033 那道 182 萬的牆
不但沒散，還長大了 2.4 倍。當下 30 分鐘內的成交**年化 8% 以上佔 0.0%**，
金額加權中位數年化 5.47%。

### ❌ 第一個結論（錯的）：「市場跌到 5.5%，8% 地板過期了」

這個結論的證據全部取自**當下這一個時間切片**，而它與 D030 記過的統計陷阱是同一類錯誤
——**用一段太短的窗去代表市場**。

### ✅ 查歷史 K 線之後：市場沒有跌，它是在每小時之內劇烈震盪

抓 `/v2/candles/trade:1h:fUSD:p2/hist`（1 小時 K、2 天期、5000 根，涵蓋 2026-01-21 起）：

| 窗 | 收盤 ≥ 年化 8% | **當根曾觸及 8%** | 收盤中位數 |
|---|---|---|---|
| 最近 7 天 | 46.7% | **89.3%** | 年化 7.75% |
| 最近 30 天 | 44.7% | **90.0%** | 年化 7.63% |

**每小時的振幅大得離譜**，隨便挑幾根 2026-08-17 的：

```
時間        開盤    收盤    最高    最低      成交量
08-17 19:00  5.51%   7.26%   9.78%   4.46%   5,720,072
08-17 20:00  7.26%   5.47%   9.78%   4.92%   5,726,224
```

也就是說：**「年化 5.47%」不是市場價，那只是最後一筆成交剛好落在區間的底部。**
同一個小時裡，需求掃到 9.78%。而 08-16 那兩筆成交正是這件事的兩面——
19:31 以 9.12% 成交（該小時最高 11.68%）、21:31 以 5.47% 成交（**該小時最高 11.77%**）。
**那筆「半價事故」是在一個曾經漲到 11.77% 的小時裡賣在底部的。**

### 回測：掛得越高，實質年化越高——一路到年化 10% 都還在漲

以「某根 K 的最高價 ≥ 掛單利率」判定會被掃到，實質年化用 D034 的式子
`r × 48 ÷ (等待 + 48)`（2 天期 = 48 小時）：

| 掛單年化 | 中位等待 | 平均等待 | 實質年化（7 天） | 實質年化（30 天） |
|---|---|---|---|---|
| 5.50% | 0.5h | 0.5h | 5.44% | 5.44% |
| 8.00% | 0.5h | 0.6h | 7.90% | 7.90% |
| 9.00% | 0.5h | 1.3h | 8.76% | 8.81% |
| 9.75% | 0.5h | 2.7h | **9.23%** | 9.40% |
| 10.50% | 0.5h | 11.7h | 8.45% | **9.61%** |

**中位等待一路都是 0.5 小時**——因為九成的小時都會掃到 8% 以上。
等待成本要到年化 10% 以上才開始咬人。

### 🔴 真正的結論：壞掉的旋鈕是 `target_queue_usd`，不是 `minimum_rate`

- **年化 8% 地板一直是對的**，而且是目前唯一在保護我們的東西。
  它擋掉的正是「賣在區間底部」。保留它的代價接近零（8% 的平均等待 0.6 小時）。
- **壞的是排隊定價**。`target_queue_usd = 1_000_000` 的意思是「排在前 100 萬以內」，
  而簿子底端被 445 萬的牆佔住時，那個條件會**強制**把我們押到 5.47%。
  排隊模型假設「排越前面越快成交」，但這個市場的成交是**陣發掃單**：
  需求來的時候一路掃到 9~10%，**站在最前面只保證你用最低價賣掉**。
- **D033 對那道牆的定性要修正**。當時寫「那代表某一個人願意賤賣，不是市場的價格」
  ——牆的存在是事實，但它不是偶發事件，而是這個市場的常態結構；
  真正的錯誤不是被牆騙到，而是**用「排隊位置」當定價基準**這件事本身。

### 這正是 D032（P1-5）預言的事，只是主角換了

D032 說「人手算出來填進設定檔的數字，市場一變就過期，而程式無從察覺」。
過期的不是它點名的 `queue_clear_usd_per_hour`，是 `target_queue_usd`——
它在 2026-08-16 簿子還薄的時候校準，一天之後簿子結構變了就失效了。
**P1-5 從「該做」升級為「擋在收益前面的唯一一件事」**：回測顯示現行策略
把年化 5.47% 當答案，而同一份資料下的期望值最佳解是 9.2~9.6%——**差距約 4 個百分點。**

### 下一步

- 定價改用期望值：候選利率 × 被掃到的機率，取 `r × 48 ÷ (等待 + 48)` 最大者（P1-5／D032）
- `minimum_rate` 年化 8% **維持不動**
- 等待時間的估計要有資料源：新增 1 小時 K 查詢，不能再靠人手填常數

### 同日實作：期望值定價上線（D035 的修正）

分支 `fix/market-floor-goes-stale`，commit `474b166`。

- 新增 `strategies/expected_value.py`（繼承 `OrderBookDepthStrategy`，只換掉定價那一步）
- 新增 `api.get_rate_candles()`；`strategies/base.py` 加 `requires_candles` 旗標與
  `candles` 參數；`core/bot_engine.py` 加 `_fetch_candles()`
- `config.yaml` 的 `strategy.mode` 改為 `expected_value`，
  **`minimum_rate` 年化 8% 與 `market_floor_pct` 原封不動**
- 測試 493 → 512 項

**驗收（同一份真實市場資料）**：舊策略 **0 筆**（排隊定價算出年化 5.47%，被 8% 地板
擋下）；新策略 **1 筆、年化 9.96%**，平均等待 3.1 小時、窗內命中 40 次、
**實質年化 9.35%**。另以 dry-run 走過 `build_strategy()` → `_fetch_candles()` →
`build_offer_plan()` 整條接線。

**寫測試時被自己的測試資料打臉、因而看清楚的一件事**：`r × 48 ÷ (等待 + 48)`
對等待非常寬容——48 小時的借出期間是分母大宗，**等 8 小時只讓實質年化打 86 折**。
原本以為「20% 但要等 8 小時」會輸給「9% 立刻成交」，實際上前者 17.1%、後者 8.9%。
這既是策略敢掛高價的理由，也正是 `ev_min_hits` 非要不可的理由——
少了它，期望值會爬到尾端那個只發生過一次、等不到的價位。

**沒做的部分：天期仍寫死 2 天。** 比較天期要一輪打三次 K 線端點（`p2`/`p7`/`p30`）
並各自估等待，是另一次改動的份量；資金到期前先讓「掛得出去」成立。列為 P1-5 的剩餘部分。

## 2026-08-17（收尾）——PR #31 合併後的盤查與路線圖重排（D036）

分支 `docs/audit-and-roadmap`。沒有程式碼變更，內容是盤查與規劃。

### 部署確認

PR #31（`fe0af92`）已合併，容器於 **21:22:15 重啟**，日誌第一行
`採用放貸策略：expected_value` ——新策略確實上線。資金仍鎖在 08-16 那筆
（餘額 0.016 USD），所以還在「可放貸金額不足，略過本輪」，行為正確。

### 盤查發現六項（三項程式、三項文件）

全部以實跑真實市場資料確認，詳見 TASKS.md 的「程式／文件盤查結果」。
**前三項都是 PR #31 自己帶進來、而且沒有任何測試蓋到的行為改變**
——因為它們的失效條件是「候選價位高過訂單簿可見範圍」，在舊策略下不可能發生：

- **A1**：`last_evaluation` 是死碼，期望值的計算過程完全沒進日誌。
  **這是 D033 的教訓在新定價鏈上原封不動重演。**
- **A2**：重掛守門檻（D034）退化成「永遠擋下往下調價」。候選價位年化 9.96%、
  簿子可見最高檔只有 7.21%，`_queue_ahead()` 對任何價位都回傳同一個截斷總額
  5,381,114 USD，分母約掉之後判準只剩比利率。實跑確認：往下調 10% → 擋下。
- **A3**：排隊位置日誌變成截斷值（數字正好等於簿子 250 檔總計）。
- **B7～B9**：README 還寫「現階段以 dry-run 為主」（自 08-15 起是真金）、
  ARCHITECTURE 沒有 `expected_value.py` 與 `get_rate_candles()`、
  兩處註解與現況矛盾。

### 路線圖重排（D036）

使用者提出「感覺沒有先規劃好就一直改不太好」。盤點兩天內的六個決策
（D030～D035），**後面的一再推翻前面的**，而共同成因是：
每個決策都用一個時間切片做出來，然後把結論寫成常數塞進設定檔，資料丟掉。
DB 有四張表，**沒有一張存過市場長什麼樣**。

所以路線圖把**量測基礎建設排在下一個策略決策之前**（第 1 期）：
市場資料落地、回測工具、成效量測。第 2 期以後的策略工作都做在那之上。
五個舊優先級的內容保留，只有排序被取代。

### 更正：CI 紅燈的成因是 GitHub 全域事故，不是這台機器（同日稍晚）

使用者找到 githubstatus.com 的公告，**推翻了先前對 C1 的成因判斷**。

事故建立於 **2026-08-17T13:40:03Z**（截至 15:10Z 仍在 `investigating`），
影響元件含 `Webhooks / API Requests / Issues / **Pull Requests** / Actions / Pages / Copilot`，
公告明寫 **「Archive downloads and raw repository content downloads are experiencing
an approximate 50% error rate」**——而 `codeload.github.com/.../tar.gz/...`
正是 archive download。

**今晚三個症狀因此是同一個根因**：CI 紅燈、「Merge status cannot be loaded」、
以及 PR 頁面載不進去（後兩者對應元件清單裡的 **Pull Requests**）。

### 🔴 這次的教訓：D036 的毛病，隔一天就自己犯了一次

先前把成因判定為「這台機器有 2 個 runner 共用對外 IP、`_work/_actions` 沒快取」。
那個說法**從頭到尾沒有直接證據**，是從「兩個 runner」這個事實推出來的合理故事。
`curl` 三次 429 同時符合兩種解釋，我卻只採用了自己那一個。

**D036 才剛寫完「每個決策都是用一個時間切片做出來的」，隔天就原封不動再犯一次。**
差別在於這次有人（使用者）去查了外部狀態頁——**而那正是我應該做而沒做的第一件事：
在歸咎自己的基礎設施之前，先確認上游是不是掛了。**

### 修正仍然保留，但理由改寫

自架 runner 要自己去公開的 codeload 下載 action，GitHub 託管的 runner 不走那條路。
所以 GitHub 的 archive download 一出問題，只有自架這邊會斷——
實測 3 比 0（同一段劣化期間 ubuntu-latest 三次全成功、自架兩次全滅）。
減少對公開 codeload 的依賴仍然正確，**只是沒有原本以為的那麼緊急**。

**連帶未解**：`deploy` 仍在自架 runner 且第一步就是 `actions/checkout`，
**GitHub 再出一次同樣的事故，部署就會斷**。已記進 C1。

## 2026-08-17（深夜）——A1：定價決策進日誌，並修掉一個講錯理由的出口

分支 `fix/log-expected-value-reasoning`（疊在 `docs/audit-and-roadmap` 上——
PR #32 卡在 GitHub 事故，不是內容有問題，所以不等它）。

### 完成 A1：期望值的推導寫進日誌

`ExpectedValueStrategy.describe_decision()` ＋ 迴圈層的 `_log_pricing_rationale()`。
`last_evaluation` 從死碼變成真的有人讀。真實市場實跑：

```
期望值定價：116 個候選價位，選中年化 9.96%（平均等待 3.1h、窗內命中 40 次、
實質年化 9.36%）；對照最快成交的候選 年化 5.47%（等待 0.5h、實質年化 5.42%）
```

**「對照最快成交的候選」是刻意加的**：那正是舊策略會選的價位，兩者並排才看得出
取捨換到了什麼。而 5.47% 剛好就是 08-16 那筆半價成交的價位——**日誌現在會把
「我們差點又賣在那裡」直接印出來。**

### 🔴 動手時才發現的更嚴重問題：不掛單的理由是寫死的，而且多半是錯的

`build_offer_plan()` 有**六個出口**回傳 `[]`，迴圈層一律寫「可放貸金額不足」，
**其中五個跟金額無關**。最糟的是「價格低於年化 8% 地板」：

```
舊：可放貸金額不足（目前 344.3 USD）        ← 帳上明明有 344 USD，自相矛盾
新：期望值算出年化 6.94%、成交價下限拉到年化 6.94%，仍低於地板年化 8.00%，本輪不賣
```

**而這正是市場走弱時最可能出現的情況**——也就是明天資金回來後很可能看到的畫面。
舊訊息會把人指向「錢為什麼不見了」，完全錯的方向。

**這是 D026「靜默失效」的第三次現身**：D026 壞了沒人知道、D030 成功了沒人知道、
這次是**決定不做，但講錯理由**。

修法刻意放在 `Strategy` 基底（`last_skip_reason` ＋ `_skip()`），
**三個策略的每個出口都要交代理由**——只修 `expected_value` 的話，
之後有人切回 `orderbook_depth` 做對照就會踩回同一個坑。
答不出來時退回中性的「未提供原因」：**講「不知道」遠比講一個具體但錯誤的原因好。**

### 測試

512 → 517 項。新增的那一組（`TestDecisionIsVisible`）逐一走過六個出口，
確認沒有任何一個是沉默的；另有一項專門釘住
「價格太低而不掛時，理由不可以說成沒錢」。

### 下一步

- A2（重掛守門檻退化）與 A3（排隊位置日誌變截斷值）**刻意留到下一條分支**：
  兩者牽涉「兩個等待估計器要統一成哪一個」，那是設計決策，不該順手夾帶
- PR #32 與這一條都等 GitHub 事故解除再合併（合併會觸發自架 runner 的 deploy）

## 2026-08-18 —— 🎉 期望值策略拿到第一份正面證據；PR #32 與 A1 合併上線

**這一天是這個專案第一次「策略贏了，而且贏在可以說清楚的地方」。**

### 事件時序（全部有日誌與 DB 佐證）

| 時間 | 事件 |
|---|---|
| 18:35:22 | 08-16 那筆年化 5.47% 的部位**被提前還款**，344.30 USD 回到融資錢包 |
| 18:35:28 | 同一輪重新掛單：344.36 USD、日利率 0.0002729（**年化 9.96%**）、2 天期 |
| 18:45～22:07 | 場上維持不動。市場常態價一路下滑 7.58% → 6.94% → **5.47%** |
| 22:11:22 | **成交**（部位 `464047005`），等待 3.6 小時 |
| 22:37～22:39 | PR #32 與 A1 合併進 main，CI 全綠，容器 22:39:02 重啟 |

### 為什麼這筆成交是「證據」而不只是「運氣好」

掛單期間市場常態價**跌到 5.47%**，我們卻掛在 9.96% 不動，最後被一波掃單吃掉：

- **含閒置時間的實質年化 9.27%**（3.6 小時閒置 ＋ 2 天借出）
- 對照當時立刻成交只能拿 **年化 5.47%**
- 而 5.47% **正是 08-16 那筆事故成交的價位**——同一個數字，這次沒有賣在那裡

**D035 的核心主張（成交是陣發掃單，撐住高價划得來）第一次被真實市場正面驗證。**
先前三筆成交裡，一筆 9.12%、一筆是 5.47% 的事故、一筆是這次。

### 🔴 但這筆的成因要誠實記下來：是策略的「不動」救的，不是策略的「判斷」救的

拆開來看，18:45～22:07 每一輪擋下重掛的都是 `_plans_match()` 的
**2% 利率容差**——候選價位始終落在 0.0002729 附近（`ev_window_hours=168`
把短期下跌平滑掉了），根本沒走到 D034 的守門檻。

也就是說：**A2（守門檻退化成永遠擋下往下調價）今天完全沒被觸發，
它的存在與否對結果沒有影響。今天的結果證明不了 A2 是對的。**
**這正是 D036 說的「用一個時間切片下結論」——要避免的就是把今天這筆讀成 A2 的背書。**

### 合併與部署（使用者指示直接處理）

機器上沒有 `gh` CLI 也沒有 token，改以本機合併後推 main；
GitHub 偵測到 head commit 已可從 main 到達，**PR #32 自動標示為 Merged**。

- 合併前先在 A1 分支跑完整測試（**491 ＋ 13 通過**，等於補上這條分支從沒跑過的 CI）
- `main`：`fe0af92` → `205a757`（兩個 merge commit：PR #32、A1）
- CI 四個 job 全綠，**部署階段成功**，容器 22:39:02 重啟

**時機是刻意挑的**：22:11 成交後資金已借出，**訂單簿上沒有我們的單**，
所以這次重啟不會取消任何掛單、不會損失排隊位置——**部署成本為零的視窗**。
（P1-3 保護的是純文件變更；這次含程式碼，本來就該部署。）

### A1 上線後第一行日誌就自己證明了價值

```
舊：本輪略過：可放貸金額低於最低門檻或單筆最小量，跳過本輪
新：本輪不掛單：可用餘額 0.01 USD 低於下限 150.00 USD
```

不必等到市場走弱那個情境，**重啟後的第一輪就看得出差別**。

### 目前狀態

- 部位 `464047005`：344.36 USD @ 年化 9.96%，**到期 2026-08-20 22:11**（可能提前還款）
- 累計三筆成交的毛利息約 **0.29 USD**，扣 15% 平台費後約 **0.25 USD**
  ——**而這個數字是手算的，`earnings_daily` 到現在還是空的**（見 PLAN 第 3 期 P2-2）

### 下一步的排序（使用者已認同，2026-08-18）

盤點後的三項急件，理由與取捨寫成 **D037**：

1. **A2 的最小誠實修法**——不做策略決策，只讓守門檻在算不出來時**棄權**而非偷偷否決
2. **M1 市場資料落地**——今天這筆是專案史上最有價值的一筆觀測，卻只存在於四行日誌裡
3. **P2-2 `earnings_daily` 接上資料源**——三筆部位跑完了，還是答不出「賺了多少」

## 2026-08-19 —— 等待估計問錯了問題；閒置時間第一次被量出來（D038）

**分支**：`fix/honest-wait-estimate-and-idle-tracking`

### 起點：使用者問「五點多被贖回、掛的單到現在還沒成交吧？」

核對日誌與 DB 的結果（時間點與使用者記憶差了半天，是**凌晨** 05:03 不是下午）：

- `05:03:19` 借款人**提前還款**，344.36 USD 收回。部位 `464047005` 原定 08-20 22:11
  到期，實際只借了 6 小時 52 分（08-18 22:11 → 08-19 05:03）
- `05:03:24` 重新掛單 `5084375241`：344.36 USD @ 0.000268（年化 9.78%）、2 天期
- 至 23:09 **已閒置 18.1 小時未成交**，期間 108 輪日誌每一輪都印同一句
  「維持不動以保住排隊位置」

### 兩份現場證據（都是當下抓的，不是事後推測）

1. **掃單發生在掛單之前**：03:00 那根 K 的 high 是 9.88%、04:00 是 9.96%，
   都掃得到 9.78%；05:00 之後 18 小時最高只到 9.12%。近 24 小時只有 2 根 K
   觸及 9.78%，兩根都在掛單前。
2. **掛單越出可見簿子**：當下 250 檔供給側總額 1,306,715 USD，
   **可見最高利率只有 9.04%**，而我們掛 9.78%——日誌印的「前方 2,289,677 USD」
   是截斷值（A3 現場重現）。順帶證實 B9：`bitfinex_client.py:146` 那句
   「250 檔對應約 500 萬 USD，足以蓋過整個供給側」在今天的簿子上是 130 萬，蓋不住。

### 做了什麼（兩層，都不動決策行為）

**第一層——把等待估計換成「從任意時刻進場」**（見 D038）

`estimate_wait_hours()` 的 docstring 宣稱處理了陣發性，但最後的 `fmean()` 把它
抹掉了：`[6,0,0]` 與 `[2,2,2]` 的平均都是 2。改成 `estimate_wait()`，
從每個小時各出發一次算等待，回傳 `WaitEstimate`（平均／中位數／p75／命中數／
右設限數）。右設限改為**計入而非丟棄**（丟掉的正是最長的那些）。

真實市場實跑（240 根 1h K）：

```
期望值定價：110 個候選價位，選中年化 9.78%（進場等待 平均 6.0h／中位數 3.5h／
四分之三在 9.5h 內、窗內命中 54 次、實質年化 8.70%、11% 的起點在窗內沒等到
「真實等待更長」）；對照最快成交的候選 年化 5.47%（平均等待 0.5h、實質年化 5.42%）
```

同一份資料舊算法選 9.96%、說「等 3.2h」，真實是 10.8h。

**第二層——閒置時間量測**（D037 順位 1 的那一項）

- `_parse_offers()` 補 `created_at_ms`（索引 2 = MTS_CREATE），**已實打驗證**：
  `'1787087004000'` → `2026-08-19 05:03:24 +0800`，與掛單當輪日誌一致
- 新表 `offer_wait_forecasts`：掛單當下的預估一張單一列
- 每輪印閒置時數、機會成本（金額）、與當初預估的對照
- 量測點放在所有「要不要動這張單」的判斷**之前**——閒置最久的輪次走的正是
  「維持不動」那條提早 return 的路徑

### 驗證

- 測試 493 → 509 項（單元＋功能），整合測試 13 項另外全過
- 測試替身一併校正：`make_offer_array()` 與 `live_offer()` 補上 MTS_CREATE
  （字串型別，與真實回應一致）；`FakeClient` 補上 `get_rate_candles()`
- 真實市場資料實跑確認日誌與落 DB 的內容

### 一個推論錯誤（留底，見 D038 最後一節）

第一次診斷寫的是「模型用均勻假設、命中率取倒數」，**那是錯的**——程式明確反對
倒數法。誤判的原因是 `168 ÷ 54 ≈ 3.1` 剛好對得上日誌，但那是數學恆等式而非證據。
真正的毛病不在用哪一種平均，而在**取平均這個動作本身**。與 C1 同一種錯誤：
數字對得上不等於機制猜對了。

### 下一步

- **A2-a／A3 仍未做**（D037 的順序不變）。A2-a 一放行就鬆開降價閘門，
  等這次的閒置資料累積出來再談
- 「等太久要不要降價」是策略決策，方向已算過：剩餘等待是重尾的
  （已等 6 小時後剩餘 15.1 小時、等 12 小時後仍是 15.0 小時），
  要用條件剩餘等待重跑期望值，**不要拍常數**
- 分批掛單算過不划算（最佳拆單 8.56% vs 最佳單點 8.77%），除非目標改成降低變異數

### 資金狀態

344.36 USD 仍掛在場上（`5084375241` @ 年化 9.78%），未成交。撐住的損益兩平點是
等待 38.6 小時（約 08-20 19:40），在那之前仍勝過「當初就用 5.47% 賣掉」。
**部署不會動到那張單**：新算法這一輪同樣選 9.78%，`_plans_match` 判定一致。

## 2026-08-19（收尾）——PR #33 合併部署，並補齊 B7／B8 文件對齊

**分支**：無（文件對齊部分依使用者指示直接在 `main` 更新，同 D037 的先例）

### 部署驗證

- PR #33 合併，main 在 `da546c2`。**兩個 commit 都以 `git merge-base --is-ancestor`
  實際比對過**，不是只看 PR 顯示已合併
- 容器 23:44:55 重啟、`Up (healthy)`，部署後無任何 WARNING／ERROR
- 新表 `offer_wait_forecasts` 已建立，目前 0 列——**符合預期**：
  場上那張單是 05:03 掛的，新表當時還不存在
- **場上那張已排隊 18.7 小時的掛單沒有被取消**，部署後第一輪就走
  「掛單條件與場上 1 筆一致，維持不動」，與部署前的實跑驗證一致

### 一個沒預期到的附帶效果

舊算法部署前那一輪選 9.96%，與場上那張 9.78% 差 **1.8%**——**卡在 2% 容差邊緣**。
新算法選出的價位與場上完全相同（0% 漂移）。
**降低等待估計的樂觀程度，連帶降低了重掛頻率**，動手前沒想到這一點。

### 文件對齊（B7／B8 清掉）

- **B7 `README.md`**：從「現階段以 dry-run 為主」改為「真金運作中」，
  補上「目前的定價策略」（白話講單位時間報酬與陣發掃單，含已知限制：
  天期仍寫死、市場資料未落地所以不能回測）與「部署」兩節
- **B8 `ARCHITECTURE.md`**：架構圖、目錄樹、元件說明三處補上 `expected_value.py`
  與 `get_rate_candles()`。**`orderbook_depth.py` 標明「已被取代但仍是父類別」**
  ——共用邏輯（金額拆分、風控上限、成交價下限、利率量化）住在那裡，它不是死碼
- **B9 未做**：兩處矛盾註解都在原始碼裡，當次指示不動程式。
  第一條（「250 檔對應約 500 萬 USD」）已在 08-19 拿到現場證據：實際只有 130 萬
- `PLAN.md` 新增「目前所在位置（2026-08-19）」：第 0 期剩 A2-a ＋ A3，
  並記下這一期為何比原訂的半天長很多——**兩天各冒出一個原本不在清單上、
  但比清單上任何一項都根本的問題（D037、D038），而且兩次都是動手修既有項目時
  讀程式本體才發現的**

### 下一步（順序有變，理由見 PLAN.md）

1. **等 D038 的量測收到第一組「預估 vs 實際」**——下一張掛單就會有
2. **A2-a ＋ A3**（要有上一步的資料才放行降價閘門）
3. **M1 市場資料落地**——D038 只落地了「掛單當下的預估」，
   那是 M3 的一半材料，**不是 M1**；市場長什麼樣仍然沒有任何一張表存過

### 資金狀態

344.36 USD 仍掛在場上（`5084375241` @ 年化 9.78%），已閒置 18.7 小時未成交。
損益兩平點在等待 38.6 小時（約 08-20 19:40）。

## 2026-08-20 —— 🎉 第二筆正常成交；A2-a／A3 完成（分支 `fix/honest-queue-position`）

**分支**：`fix/honest-queue-position`（收尾已跨到 08-21 凌晨）

### 今天的成交

| 時間 | 事情 |
|---|---|
| 00:00–15:05 | 場上仍是 08-19 05:03 那張年化 9.78% 的單，**已閒置 34.2 小時** |
| 10:12 | 期望值目標從 9.78% → **9.50%**（候選價位數 111 → 110，**是一根 K 滾出 168 小時窗**，不是市場變了：當時常態價與一小時前同為 8.00%） |
| 10:12–15:05 | **守門檻連續 30 輪擋下重掛** |
| 15:15:21 | 取消重掛：344.41 USD @ 0.00026027（年化 9.50%）、2 天期 |
| 19:10:59 | **成交**，等待 3.93 小時；19:17:50 偵測到並推播 |

全天 **0 WARNING／0 ERROR**，容器 `Up 23 hours (healthy)`。

### 三個數字要並排看

- **掛單當下的預估 vs 實際**：預估 平均 6.1h／中位數 3.5h／四分之三在 10.0h 內，
  **實際 3.93h**。這是 D038 的量測上線後**第一組校準資料**，落在中位數附近，
  沒有系統性偏差（**一個樣本**）
- **整個週期**：資金 08-19 05:03 就回來了，到成交空轉 **38.1 小時**。
  就算借滿 2 天，**週期實質年化只有 5.29%**；而 08-19 當時「立刻賣掉」的價位是
  5.47%（實質 5.42%）——**硬撐 38 小時，換到的比直接賣掉還少一點點**
- **首次成交至今（99.5 小時）**：真正借出去 57.6 小時，**資金使用率 57.9%**，
  累計毛利息約 0.145 USD，**整體實質年化 3.70%**

> 掛單的價格越來越漂亮，但「錢有多少時間在工作」還是輸家。

**成交的成因要誠實記**：市場常態價 12–14 點掉到 5.47%、19 點回到 9.12%，
我們掛 9.50% 就在市場漲上來的那一刻被掃到。**是市場漲上來找我們，不是降價換來的**
——降的那 2.9%（9.78 → 9.50）在這個波動面前是雜訊。這是 D035「撐住價格、
不要跟著跌」的第三次正面證據，但**不是**「今天那次降價是對的」的證據。

### A2-a ＋ A3：今天量到 30 個樣本，證明守門檻早就壞了

30 輪被擋下的日誌，每一輪的兩個排隊金額**完全相同**：

```
14:04 候選價位比場上那張單低，而排隊位置的改善補不回少收的利息
      （利率 0.00026800 → 0.00026027、前方 3,535,093 → 3,535,093 USD，
        單位時間報酬 0.0002358356 → 0.0002290333），維持不動。
```

30/30 命中率。**兩個數字一樣就是自白**：候選價位與場上那張單雙雙高過可見簿子，
`describe_queue()` 對兩者回同一個截斷值，分母約掉、判準退化成純比利率，
而前置條件已保證候選比較便宜——**答案恆定為「划不來」**。

**15:15 那唯一一次放行也不是判斷**：那一輪簿子剛好變深，兩個截斷值不再相同
（774,674 vs 1,817,452），閘門因此開了。**是資料的巧合。**

改法與驗收見 DECISIONS.md **D039**。要點：`describe_queue()` 多回 `truncated`
與 `visible_top_rate`；`_queue_ahead()` 越界回 `None`（棄權，不是否決）；
守門檻棄權時**要在日誌講出來**；兩行排隊位置日誌越界時改口說「至少」。
順帶清掉 B9 的兩處矛盾註解。

### 驗收（三層，都用真實資料）

1. **測試 509 → 520 項**（單元＋功能），整合 13 項（not live）全過
2. **真實簿子重演**：把當下簿子截到 2026-08-19 的可見上限（年化 9.04%）——
   舊版兩個數字相同（41,841 → 41,841）並否決、新版棄權。掃過所有候選價位，
   **30 個行為改變，全部落在越界那一側**；可見範圍內的 33 個一個都沒動
3. **以正式 `config.yaml` ＋ 真實市場資料實跑 `run_once()`**：

```
越界（重演 08-19／08-20）：
  掛單排隊位置估計：同天期前方 至少 119,774 USD、全天期前方 至少 126,180 USD
    ——候選價位年化 9.50% 已超出可見簿子（可見最高年化 9.04%），以上是下界
  往下重掛的守門檻棄權（…）：排隊金額只知道下界、比不出快慢，這一項不擋事
  → 取消 1 次、掛單 1 筆

可見範圍蓋得住（同一時刻、未截斷的真實簿子）：
  場上掛單排隊位置：前方 258,493 USD          ← 語氣不變，仍是量測值
  候選價位比場上那張單低，而排隊位置的改善補不回少收的利息（…），維持不動。
  → 取消 0 次、掛單 0 筆
```

**第 3 點的對照組是這次驗收的重點**：兩個價位一模一樣，只有簿子的可見範圍不同。
比得出來的時候，這條判準**照樣否決**——A2-a 修的是「算不出來時偷偷否決」，
不是把判準拿掉。

### 🔴 今天新發現的問題（都還沒做）

1. **期望值公式假設借滿 48 小時，但四筆有三筆提前還款**
   （`expected_value.py` 的 `hold_hours = offer_period * 24`）：
   實際持有 1.8h／45.1h／6.9h。而且**看起來與利率反向相關**——
   9.12% 借 1.8h、9.96% 借 6.9h、5.47% 借 45.1h（借款人跑去借更便宜的）。
   若成立，「撐住高價」的期望值被系統性高估。**4 個樣本，是假設不是結論**，
   而它正好是 M2 回測工具該回答的問題
2. **LINE 通知講錯話**：15:15 那次重掛推的是「啟動後首輪掛單已送出」，
   但容器已經跑了 15 小時。`_offers_live` 在「維持不動」那條路徑上永遠留著 `None`
3. **價格目標會因為一根 K 滾出視窗而自己跳一階**（10:12 的 111 → 110）。
   今天整條事件鏈的起點就是這個，而**我們無法直接證實**——市場資料沒有落地（M1）

### 資金狀態

344.41 USD 借出中（部位 `464168644` @ 年化 9.50%、2 天期，08-20 19:10:59 起），
最晚 08-22 19:10 到期。場上沒有我們的掛單——**部署成本為零的視窗**。

## 2026-08-21 —— PR #34 部署完成；合併後盤查抓到 D4（分支 `fix/abstain-reason-names-the-right-one`）

### 部署驗證

- PR #34 合併，main 在 `277620f`。**兩個 commit 都以 `git merge-base --is-ancestor`
  實際比對過**，不是只看 PR 顯示已合併
- 容器 **00:34:05 重啟、`Up (healthy)`**，systemd 接管正常，**部署後 0 WARNING／ERROR**
- **容器內的 `bot_engine.py`／`orderbook_depth.py`／`bitfinex_client.py` 與 main
  的 sha256 逐一比對一致**——跑的確實是新程式碼，不是舊映像

**但要誠實記：新程式碼的關鍵路徑到現在一次都沒被執行過。** 錢還借在外面
（餘額 0.01 USD < 150 下限），策略整輪不產生計畫，而排隊位置日誌與守門檻
都在「有計畫」之後才走得到。真正的上線驗證要等資金回來。

### 合併後盤查抓到 D4：棄權的理由指錯對象

**D039 自己帶進來的 bug**，是回頭讀程式碼＋實跑探測才發現的：

```
場上 0.000268 越界？ True      ← 真正答不出來的是這一張
候選 0.00026 越界？  False
日誌：往下重掛的守門檻棄權（候選價位年化 9.49% 已超出可見簿子（可見最高年化 9.67%））
```

**9.49 小於 9.67**，這句話自己就矛盾。棄權有**三個**成因（場上那張單越界、
候選價位越界、換算速率設成 0），而 D039 的第一版只看了候選價位。
換算速率被關掉時更離譜：當下沒有任何東西越界，它照樣寫「已超出可見簿子」。

而「可見上限落在舊價與新價之間」正是**市場走弱時最常見的形狀**——
場上那張是幾天前的高價、新算出來的比較低，簿子頂端剛好落在中間。不是角落案例。

**決策從頭到尾都是對的，錯的只有理由。** 與 A1 修過的病同一種
（D026 靜默失效的家族：這次是「決定不做，但講錯是哪裡不知道」）。

同批還修掉第四個角落：`not book` 原本混在第一道 guard 裡，**簿子抓不到時
整條判斷靜悄悄跳過**。現在的分界是——「沒有場上的單、沒有計畫」代表根本沒有這個
問題，安靜返回是對的；「拿不到簿子」是**有問題卻答不出來**，那是棄權，要出聲。

四種情境的實際輸出：

```
兩個都越界：  場上那張單（年化 9.78%）與候選價位（年化 9.49%）超出可見簿子（可見最高年化 8.03%）
只有場上越界：場上那張單（年化 9.78%）超出可見簿子（可見最高年化 9.67%）
換算速率關掉：queue_clear_usd_per_hour 設為 0，排隊金額換算不成等待時間
拿不到簿子：  拿不到訂單簿，排隊金額無從算起
```

### 驗收

- 測試 520 → **524 項**（單元＋功能），整合 13 項（not live）全過。
  四個新測試各釘住一種理由，其中兩個是**反向斷言**：
  「候選價位看得到時不可以被點名」、「沒有任何東西越界時不可以寫超出可見簿子」
- 真實市場資料端到端重演一次：越界時棄權並重掛、簿子蓋得住時照樣否決，
  **行為與 D039 完全一致——這次只動理由，沒動任何決策**

### 這件事本身值得記

修 D026 第四次現身的那個 PR，自己生出了 D026 的第六次現身。
**一個判斷式改成「會說不知道」之後，「不知道什麼」就成了新的說謊空間。**
下次再寫這類棄權路徑，理由要從一開始就按成因逐一列，不要先挑一個代表。

### 資金狀態

344.41 USD 借出中（部位 `464168644` @ 年化 9.50%），最晚 08-22 19:10 到期。
場上沒有我們的掛單——**部署成本仍是零**。

## 2026-08-21（續）—— PR #35 部署驗證

- PR #35 合併，main 在 `66e7d3c`。**兩個 commit 都以 `git merge-base --is-ancestor`
  實際比對過**
- 容器 **01:09:09 重啟、`Up (healthy)`**，`NRestarts=0`，**部署後 0 WARNING／ERROR**
- 容器內 `bot_engine.py`／`orderbook_depth.py` 與 main 的 sha256 一致
- 部署後第一輪日誌：`本輪不掛單：可用餘額 0.01 USD 低於下限 150.00 USD`——正常

### 🔴 一件要盯著的事：兩次部署都還沒真正驗證到新程式碼

D039（PR #34）與 D4（PR #35）改的是**排隊位置日誌**與**往下重掛的守門檻**，
而這兩段都在「本輪有掛單計畫」之後才走得到。資金 08-20 19:10 借出後一直沒回來，
所以自 00:34 起的每一輪都停在「餘額低於下限」那個出口——
**新程式碼一行都沒被執行過**。

**資金回來的那一輪才是真正的上線驗證**（部位 `464168644` 最晚 08-22 19:10 到期，
也可能提前還款）。屆時要確認三件事：

1. `掛單排隊位置估計` 與 `場上掛單排隊位置` 兩行的「至少／量測值」語氣是否正確
2. 若走到守門檻，棄權的理由有沒有點名正確的對象
3. 新掛單有沒有在 `offer_wait_forecasts` 留下預估（第二組「預估 vs 實際」）

### 文件流程備忘（這次確認過）

CI 的 `docs_only` 判斷：**變更全部落在 `.project-docs/` 才會跳過部署**，
只要有一個檔案在它之外就照常部署（fail-open，理由見 workflow 註解與 C2）。
所以純文件同步可以直接推 main，不會重啟容器、不會動到場上的掛單。

## 2026-08-22 —— 8/21 起的交易盤點（上線以來最好的一段），以及 D1 的量測落地

**分支**：`feature/measure-actual-hold-time`

### 一、8/21 00:00 → 08-22 12:07 的成績（36.1 小時）

| 指標 | 08-20 當時 | 這一段 |
|---|---|---|
| 資金使用率 | 57.9% | **88.8%** |
| 期間實質年化（毛） | 3.70% | **8.44%** |
| 累計（首次成交至今 136.6h） | — | 使用率 66.4%、實質年化 5.00% |

36 小時內走完兩個完整週期，**0 ERROR**、5 個 WARNING（都是 API 逾時／速率限制，
重試後全部成功）：

| 時間 | 事情 | 等待 |
|---|---|---|
| 16:09:01 | 部位 `464168644` 提前還款（借 20.97h／48h） | — |
| **16:09:06** | 重掛 344.41 USD @ 年化 9.50%——**還款後 5 秒** | |
| 16:51:03 | 成交（部位 `464223965`） | **0.70h** |
| 19:11:01 | 部位 `464223965` 提前還款（**只借 2.33h**） | — |
| **19:11:07** | 重掛 @ 年化 9.50%——**還款後 6 秒** | |
| 22:31:00 | 成交（部位 `464242253`） | **3.33h** |

**使用率從 57.9% 跳到 88.8%，主因就是這個「同一輪內收回即重掛」**，
閒置全段只剩 4.03 小時。

### 二、三件標記待驗證的事，可以結案了

1. **PR #34／#35 的新程式碼終於被執行到**（TASKS.md 記的「兩次部署都還沒真正
   驗證到新程式碼」）。實錄：
   ```
   掛單排隊位置估計：同天期前方 至少 1,282,760 USD、全天期前方 至少 1,916,242 USD
     ——候選價位年化 9.50% 已超出可見簿子（可見最高年化 7.74%），以上是下界
   ```
   **D039 的「至少」語氣正確、D4 點名的對象也正確**（9.50 > 7.74，確實是候選越界）。
2. **D038 的校準拿到第二、三組資料**：預估中位數 3.5h，實際 **3.93h／0.70h／3.33h**。
   三個樣本都落在中位數附近或更快，**沒有系統性偏差**——甚至偏保守。
3. **D035「撐住價格」的第四、五次正面證據，而且是最強的一次**：16:09 掛單當下
   市場常態價只有 **5.47%**、可見簿子上限 **7.74%**，我們掛 9.50% 完全在簿子之外，
   **42 分鐘被掃到**。

### 三、一個要並排看的對比

全段 **123 輪巡檢，選中的價位一次都沒變過**（全部年化 9.50%）；
同期市場常態價在 **5.47%～9.49%** 之間震盪、平均 **6.37%**。

> 掛的價格比市場均價高 49%，卻在 0.7 小時與 3.3 小時內成交。

這是 D035 那個判斷（陣發掃單、站最前面只保證賣最低價）到目前為止最乾淨的一次驗證。

### 四、但同一批資料揭穿了 D1，而且比 08-20 記的更嚴重

| 年化 | 實際持有 | 佔預定 48h |
|---|---|---|
| 9.12% | 1.84h | 4% |
| 5.47% | 45.08h | 94% |
| 9.96% | 6.87h | 14% |
| 9.50% | 20.97h | 44% |
| 9.50% | **2.33h** | **5%** |
| 9.50% | 16.30h（12:07 時仍在借出中） | 34% |

已結束五筆的**中位數 6.87h、平均完成率 32.1%**，而
`strategies/expected_value.py` 假設 **48h**。**唯一借好借滿的，是最便宜的那筆 5.47%。**

**這裡要誠實**：8/21 的結果（88.8%、8.44%）**並不支持**「選 9.50% 太高」這個推論。
被證實的只有**公式的分子是錯的**，不是決策是錯的。

> 這是 D036 那個病的反面版本：**這次是機制錯了、但數字很好看**，
> 而好看的數字比難看的更容易讓人不去查。

### 五、今天做的：D1「可以先做的那一半」（D040）

TASKS.md 早就寫了「`funding_positions` 已經有 `opened_at` / `closed_at`，
實際持有多久現在就算得出來，不需要等 M1」。今天把它做掉：

- **`core/hold_time.py`**（新）：持有時間的量測。右設限（仍在借出中）分開報、
  偵測延遲與近似起算時間都跟數字並排、完成率取代「借滿」的二分類
- **`core/bot_engine.py`**：部位收回的當下就在日誌講出「實際借了多久、佔預定幾成、
  是提前還款還是借到期」——不必等有人想到去翻資料庫
- **`scripts/hold_report.py`**（新）：隨時可跑的彙總報告，唯讀開 DB
- **`db/repository.py`**：新增 `all_positions()`（**刻意不濾掉還開著的**，
  右設限樣本要看得見才算得出蓋掉多少）
- **`strategies/expected_value.py`**：那個 48 的旁邊留下註解說明落差並指向報告，
  **但沒有改它**（見下）

**刻意不改 `hold_hours`。** 要換成什麼本身就是策略問題，要在 M2 回測工具上跑過
才知道——先改參數再建量測正是 D036 記下的錯誤。

### 六、實作到一半發現的事：報告差點說謊

第一版對「越貴借越短」做利率分組，便宜組只有 1.84h 與 45.08h 兩筆（差 25 倍），
`statistics.median` 插值給出 **23.46h**——一個沒有對應任何一筆真實借貸的數字。
報告據此印出「**差距 16.59h，方向符合「越貴借越短」**」。

**一個支持假設的結論，建立在一個虛構的數字上。**

改成：少於 3 筆不報中位數，直接攤開原始值；兩組任一組不足就說「還比不出來」。
現在的輸出誠實地說不知道，而兩筆原始值並排反而一眼看得出那組沒有集中趨勢。
這是 08-19 那句「真正的毛病不在用哪一種平均，而在取平均這個動作本身」的直接應用。

### 七、同批修掉一個藏在回傳值裡的 bug

`sync_positions()` 回傳的 `closed` 是 **UPDATE 之前**查出來的 dict，`closed_at`
還是 `None`。不補的話「剛收回」與「還開著」長得一模一樣，**每一筆還款的當下
都會被講成「至少借了 N 小時（仍在生息中）」**。以對照實驗驗收（拿掉修正兩個
測試立刻變紅）。

這個 bug 在這一輪之前不會現形——**因為之前沒有任何東西去讀那個回傳值**。

### 驗收

1. **測試 524 → 575 項**（單元＋功能 549、整合 26），全過。三個反向斷言釘住語氣
2. **對照實驗**：移除 `closed_at` 修正後測試變紅，還原後恢復綠燈
3. **真實資料端到端**：拿正式 DB 副本模擬 `464242253` 被還款——
   還款前 `已借出 至少 16.30 小時…（仍在生息中）`、
   還款後 `實際借出 16.30 小時…——提前還款`，語氣切換正確
4. **`scripts/hold_report.py` 以正式 `config.yaml` ＋ 真實 DB 實跑**，六筆數字與手算一致

### 下一步

1. **M1 市場資料落地**——仍是擋在所有策略問題前面的東西，而 8/21 給了它更明確的
   形狀：現在靠「掛在可見簿子之外、等陣發掃單」賺錢，而決定成敗的三個量
   （掃單多久來一次、掃多深、借款人多快跑掉）只有第三個落地了
2. **P2-2 `earnings_daily` 接資料源**——上面每一個數字都還是手算的
3. D2（LINE 把重掛講成「啟動後首輪」）仍在：08-21 兩次重掛講對了不是因為修好，
   是因為先走到 `bot_engine.py` 那條把 `_offers_live` 設成 `False` 的路徑。
   **觸發條件比原本想的窄，但沒有消失**

### 資金狀態

344.41 USD 借出中（部位 `464242253` @ 年化 9.50%、2 天期，08-21 22:31:00 起），
最晚 08-23 22:31 到期。**場上沒有我們的掛單——部署成本為零的視窗。**

## 2026-08-23 —— PR #36 部署驗證；查證下一步時挖出兩個跨輪殘留狀態（D041）

**分支**：`fix/stale-cross-round-state`

### 一、先補上 PR #36 的部署驗證（08-22 漏記）

08-22 的紀錄停在「尚未開 PR」，但 PR #36 當天 **19:21:18 已合併**、
**19:22:32 容器重啟**，只是沒有人回來寫驗收。實機查證：

| 項目 | 狀態 |
|---|---|
| 容器 | `healthy`，08-22 19:22:32 起連續運行 16 小時 |
| 日誌 | 該區間 **0 ERROR、0 WARNING** |
| 告警 | 部署當下那則 `OnFailure` 觸發被正確判讀為「單元正常運作中，不需人工介入」 |

**但 D040 的新程式碼仍然沒被執行到**：`_log_hold_times()` 要等部位收回才會講話，
而部位 `464242253`（08-21 22:31 起、年化 9.50%）到現在還開著，
**已借出 36.7 小時，最晚今晚 22:31 到期**。這一項繼續掛著待驗證。

> 這是連續第三次「部署完成 ≠ 新程式碼被執行過」。
> PR #34／#35 也各等了一天以上才等到觸發條件。

### 二、規劃下一步時，日誌自己招了

原本只是要確認現況好排順序，結果發現 **`期望值定價` 那一行在重啟後整個消失**
——08-22 19:22 之後 16 小時、96 輪，一次都沒印。往回查才看懂那是反證：

```
2026-08-21 22:34:02 INFO 期望值定價：113 個候選價位，選中年化 9.50%（…命中 47 次…）
2026-08-21 22:34:02 INFO 本輪略過：本輪不掛單：可用餘額 0.01 USD 低於下限 150.00 USD
```

同一輪的兩行互相矛盾。那份評估真正算出來的時刻是 **08-21 22:23**（成交前最後一輪），
之後**連續 21 小時、87 輪以上**每輪重播，位元組完全相同。重啟後之所以消失，
是因為新行程的 `last_evaluation` 是空的——**同樣的市場、同樣的餘額，只差在行程是新的**。

根因：`build_offer_plan()` 有六個出口在 `choose_rate()` 之前就 return，
而評估結果只在 `choose_rate()` 內重置。

### 三、查下去發現它有個雙胞胎

`_offers_live` 是同一個形狀：**只跟著「我們做了什麼」走，不跟著「看到的」走**。
而新策略下最常走的路是**什麼都不做**（08-21 那 36 小時裡 123 輪價位沒變過），
那條路提早 return，場上狀態沒人登記。五個出口漏掉，兩個後果：

1. **TASKS.md D2**：啟動後第一次重掛被講成「啟動後首輪」（08-20 15:15 實錄，
   當時容器已跑 15 小時）
2. **新發現，比 D2 嚴重**：FRR 讀不到時無條件推「掛單已不在場上」，
   但取消發生在 FRR 檢查**之後**，那一刻單還掛在場上
   ——**D2 是把真事講錯，這是把假事講成真的**

### 四、修法，以及一個意外的結果

- `last_evaluation` 的重置點移到 `build_offer_plan()` 開頭，與 `last_skip_reason` 並排。
  **刻意不在六個出口各補一次**——那正是這個 bug 當初的成因
- 新增 `_note_offers_unchanged(existing)`，在五個「什麼都沒動」的出口照實登記，不推播
- FRR 那個出口改成先看一眼場上再決定要不要說「單不見了」

**意外**：原以為 D2 那則訊息要從「啟動後首輪」改成「掛單已重新上線」。
實際跑過才發現通知推的是**狀態轉換**（D029），而場上本來有單、重掛後還是有單
——**沒有轉換，所以根本不該送**。修正後那一輪安靜通過。訊息是假的，額度是真的。

### 五、為什麼插在 M1 前面

M1 要把每輪市場狀態寫進 DB，而它想回答 D3（目標為什麼因為一根 K 滾出窗而跳一階）
就必須把本輪選中的價位與候選集一起存——**那正是 `last_evaluation`**。

> 日誌印錯，旁邊還有一行「餘額不足」可以拆穿它；
> DB 裡多一列假資料沒有鄰行會反駁，而 M2 回測工具會拿它當事實。

### 六、為什麼 575 項測試一項都沒抓到（兩個都是 D027）

- `test_沒評估過就不硬掰` 從**全新的策略物件**出發，而全新物件的清單本來就是空的。
  真實運作裡策略物件活得跟行程一樣久，**bug 只在第二次呼叫才現形**
- `FakeClient.create_loan_offer()` 不把掛出去的單放回 `active_offers`，
  所以替身世界裡「掛單之後場上有單」從來不成立。既有的
  `test_invalid_frr_also_counts_as_offers_gone` 因此**假性通過**，
  它的 docstring 還寫著「單已經在流程開頭被取消了」，而那句話在真實流程裡是錯的

新測試一律**跑兩輪**，並手動把單放回場上。

### 驗收

1. **測試 575 → 587 項**（單元＋功能 561、整合 26），全過
2. **對照實驗兩次**：分別移除兩處修正，11 項變紅、還原後全綠。
   每組各留一項對照組，確認不是把功能整個關掉
3. **反向斷言**：「不可以印出上一輪那一行的內容」——只斷言 `is None` 的話，
   把方法改成永遠回傳 `None` 也會過
4. **真實市場資料端到端**：以正式 `config.yaml` 抓當下簿子 250 檔、成交 10,000 筆、
   K 線 240 根，重演 08-21 22:34 那個處境，兩個方法都正確回 `None`

### 資金狀態

344.41 USD 借出中（部位 `464242253` @ 年化 9.50%、2 天期，08-21 22:31 起），
**最晚今晚 22:31 到期**。場上沒有我們的掛單——**部署成本為零的視窗，但只剩約 11 小時**。

### 下一步

1. **M1 市場資料落地** —— 仍是擋在所有策略問題前面的東西
2. **P2-2 `earnings_daily` 接資料源** —— 所有績效數字仍是手算的（表還是 0 筆）
3. M2 回測工具 → 第 2 期的策略決策（D1 改公式那一半、A2-b、天期、`ev_window_hours`）


## 2026-08-23（續）—— PR #37 合併與部署驗證，以及一次全文件對齊盤查

### 一、PR #37（D041）已合併並部署

| 事件 | 時間 | 佐證 |
|---|---|---|
| 合併進 main | 08-23 12:19:26 | `d81121a` |
| 容器重啟 | 08-23 12:20:44 | `podman inspect` StartedAt、systemd `ActiveEnterTimestamp` |
| 健康狀態 | 至 14:36 | `healthy`、`NRestarts=0` |
| 日誌 | 14 輪 | **0 ERROR 0 WARNING 0 CRITICAL** |

合併時機同樣挑在資金借出、簿子上沒有我們的單的零成本視窗——**第四次這樣做**。

### 二、新程式碼仍未被執行到，連續第四次（而這次的形狀最清楚）

部署後 14 輪**全部**走「可用餘額 0.17 USD 低於下限 150.00 USD」那個出口，
`choose_rate()` 一次都沒跑到——**日誌裡「期望值定價」出現 0 次**。

所以 `last_evaluation` 的重置**從來沒有東西可以重置**。而 D041 自己記下的反證
正是「新行程的清單本來就是空的」：

> **「那一行沒印出來」在修正前後長得一模一樣，證明不了任何事。**

同理，場上沒有我們的單，`_note_offers_unchanged()` 的五個出口一個都沒走到。
**第一次真正的驗收條件**：資金回來 → 策略真的評估過一輪 → 下一輪又掉回門檻以下。

三個綠燈（測試全過、部署成功、0 ERROR）同時亮著，**仍然可以代表新程式碼一行都沒跑過**。
前三次還能算巧合，第四次該當成常態來安排：往後每條修正分支在合併時就要寫下
「什麼事件發生才算被執行到」。

### 三、全文件對齊盤查：五個落差，方向全部一致

規劃下一步前先把六份文件逐項對照程式碼、`config.yaml`、CI workflow、systemd 單元與
git 狀態。**核對相符的部分佔絕大多數**——測試 587 項（單元＋功能 561 實跑全過、整合 26）
與文件記的數字精準吻合；D041 兩處修正確實在程式裡；ARCHITECTURE 描述的 `run_once()`
順序與程式一致；DB 五張表、client 十個方法、`with_retry` 的套用範圍、healthcheck 門檻
公式、CI 的 `docs_only` 規則、`HealthOnFailure=kill` 全部相符；`config.yaml` 沒有孤兒鍵。

**落差五項，全部是文件落後於程式，沒有一項是反向的**（沒有「文件寫了但程式沒做」）：

1. **PR #37 已合併，四份文件都還停在「尚未開 PR」**——六份文件搜尋 `#37` 命中 0。
   本次補上，連同上面的部署驗證
2. **CHANGELOG.md 斷了三筆**：最新條目停在 08-22（D040），中間缺 D039（PR #34）、
   D4（PR #35）、D041（PR #37）三筆已合併的程式變更；最後一則 `Deployed` 還停在 08-19。
   **六份裡唯一有系統性缺漏的一份**——因為它是唯一「不寫也不會有人立刻發現」的
3. **ARCHITECTURE.md 漏掉兩個實際存在的模組**：`notify/messages.py`（420 行，D029
   的通知訊息層）與 `utils/clock.py`（D028 的時區）。兩個都有對應的 DECISIONS 條目，
   只是從來沒進架構文件
4. **ARCHITECTURE.md 的測試數字停在 575**（實際 587）。**改法是把數字拿掉**，
   不是改成 587——嵌在目錄樹裡的快照數字每開一條分支就過期一次，追不完
5. **TASKS.md 舊 M4 區塊三個殘留未打勾項，跟同一份文件自己矛盾**：
   `deploy/m4-podman` 父項、`secrets.env` 填值（同文件別處寫 08-15 完成且已實際下單）、
   「不健康就自動重啟」（同文件 A6 已勾，`shuyu-lending-bot.container` 確實有
   `HealthOnFailure=kill`）。分支清理的數字也從 23 條變成 37 條

### 四、這次盤查自己的教訓

五項裡有四項的成因是同一個：**一件事做完的當下只更新了「最近在看的那份文件」**。
D039／D4 的內容在 DECISIONS、PLAN、TASKS、PROGRESS 都寫得很完整，唯獨 CHANGELOG 沒有；
`secrets.env` 填好了寫進「基礎建設待辦」，卻沒回頭勾掉 M4 區塊裡的同一件事。

> **文件之間互相矛盾的成本，比文件缺漏更高。**
> 缺漏會讓人去問；矛盾會讓人挑一個相信，而且通常挑到錯的那個。

### 資金狀態

344.41 USD **借出中**（部位 `464242253` @ 年化 9.50%、2 天期，08-21 22:31:00 起，
`closed_at` 仍是 `None`，**已借出 40.1 小時**），最晚今晚 22:31 到期。
`earnings_daily` 覆核仍是 **0 筆**、`loan_offers` 4,172 筆、`offer_wait_forecasts` 3 筆。

### 下一步

1. **等部位收回**，完成 D040 與 D041 的第一次真實驗收
2. **M1 市場資料落地** —— 仍是擋在所有策略問題前面的東西
3. **P2-2 `earnings_daily` 接資料源** —— 所有績效數字仍是手算的（表還是 0 筆）
4. M2 回測工具 → 第 2 期的策略決策（D1 改公式那一半、A2-b、天期、`ev_window_hours`）



---

## 2026-08-23（續二）—— M1-a 市場快照落地，DB 第一次存下市場長什麼樣

分支 `feature/market-snapshot-landing`。**第 1 期正式開工的第一批。**

### 一、為什麼是這件事，以及為什麼只做一半

M1 是三份文件（STATUS 的「接下來三步」、TASKS 第 1 期、PLAN）一致指向的下一步。
但 TASKS 自己寫了一個前提：

> ⚠ 存之前先確認 D041 的修正已在正式環境驗收過——日誌印錯還有鄰行可以拆穿，
> **DB 裡多一列假資料沒有鄰行會反駁**，而 M2 回測工具會拿它當事實。

**這個前提只管得到「本輪選中的價位與候選集」，管不到簿子／成交／K 線**
——後者是當輪現抓的外部觀測值，沒有跨輪殘留的可能。所以拆成兩批：

| | 內容 | 擋在什麼前面 |
|---|---|---|
| **M1-a（本批）** | 簿子摘要、成交摘要、K 線 | 不擋——現在就能做 |
| **M1-b** | 本輪選中的價位與候選集 | 等部位 `464242253` 收回、D041 驗收過 |

界線寫進程式：`core/market_snapshot.py` 的模組說明明講「**不認識任何策略物件**」，
每個欄位都算自本輪剛抓回來的原始資料。界線看得見，才不會下次順手就越過去。

### 二、拆成兩張表，不是文件原本寫的一張（D042）

TASKS 寫的是「新增一張表，每輪把三樣東西寫一列」。實作前看資料形狀發現一個浪費：
**K 線每小時才換一根，而巡檢 600 秒一輪。** 每輪存一份 240 根的窗，等於一天寫
三萬多列去講 24 根 K 的事。

所以：`market_candles` 一根一列、主鍵天然去重；`market_snapshots` 只留每輪不重疊的部分。
**實測驗證去重生效**：同一份 240 根的 K 線第一次寫 240 根，第二次只寫 1 根
（就是還在成形中的那一根——邊界含等號是刻意的，少了它每根 K 都會被凍結在
剛出生那一刻的樣子，而 `high` 正是這個策略唯一在意的欄位）。

完整理由記成 **DECISIONS.md D042**。

### 三、落地點的位置，是 D038 那一課的重演

寫入放在三個 `_fetch_*` 之後、**所有提早 `raise SkipCycleError` 的出口之前**。
底下有五條路徑會提早離開（策略無計畫、維持場上既有掛單、重掛不划算……），
而那些正好是「市場走弱、單子空掛」的輪次——也就是最需要留下市場長相的輪次。

D038 的閒置量測原本就擺錯位置，於是永遠量不到閒置最久的那些輪。
**差別在於日誌漏印還看得出來，DB 漏一列事後完全無感。**
四條功能測試專門守這件事，包含「FRR 無效那一輪確實什麼都沒觀測到，所以不寫列」
——寫一列空的比不寫更糟，那會讓「這段期間有幾筆觀測」這個數字說謊。

### 四、有損的地方都自己講出來（D039 的延伸）

摘要一定是有損的，所以每一個有損的地方都要有欄位承認：

- **`book_truncated`**：檔數等於 250 上限就標成截斷。**跑真實資料時此刻就在發生**
  ——250 檔可見最高只有年化 **8.00%**，正是 A2／A3 那個場景。
  少了這一欄，M2 會把截斷值當成真實深度讀，於是「前面排了多少錢」在每一個高價位上
  都得到同一個答案，而看起來一切正常。
- **`trade_span_minutes`**：這批成交涵蓋多久。D035 的第一個錯誤結論正是敗在
  樣本窗只有 4 小時，而當時沒有任何欄位記下這件事。實測這次是 36.1 分鐘。
- **沒觀測到的欄位一律留 NULL 而不是 0**：用 0 填會讓一段沒有資料的期間
  被讀成一段市場死掉的期間。

### 五、拿真實回應驗過，不是只有替身（D027）

實測一輪（2026-08-23 16:5x）：

| 量 | 值 |
|---|---|
| 原始資料 | 簿子 250 檔、成交 2,000 筆、K 線 240 根 |
| 簿子可見範圍 | 年化 5.83%～**8.00%**，總額 801,374 USD，**截斷 = True** |
| 成交加權中位數 | 年化 **6.57%**（每筆一票版本是 8.00%——D033 那個現象此刻仍在） |
| 2 天期加權中位數 | 0.00015（年化 5.48%），而全天期混算是 0.00018 |
| K 線窗內 high | 中位數年化 8.73%、最高 11.85% |
| 一列大小 | **約 1.2 KB** |

**成本因此是量出來的，不是估的**：一天約 190 KB、一年約 68 MB。
TASKS.md 原本寫「每輪幾百位元組」，那是還沒把簿子曲線算進去時的估計，
兩處程式註解都已改成實測值。

### 六、順帶修掉 C2，而且是它自己第二次現身

今天稍早的兩次文件推送又送了一組對照實驗，形狀與 08-19 那次一模一樣：

| 推送 | 內容 | 結果 |
|---|---|---|
| PR-A `0dcc6e7` | `.project-docs/` 五份 ＋ **`README.md`** | **15:19 容器重啟** |
| PR-B `e8694ec` | 全部落在 `.project-docs/` | 沒重啟 |

判斷式改為白名單（`.project-docs/`、`README.md`、`LICENSE`、`.gitignore`、
`.gitattributes`），**fail-open 維持不變**。

**驗證時踩到一個值得記的坑**：本機的 `grep` 是包裝 **ugrep 7.8.4** 的 shell function，
而 ugrep 的 `-q` 會忽略 `-v`，於是「文件＋程式碼」這個案例在本機測出
「跳過部署」這個假結果。改用 `/usr/bin/grep`（GNU grep 3.11，也就是 `ubuntu-latest`
實際跑的那個）重驗，七個案例全部符合預期。
**差點就把一個工具差異當成程式錯誤去修**——這與 D038 記的「從文件描述推論機制，
而沒去讀實作本體」是同一族：**用一個沒查過的工具去驗證，得到的紅燈不比綠燈可信。**

### 七、不需要遷移腳本

`Repository._create_schema()` 每次啟動都跑一遍 `CREATE TABLE IF NOT EXISTS`，
新表在下次部署啟動時自動建好——與 `offer_wait_forecasts`（D038）當初走同一條路。
已確認正式環境的 DB 目前有 `bot_state` / `earnings_daily` / `funding_positions` /
`loan_offers` / `offer_wait_forecasts` 五張表，新的兩張還不存在。

測試 587 → **618 項**（新增 31：單元 14 ＋ 資料層 11 ＋ 功能 6）。

### 資金狀態（16:30 查核）

344.41 USD **仍借出中**（部位 `464242253`，`closed_at` 仍是 `None`），
最晚今晚 22:31 到期。**D040 與 D041 的正式環境驗收仍在等這件事**，
而今天 15:19 那次重啟已把 `last_evaluation` 清空——所以現在日誌沒有重播定價決策
**不能當成 D041 驗收通過**，它只是還沒有機會犯錯。

### 下一步

1. **等部位收回**（最晚今晚 22:31），完成 D040 與 D041 的第一次真實驗收
2. **M1-b 決策落地** —— 驗收過才動
3. M2 回測工具 → 第 2 期的策略決策

---

## 2026-08-24 —— D040／D041 第一次真實驗收通過；順帶算出「掃單多久來一次」

**沒有程式碼變更的一天。** 機器人 08-24 全天 130 輪巡檢，每一輪都是「本輪略過」
——而正是這 130 輪把 D041 驗收完成了。真正的動作發生在昨晚（08-23 深夜），
但當時沒有人在看，是今天回頭查日誌才確認的。

### 一、等了三天的驗收窗口，昨晚 23:04 打開

部位 `464242253` 在 **2026-08-23 23:04:42 收回**（STATUS.md 預期是 22:31 到期，
晚了半小時才被記錄——巡檢 600 秒一輪，`closed_at` 是**偵測時間**不是交易所還款時間，
這個高估最多 10 分鐘的偏差 `hold_report.py` 自己會標註）。

**D040 通過** —— `_log_hold_times()` 第一次在正式環境跑起來：

```
2026-08-23 23:04:42 INFO 部位 464242253（年化 9.50%）實際借出 48.56 小時，
                         佔預定 48 小時的 101%——借到期
```

**D041 通過**，而且不是勉強通過。要抓的三段序列完整走完：

| 時間 | 發生什麼 | 對 D041 的意義 |
|---|---|---|
| 23:04:45 | 資金回來 → 評估一輪，`期望值定價` 選中年化 9.11% | 第一段：評估過 |
| 23:14〜23:45 | 場上有掛單，4 輪都正常重新評估 | 中段：該印的有印 |
| 23:50:49 | **成交**，可用餘額掉到 0.08 USD | 第二段：掉回門檻以下 |
| 08-24 全天 **130 輪** | 每一輪只有「本輪略過」，**沒有任何一輪重播 `期望值定價`** | 第三段：無殘留 |

原本的驗收條件只要求「下一輪不重播」，實際拿到的是**連續 130 輪不重播**。

**這一條要記下來**：08-23 的 PROGRESS 寫過「現在日誌沒有重播定價決策不能當成
D041 驗收通過，它只是還沒有機會犯錯」——今天這 130 輪是**有機會犯錯而沒有犯**，
兩者的差別就是驗收的全部意義。連續五次「部署完成 ≠ 新程式碼被執行過」的紀錄，
到 M1-a（08-23）與這裡（08-24）連續兩次兌現為止。

### 二、那筆交易本身：掛出去 46 分鐘就成交

| | |
|---|---|
| 23:04:47 掛出 | 344.57 USD @ 年化 **9.11%**（`0.00024971`／日），2 天期 |
| 策略預估等待 | 平均 8.7h／中位數 4.5h／四分之三在 13.5h 內 |
| **實際等待** | **0.77 小時**（23:50:49 成交，部位 `464372858`） |

預估 4.5h、實際 0.77h。**這次誤差是往好的方向，但這不是策略準的證據**
——反方向的誤差不會有人通知，見下面第五節。

目前 `464372858` 仍在生息，**08-25 23:50 到期**，那是下一個資金窗口。

### 三、持有時間統計更新（D040 的數字動了）

那筆 101% 是上線以來**第二筆借到期的**，把 D040 的統計拉回了一些：

| | 08-22（五筆已結束） | 08-24（六筆已結束） |
|---|---|---|
| 中位數持有 | 6.87h | **13.92h** |
| 平均完成率 | 32.1% | **43.6%** |
| 借到期／提前還款 | 1／4 | **2／4** |

**方向沒有變**：模型假設 100%，實測 43.6%，`expected_value.py` 的分子仍然被高估。
「越貴借越短」**還是比不出來**——便宜組仍只有 2 筆已結束（門檻是各 3 筆）。

### 四、累計績效（仍是手算，`earnings_daily` 至今 0 筆）

觀測期 08-16 19:31 → 08-24 21:48 共 194.29h：

| | 08-22 快照 | 現在 |
|---|---|---|
| 資金使用率 | 66.4% | **76.0%**（借出 147.6h） |
| 實質年化（含閒置） | 5.00% | **5.32%** |
| 借出期間加權年化（毛） | — | 8.23% |

淨利累計約 **0.41 USD**（毛利 0.478 扣 15% 手續費）。

### 五、順手算了 M1-a 落地的資料：市場這三天在轉弱

TASKS.md 的 M1 底下寫著「掃單多久來一次、掃多深，原始資料由 M1-a 落地
**（還沒有人去算它們）**」。今天用 `market_candles` 的 268 根 1 小時 K
（08-13 18:00 → 08-24 21:00）算了第一版：

**每天「日內最高成交年化」碰得到各門檻的小時數**

| 日期 | ≥8.00% | ≥9.11% | ≥9.50% | 日內最高 |
|---|---|---|---|---|
| 08-14 | 23 | 19 | 17 | 10.99% |
| 08-16 | 24 | 18 | 12 | 11.77% |
| 08-20 | 20 | 13 | 6 | 11.47% |
| 08-21 | 20 | 18 | 13 | 11.41% |
| **08-22** | 8 | 2 | **2** | 9.88% |
| **08-23** | 12 | 2 | **0** | 9.12% |
| **08-24** | 6 | 1 | **0** | 9.12% |

**掛在某個年化，多久會等到一次掃單**（用 K 線 high 當代理）

| 年化門檻 | 全期命中率 | 全期最長乾旱 | 最近 72h 命中率 | 最近 72h 最長乾旱 |
|---|---|---|---|---|
| 8.00% | 63.8% | 13h | 38.9% | 13h |
| **9.11%**（目前部位） | 38.8% | **40h** | **9.7%** | 40h |
| 9.50%（前三筆的價位） | 26.1% | **68h** | 4.2% | 68h |

**昨晚那筆 9.11% 是貼著全市場天花板成交的**：08-23 與 08-24 的日內最高都只有 9.12%，
只高 0.01 個百分點。今天一整天只有 00:00 那一小時碰得到它——**沒搭上昨晚那波的話，
現在還在空掛**。前三筆用的 9.50%，這三天一次都碰不到。

⚠ **這組數字是上界，不是實況**：K 線 high 只說「市場上有人以那個價位成交」，
不說「排在我們前面的幾百萬 USD 已經被清光」。真正的「我們會不會被掃到」
要等 M1-b 把候選集與排隊位置存下來、再由 M2 回測工具比對才算得準。
**拿它當「掛這個價位會等 N 小時」用就是又一次把代理當實況。**

### 六、一個要記下來的訊號：168 小時窗把這三天的轉弱平滑掉了

策略的等待估計用 168 小時窗（`ev_window_hours`）。上表顯示 9.11% 這個價位
**全期命中率 38.8%、最近 72 小時只剩 9.7%**——窗夠長就看不到轉折。
這與 TASKS.md 的 A2-b 記過的現象是同一個（「168 小時窗把短期下跌平滑掉了」），
但這次有了量化的形狀。

**這不足以構成「該把窗改短」的結論**——`ev_window_hours` 的敏感度本來就排在
M2 回測工具的題目清單裡（TASKS.md 第 2 期）。記在這裡是為了讓那時候有現成的證據，
**不是現在就去動那個參數**（先改參數再建量測，正是 D036 記下的那個錯誤）。

### 下一步

1. **M1-b 決策落地** —— 放行條件（D041 正式環境驗收）今天解除了，這是路線上的下一步
2. P2-2 `earnings_daily` 接資料源 → M2 回測工具

---

## 2026-08-24（續）—— M1-b 決策落地：DB 第一次存下「我們怎麼想」

分支 `feature/persist-pricing-decision`，完整決策見 [DECISIONS.md](DECISIONS.md) 的 **D043**。

**今天上午驗收通過的 D041，就是這一項的放行條件。** 當初擋著它的那句話是
「日誌印錯還有鄰行可以拆穿，DB 裡多一列假資料沒有鄰行會反駁」——
**驗收解除了放行條件，但沒有解除那句話**，所以下面每一個設計決定都繞著它轉。

### 做了什麼

新表 `pricing_decisions`，策略每**真的評估過一輪**就一列：

| 存什麼 | 為什麼 |
|---|---|
| 選中的價位 ＋ 它的等待分佈（平均／中位數／p75／命中數／右設限比例） | 「當初以為要等多久」不存就永遠消失（D038 的同一課） |
| 對照組（等最短的那個候選） | 那是舊策略會選的價位，兩者並列才看得出取捨換到什麼 |
| 候選集的**價位與實質年化兩排** | `effective` 是排序依據，留下它才答得出「為什麼是這個價位」（D3） |
| 窗長、最新那根 K 的 `mts` | D3 問的是「哪一根 K 滾出窗」，少了座標事後只能猜 |
| `hold_hours_assumed`（那個已知錯的 48） | 存的是**假設**不是事實——M2 換掉它之後，舊決策才比得了 |
| `snapshot_id` | 接回同一輪的 `market_snapshots` |

### 三個值得記下來的判斷

**一、寫入點緊貼 `_log_pricing_rationale()`。** 兩者讀同一份 `last_evaluation`，
於是**日誌那一行就是 DB 那一列的鄰行**——D041 擔心的「沒有鄰行會反駁」
是靠位置接回來的，不是靠承諾。功能測試把它釘住。

**二、沒評估過的輪次一列都不寫。** 今天白天那 130 輪（資金全借出、餘額守門檻
擋在 `choose_rate()` 之前）在這張表裡就該是 **0 列**。寫一列全是 NULL 的決策，
會讓「這段期間評估過幾次」從 0 變成 130。

**三、`last_window` 是新加的第三個「本輪狀態」，放進同一個重置點。**
每多一個都是一個會跨輪殘留的成員（D041 已經有兩個），而漏掉的那一個不會有人發現。
測試 `test_窗的座標也不可以跨輪殘留` 就是為它寫的。

### 成本是量出來的

| 存法 | 大小 |
|---|---|
| 每個候選的完整評估 | 17 KB |
| 陣列的陣列（去掉鍵名） | 5.3 KB |
| **只留價位與實質年化兩排（採用）** | **2.6 KB** |

整列實測（寫 200 列再 VACUUM 量檔案增長）：**4,137 位元組**。
每天約 35 輪會評估 → 一年約 50 MB；**最壞情況（每輪都評估）207 MB**。
最壞那個數字要寫出來——那是「市場一直很好、錢一直掛不出去」的日子，
**正好也是最想回頭看的日子**。

### 驗收

1. **測試 618 → 650 項**（單元＋功能 624、整合 26），全過。新增 32
2. **對照實驗兩組**，都立刻變紅：
   - 拿掉 `last_window` 的跨輪重置 → 1 個測試紅
   - 讓沒評估過的輪次也寫一列 → 3 個反向斷言同時紅
3. **正式 DB 副本端到端**：新表在開啟時自動建好（不需要遷移腳本，同 D042／D038）；
   拿真實的 268 根 K 線跑出 109 個候選，寫入後讀回來全部對得上

### ⚠ 這一項部署後不會立刻被執行到，而那是正確的

部位 `464372858` 要到 **2026-08-25 23:50** 才到期，在那之前每一輪都走餘額守門檻，
`pricing_decisions` 會**正確地保持 0 列**。

**0 列在那段期間是通過，不是失敗。** 先寫下來，免得屆時把它當成 bug 去查
——這是照 D041 的「一條該推廣的做法」：合併時就寫下「什麼事件發生才算被執行到」，
而不是等部署完再回頭想。完整的五條驗收條件在 D043 的最後一節。

### 下一步

1. **等 08-25 23:50 部位收回** —— D043 的正式環境驗收
2. **P2-2 `earnings_daily` 接資料源** → **M2 回測工具**（這張表是 M2 的輸入）


## 2026-08-24（收工）—— 盤點文件排出優先序，修掉 B5：餘額不足不再整夜停機

### 先排序，再動手

從 STATUS ＋ TASKS 盤點目前所有未完成項，排序時發現**三個約束決定了答案**，
而不是「哪一項比較重要」：

1. **08-25 23:50 是 D043 的驗收窗口**——在部位收回、`pricing_decisions` 寫出第一列
   之前，不能碰策略層與那條寫入路徑。這直接排除了 D1 改公式、A2-b、
   `ev_window_hours`、天期維度（它們本來也在等 M2）
2. **現在是零成本部署視窗**：資金全鎖在部位裡、每輪都走餘額守門檻，
   重啟不會打斷任何掛單。**但過了 23:50 就不是了**
3. P3-1／P3-2 要資金超過 500 USD 才有意義（目前 344.57 USD）

排出來的順序：**B5（有時效）→ P4-1／B6 → P2-2 → M2**。
B6 要排在 P2-2 之前是 TASKS 三處都寫過的：ledger 是下一個回應解析的重災區。

### B5：一個「不急」的待辦，其實有時效

B5 在 TASKS 裡標著「不急」，理由是「金額 bug 修掉之後這條路徑不會再被觸發」。
**那句話講的是機率，不是後果**——它的後果是 `FatalError` 讓機器人**永久停止**，
而最可能踩到它的時刻正是 **08-25 23:50 資金收回、重新掛單那一刻**，半夜沒人看。

完整敘述、三個取捨與五條驗收條件都在 **D044**，這裡只記時間軸。

### 時間軸

| 時間 | 事件 |
|---|---|
| 22:5x | 盤點文件、排出優先序 |
| 23:0x | 分支 `fix/b5-insufficient-balance-classification`，實作 ＋ 測試 |
| 23:2x | PR #44，CI 全綠（測試 660 項） |
| **23:27:54** | 合併 |
| **23:29:14** | 部署完成，四段全綠（判斷文件／測試／整合／容器化部署） |
| 23:3x | 文件四處更新推 main → **容器化部署階段 `skipped`**，機器人沒有被重啟 |
| 08-25 00:0x | 補寫 D044 的驗收條件 |

### 兩件值得記的

1. **測試有效性是驗過的，不是假設的**：依 D025 的教訓（「斷言正確、輸入卻挑不出
   反例的測試比沒測試更危險」），兩處實作**各自反轉過一次**——判斷函式改成永遠回
   `False` → 4 條紅；拿掉引擎的留痕分支 → 1 條紅。
2. **只改 client 會有一個沒人要求的退化**：`bot_engine` 的留痕分支只接
   `RetryableError` / `FatalError`，所以把餘額不足降級成 `SkipCycleError` 之後，
   這種拒單會從「落帳 ＋ 通知」掉成**只剩一行日誌**。引擎補了一條分支接回來
   ——**降級一個例外的分類，會連帶改變所有接住它的地方**。

### 下一步（明天回來看什麼）

1. **2026-08-25 23:50 部位收回那一輪，要看三件事**——D043 的第一列、
   這個轉弱的市場裡策略選什麼價位、以及 D044 有沒有被觸發。
   三者的清單在 [STATUS.md](STATUS.md) 的「待驗證」一節
2. ⚠ **D044 可能整晚都不觸發，而「沒發生」不等於驗收通過**
3. 之後接 **P4-1／B6 測試替身校正 → P2-2 → M2**

---

## 2026-08-25（深夜）—— 市場翻轉，兩天前的結論失效；第一次去算簿子

**這一段沒有任何程式碼變更**，只有分析。時間在 23:5x～00:0x，
部位 `464372858` 的到期時刻（23:50:49）剛過。

### 一、驗收窗口的狀態：還沒開，而 0 列是通過

23:59 實測：部位**仍在借出中**（已滿 48.13h、完成率 100.3%），
融資錢包 0.154 USD，每一輪照樣走餘額守門檻，`pricing_decisions` **0 列**。
D043 事先寫死過「這段期間 0 列是通過，不是失敗」，所以沒有去查。

**資金回來的時刻比到期時刻晚**：上一筆 `464242253` 也是逾期 33 分才收回，
所以第一列預計落在 00:04～00:34 那幾輪。**這件事本身要記下來**
——「到期時間」不是「資金可用時間」，兩者差半小時上下，
排任何以到期時刻為準的驗收都要把它算進去。

### 二、兩天前那句「市場這三天在轉弱」已經失效

08-24 那一段（本檔上一則的第五節）用 268 根 K 線算出「08-22 之後 9.50%
每天只碰得到 2、0、0 小時」。今天用 **294 根**重算，**08-24 19:00 前後整個翻過來**：

| 日期 | ≥8% | ≥9.5% | ≥10% | ≥11% | 當日 high 中位 |
|---|---|---|---|---|---|
| 08-22 | 8h | 2h | 0h | 0h | 7.77% |
| 08-23 | 12h | 0h | 0h | 0h | 8.00% |
| 08-24 | 8h | 2h | 2h | 2h | 7.54% |
| **08-25** | **24h** | **22h** | **18h** | **14h** | **11.19%** |

08-25 是整個 12 天資料裡**唯一一天全天不低於 8%**。
成交天花板在 08-24 19:37 從**恰好 10.00%**（`0.000273972602739726` = 0.1 ÷ 365，
很像人為的整數牆）跳上去貼住 FRR 的 11.36%，之後就沒下來過。
FRR 從觀測期第一輪到最後一輪都是 11.36%，**它一路都在說市場更貴，
而我們的成交價一路貼著 9.1～9.5%**。

**而我們的錢整整兩天鎖在 9.11%。** 這一段的教訓不是「該掛高一點」
（那是 M2 的題目），是**任何寫進 STATUS 的市場敘述最多只活兩天**。

### 三、第一次去算 `market_snapshots`（不只 K 線）：簿子底部 54.5 小時被掃空 40 次

08-24 算的是 `market_candles`。**簿子那一半（`market_snapshots`）到今天才第一次有人碰。**
325 輪、54.5 小時、無一個 >20 分的缺口。

**掃單頻率**：`book_lowest_rate` 相對前一輪跳升 ≥1 個百分點共 **40 次**，
約 **1.4 小時一次**，單次最大跳幅 **4.76pp**。
其中 08-25 07:44 與 08:54 兩輪，**整個可見簿子的底部已經在 9.97% / 11.05%**
——那兩刻低於 10% 的供給被清光了。

**這比 K 線的 high 更接近實況**：K 線只說「有人以那個價位成交」，
簿子底部跳升說的是「這個價位以下的供給真的沒了」。
兩者一起看才答得出 TASKS 裡「掃單多久來一次、掃多深」那兩個量。

### 四、可見總額偶爾暴衝到 2.7 億——查證後不是資料錯誤

325 輪裡有 **26 輪（8.0%）** 的 `book_total_amount` 超過 1 億 USD，
最高 2.74 億，而常態是 1～10 M。先當成資料 bug 去查，**結果是真的**：
`book_period_totals_json` 的期別總和與 `book_total_amount` 分毫不差，
而暴衝的組成是**一筆約 1.96 億 USD 的 30 天期供給，常駐在年化 11.33～11.35%**。

它為什麼只在 8% 的輪次看得見：**簿子固定截斷在 250 檔**（`book_truncated` 永遠是 1），
250 檔是「最便宜的 250 個相異價位」。底下便宜供給一被掃薄，
可見範圍才伸得到 11.33% 那一層。

**兩個要記下來的後果**：

1. **`book_highest_rate` 的變動有一部分是簿子密度的假象，不是市場訊號。**
   它從 7.2% 走到 11.3% 可以完全不代表市場變貴，只代表底下被掃薄了。
2. **這正是 A2-a 那個 bug 的根**（TASKS 第 2 期）：候選價位越出可見範圍時
   `_queue_ahead()` 回傳同一個截斷總額，於是兩邊等待估計相同、判準退化成純比利率。
   今天看到的是它的成因側——**可見範圍本來就會呼吸**。

### 五、`ev_window_hours` 第一次有雙向證據

拿 294 根真實 K 線直接跑策略自己的 `choose_rate()`（沒有改任何程式）：

| 窗長 | 會選的價位 | 實質年化 | 平均等待 | 命中次數 | 右設限 |
|---|---|---|---|---|---|
| 24h | 沒有候選（命中數不足 `ev_min_hits`） | — | — | — | — |
| 48h | **11.23%** | 9.93% | 6.27h | 14 | 0.0% |
| 72h | 11.24% | 8.44% | 15.88h | 13 | 6.9% |
| **168h（現行）** | **9.11%** | 7.81% | 8.04h | **66** | 0.0% |

**今晚資金回來，機器人會選 9.11%**——兩道地板都擋不住它
（`market_rate × 0.85` = 8.05%、`minimum_rate` = 8.00%），市場在 11.3%。

**08-24 記的是「168 小時窗把轉弱平滑掉了」，今天是同一個機制、反方向再演一次
——它把上漲也平滑掉了。** 這比單邊的證據強：問題不是「窗對壞消息反應慢」，
是**窗就是慢**，兩個方向都慢。

⚠ **但 168h 的答案不是沒有根據**：9.11% 在窗內有 **66 次命中、0% 右設限**，
而 48h 的 11.23% 只有 14 次。這是「過度貼合 vs. 反應遲鈍」的標準取捨，
**不是拍一個新數字能解決的**。仍然照 D036：先建量測（M2），再動旋鈕。

### 六、錢：小到不該今晚動手

| 對照 | 兩天差 | 年化推估（365÷2 輪都這樣錯） |
|---|---:|---:|
| 9.11% → 11.23%（48h 窗的選擇） | **+0.0400 USD** | +7.30 USD |
| 9.11% → 11.36%（貼 FRR） | +0.0425 USD | +7.75 USD |
| 9.11% → 10.00% | +0.0168 USD | +3.07 USD |

**這一節的用途是擋住自己**：上面五節看起來都很急，而全部的錢是**四分錢**。
D030 第六節記過同一件事（這個規模下年收益都是 +0.00 USD）。
**急的是量測，不是價格。**

### 七、順手發現：D1 的「越貴借越短」今晚會第一次跨過門檻

`hold_report.py` 目前說「便宜組 2 筆、昂貴組 4 筆，**還比不出來**」
（門檻是 D040 決定五：兩組各至少 3 筆已結束的部位）。

**今晚這筆一收回，便宜組就變 3 筆**——9.11% 落在已結束部位利率中位數 9.50% 之下。
屆時第一次算得出：便宜組中位數 **45.08h** vs 昂貴組 **13.92h**。

⚠ **先把警告寫在前面，免得明天看到 3.2 倍就宣告假設成立**：

1. **這是很虛的 3**，而 D040 的門檻是「算得準」的下限，不是「可下判斷」的下限。
2. **有共變因子**：9.11% 這筆之所以借滿 48h，很可能是因為期間市場漲到 11.3%，
   借款人沒有理由提前還掉一筆便宜錢。**方向與假設一致，但機制不見得是「貴」，
   可能是「市場漲」。** 第一版 D040 報告差點用一個插值出來的 23.46h 宣告假設成立
   ——同一個坑不要踩第二次。

### 累計績效（23:5x 手算，含仍在生息的第七筆）

首次成交至今 **220.4h**、借出時數合計 173.8h → 資金使用率 **78.8%**、
毛利息 **0.5714 USD**（淨 0.4857）、實質年化（毛）**6.59%**。
比 08-24 手算的 5.32% 高，主因就是第七筆借滿了 48h。
⚠ 仍是手算：`earnings_daily` 至今 0 筆（P2-2 未做）。

### 下一步（明天回來看什麼）

1. **先驗收 D043**（查法在 D043 最後一節），再看 D044 有沒有被觸發
   ——⚠ 沒觸發不等於通過
2. **重跑 `hold_report.py`**，用第七節那兩條警告去讀它
3. 之後照舊：**P4-1／B6 → P2-2 → M2**。今天這一段讓 **M2 的優先度更硬了**
   ——第五節那張表就是 M2 要回答的題目本身


## 2026-08-26 —— D043 驗收通過、M1 完成；「為什麼老是同一個利率」查出兩個答案

**沒有程式碼變更**（文件與離線分析）。起點是兩件事：例行的監控驗收，
以及使用者的一個問題——「為什麼掛了幾次都是同樣的利率 `0.00024971` 出去？」

### 一、D043 驗收：五條全數通過，M1 完成

部位 `464372858` 於 **00:14:07** 收回（到期 08-25 23:50，**逾期 24 分**
——比 STATUS 預估的「半小時上下」稍短），`pricing_decisions` 第一列就落在那一輪。
逐條實測寫在 [DECISIONS.md](DECISIONS.md) 的 D043「驗收結果」一節，不在這裡重複。

三個值得記下來的細節：

1. **第二列（00:24:19）不是多寫的**：那一輪掛單還在場上、策略照樣評估過，
   照設計就該寫。**同一小時內兩列的每個欄位值完全相同**，因為 K 線一小時才換一根，
   而巡檢 600 秒一輪——這一點後來變成回答使用者問題的第一個線索。
2. **D041 的保護在 DB 側被證實**：資金 00:30:51 借出後又跑了 **127 輪**，
   一列都沒有多寫。
3. **D044／B5 沒有觸發**。最可能觸發的一刻（00:14 重新掛單）當時餘額 344.72436、
   掛單 344.72，**剛好沒有超**。照 D044 自己寫的：「沒發生」不等於通過，維持 🟡。

### 二、順手重跑 `hold_report.py`：門檻跨過了，但比預警的還虛

第七筆（`464372858`，48.39h／101%）收回後：已結束 **7 筆**、
平均完成率 **43.6% → 51.8%**，D1 的分組門檻如 TASKS 預先寫的那樣第一次被跨過
——便宜組 3 筆、中位數 **45.08h**，昂貴組 4 筆、中位數 **13.92h**，差 3.2 倍。

**但便宜組那三筆是 1.84／45.08／48.39h。** 中位數完全靠兩筆借到期的撐著，
第三筆只有 1.84 小時。TASKS 預先寫下的兩條警告（很虛的 3、共變因子）成立，
**要再加第三條：組內離散到中位數沒有代表性。**
預先把警告寫在前面這件事本身有效——看到 3.2 倍的當下沒有宣告假設成立。

### 三、使用者的問題：為什麼老是 `0.00024971`

查下去發現**兩個各自獨立的答案**，完整敘述在 [DECISIONS.md](DECISIONS.md) 的 **D045**。

**機制那一半**：候選集是「168 小時窗內出現過的 K 線 high，量化去重」，
一根 K 進窗待滿 7 天，加上巡檢比 K 線密（600 秒對 1 小時），
於是同一小時內輸出必然相同、跨天也很難換人。這是設計的必然，不是缺陷。

**真正的發現那一半**：把 125 個候選的實質年化全部印出來之後——
**從 8% 到 11.35%，實質年化全部落在 6.9%～7.9% 之間，第 1 名贏第 2 名 0.0005 個百分點。**
所謂「選中」，是在一堆實質上打平的選項裡取 argmax。
**D3 記的「一根 K 滾出窗，價格目標就自己跳一階」與這是同一件事**：
不是價格目標會亂跳，是它本來就沒有被決定。

### 四、`offer_wait_forecasts` 存了兩週，今天第一次拿去對答案

D038 起就在記「掛出去那一刻我們以為要等多久」，
而實際成交時間一直躺在 `loan_offers` 與 `funding_positions` 裡。**從來沒有人對照過。**

**五筆全部高估**，平均預估 7.09h 對實際 1.80h＝**3.9 倍**（逐筆 1.6×～28.7×）。
而 `W` 正是算式 `r × P ÷ (W + P)` 裡懲罰高利率的那一項——
**使用者的直覺「應該掛得到更漂亮的利率」，方向與這個量測一致。**

**但今天不改價，三條理由**（完整在 D045）：五筆全落在 9.11%～9.50% 這個
模型自己選出來的窄帶、9.5% 以上零樣本；唯一的反例正好在高價端
（08-19 掛 9.78%，**在場 34.2 小時沒成交**）；而只看成交的單會系統性看低等待
——右設限，D040 已經踩過的同一個坑。

另有一個**方向相反**的偏誤：`P` 寫死 48h 而實測完成率 51.8%，那一項是把高利率評得**過高**。
兩個偏誤反向、量級都還沒定，**淨效果不能用手算宣告**。

**決定**：建校準報告（`core/wait_time.py` ＋ `scripts/wait_report.py`），
與 D040 的 `hold_time` ＋ `hold_report` 成對——算式的 `P` 與 `W` 到這裡都有了
自己的校準報告。分支 `feature/measure-wait-accuracy`。

### 五、兩個順帶算出來的數

- **`pricing_decisions` 幾乎不會長**：60 小時 2 列，因為資金 98% 的時間鎖在部位裡。
  一年約 500 列。不擋 M2（M2 吃 `market_snapshots` 454 列、`market_candles` 316 根），
  但別指望靠它累積樣本。
- **市場三天翻了三次面**：08-24「轉弱」→ 08-25「翻上去」→ 08-26「又回落」
  （high 中位 11.19% → 9.82%，≥11% 從 14h 掉到 2h）。
  08-25 算出的「48h 窗選 11.23%」，追的是一個**只活了一天**的尖峰。
  **這第三個方向站在「不要動 `ev_window_hours`」那一邊。**


## 2026-08-26（續）—— D045 實作：算式的兩項到這裡都有校準報告了

分支 `feature/measure-wait-accuracy`。新增 `core/wait_time.py` ＋
`scripts/wait_report.py` ＋ `tests/unit/test_wait_time.py`，**沒有動任何策略參數**。

### 為什麼長成 hold_report 的樣子

    實質年化 = r × P ÷ (W + P)
                   ↑        ↑
          hold_report  wait_report
            （D040）    （D045）

不是為了對稱好看：**兩支報告要回答的是同一個算式的兩個輸入**，
而 D040 已經把「量測報告該有什麼樣子」試過一遍——右設限分開報、
偵測延遲跟著印、小樣本不報中位數。照抄那個形狀，比重新想一個省事，
而且下一個人只要讀懂一份就讀得懂另一份。

`core/hold_time.py` 的 `_parse` 因此改名成公開的 `parse_moment`：
兩支模組要用同一條規則讀同一批時間戳，各寫一份的話，
「舊列帶 `+00:00`、新列帶 `+08:00`」這件事就會有兩個各自演化的答案。

### 🔴 實跑正式資料抓到一個測試抓不到的 bug

第一版的合併規則只有一條：**連續 ＋ 同利率的重掛算同一段**。
理由是 08-15～16 那兩天每輪取消重掛，一個掛單意圖在 DB 裡是一百多列。

拿正式 DB 一跑，報告說「可校準 2 段」——**而我手算時明明有 5 筆。**

原因是 08-20 15:15、08-21 16:09、08-21 19:11 這三張 9.50% 的單**各自都成交過**，
資金出去又回來，但利率剛好相同，於是被併成一段。**五個校準樣本靜靜地變成兩個**，
而報告照樣印得出來、數字（3.2 倍）也不離譜到會讓人起疑。

修法是加第二條規則：**中間成交過就切段**。

**這件事本身比那個修正重要**：它是 D026「靜默失效」的又一次現身，
而抓到它的不是測試——**測試是照著我當時的理解寫的，而錯的正是那個理解**。
抓到它的是「拿正式資料跑一次，然後跟手算的結果對一對」。
D041 記過「日誌印錯還有鄰行可以拆穿」，這裡的鄰行就是那份手算。

順帶還修掉一個小的：`spread_count > 1` 時期同一輪掛出的兄弟單，
時間戳相同，前一張的存活區間長度是零，本來會印成「至少掛了 0.00 小時沒有成交」
——那不是沒等到，是根本還沒開始等。改成單獨標記、不計入右設限。

### 驗收：四條全過

四條條件在合併前就寫進 D045（照 D041／D043 的做法），
實測結果寫在 D045 的「驗收結果」一節，不在這裡重複。

測試 660 → **676 項**（新增 16）。依 D025 的教訓，
**兩處實作各自反轉過一次確認新測試會紅**：把「成交切斷合併」關掉，
`test_成交會切斷合併即使利率相同` 紅；把同輪兄弟單的判斷關掉，
`test_同一輪掛出的兄弟單不算一段等待也不計入右設限` 紅。

### 這一輪沒有做什麼

**沒有改利率、沒有改 `ev_window_hours`、沒有改 `estimate_wait()`。**
使用者問的「應該可以掛到更漂亮的利率吧」，量測結果站在他那一邊——
但站在他那一邊的是**方向**，不是**幅度**，而幅度正是動手需要的東西。
五筆樣本全落在模型自己選出來的 9.11%～9.50% 窄帶、9.5% 以上零樣本、
唯一的反例（08-19 掛 9.78%，34.2h 沒成交）就在要外推過去的那一端。
再加上 `P` 那個方向相反的偏誤，**淨效果不是手算能宣告的**。那是 M2 的題目。


## 2026-08-26（收工）—— 使用者提案：每十分鐘該不該調高？查出往上調價沒有判準

**沒有程式碼變更**（規劃與文件）。使用者的原話：

> 「每十分鐘跑的那一次，就要看一下目前市場的單子，和我現在掛出去的單子相比，
> 到底適不適合調高，調高成交的機率符不符合機會成本？如果算出來該調高，
> 是不是應該可以砍掉目前還沒有成交的這筆單子，趕快把利率調高呢？」

完整敘述在 [DECISIONS.md](DECISIONS.md) 的 **D046**，這裡只記查證過程與排序。

### 查證：四個事實，三個支持提案、一個要修正它

**一、每十分鐘確實已經在比了。** 引擎每輪的順序是：重新定價 → 量測閒置 →
`_plans_match()`（2% 容差）→ 往下調價的守門檻 → 取消重掛。提案的前半已經有了。

**二、但往上調價完全沒有判準。** `_cheaper_repost_is_not_worth_it()` 的第一件事是
`if candidate.rate >= live_rate: return None`——**它只管往下**。
往上的實際規則是「差超過 2% 就直接取消重掛，不問划不划算」。
**兩個方向一個有測試一個沒有，而沒有測試的正是使用者問的那個。**

**三、D034 為「往上不設限」給的理由被推翻了一半。** 原話是「往上調，
多賺的利息是**確定的**」——**那句話的前提是那張單會成交**。沒成交拿到的不是
「少賺一點」，是 0%。而反例 D045 的報告裡就列著：08-19 掛 9.78%，**在場 34.2 小時
沒有成交**。所以往上調價是「確定的閒置成本 × 不確定的成交機率」，兩邊都不確定。

**四、機會成本每輪都在算，但算完就丟掉。** `_log_idle_time()` 每輪算出
`idle_hours` 與 `forgone`，寫進日誌，然後**函式回傳 `None`**——
沒有任何判斷式讀得到。使用者要的「符不符合機會成本」，那兩個數字已經算好了，
只是沒有出口。

### ⚠ 提案裡要修正的一點：閒置久不是漲價的理由

這是直覺最容易走反的地方，所以寫在 D046 的顯著位置：

- **「等太久」是「這個價位成交比預期慢」的證據 → 它支持降價**
- **支持漲價的證據是市場整體成交價往上走**（別人成交在更高的價位）

兩者混在一起，會在**市場走弱、我們的單掛不出去的時候去漲價**——最糟的時機。
規則裡這兩個訊號要分開餵。

### 順帶量到：這個機制的作用窗口很短，價值在尾端

用今天剛上線的 `wait_report.py` 一查：08-18 之後六段成交的掛單，
**在場時間平均 2.10 小時、中位數 2.05 小時**——約 12 輪巡檢。
其餘時間資金都鎖在部位裡，場上根本沒有單。

**所以平常只有十幾輪可以作用，真正需要它的是 08-19 那種 34 小時（205 輪）的長尾。**
不是不做的理由，而是**設計時要拿長尾當主場景，不要拿中位數**。

### 排序：先落反事實，不先改規則

拆成兩段，**第一段現在就能做且不擋 M2**：

- **M1-c 重掛反事實落地**（新增在第 1 期）：`_log_idle_time()` 回傳算出來的值；
  每輪有場上掛單時落一列「保住 vs 改掛」的並排比較。
  **驗收的第 3 條是「行為零變化」**——部署前後同一份市場資料下掛出去的單要完全相同。
- **A2-b 重掛政策**（第 2 期，等 M1-c ＋ M2）：門檻設多少，用回測回答。

**為什麼不直接改規則**：沒有反事實資料，門檻就只能拍，而那是 `target_queue_usd`
的死法（D032）。使用者也明講了「先別急著做下一步的 M2，等規劃好再往下」
——這一則就是那個規劃。


## 2026-08-28 —— M1-c 反事實落地，M1 三塊到齊

**分支** `feature/record-repost-counterfactual`，**PR #46**，00:36 部署完成。
決策與完整理由見 [DECISIONS.md](DECISIONS.md) 的 **D046「第一段實作結果」**一節。

### 做了什麼

新表 `repost_comparisons`（34 欄），場上有掛單的每一輪落一列「保住 vs 改掛」的
並排比較。**只落資料，不改任何行為。**

三處改動裡，只有一處是原本沒預料到的：**必須新增
`ExpectedValueStrategy.evaluate_rate()`**。原本以為場上那張單的實質年化可以從
`last_evaluation` 撈——實作時才確認不行，因為 `choose_rate()` 的候選只取自窗內
出現過的 `high`，而 `plan.rate` 還經過成交價下限與 spread 的加工
（`build_offer_plan()`），**所以實際掛在場上的利率通常不在候選集裡**。
兩邊必須用同一個函式、同一窗 `high` 算，比出來的差額才是市場的差、不是尺的差。

### 「行為零變化」是怎麼驗的

**沒有靠測試套件當唯一證據。** 把 `HEAD` 的舊 `bot_engine.py` 抓出來，
與新版並排載入，餵**同一份**市場資料各跑一輪，比對 `client.offers`、
`cancel_calls`、離開方式——**六個情境全部相同**：場上沒單、維持不動、
往上調價重掛、往下重掛不划算、餘額不足、拿不到簿子。

而更根本的保證在結構上：兩個判斷的求值提到 `if` 之前，**求值順序與短路完全不變**
（`matched` 為真時守門檻依然不會被呼叫，連它那條棄權日誌的行數都一樣）。
`action` 因此是**讀出來的、不是預測的**——落在 `_log_idle_time()` 旁邊的話就得
預測下面的判斷結果，而預測與實際可能分岔，那正是 D046 驗收條件 2 要擋的事。

### 兩件實作時才算出來的事

1. **一列 418 位元組**（寫 500 列再 VACUUM 量的），比 `pricing_decisions` 的
   4,137 B **小一個數量級**——那張表的兩排候選集就佔 2.6 KB，這張沒有陣列欄位。
   最壞情況（每輪都有場上掛單）一年 21 MB。
2. **`live_effective` 可以是 NULL，而那一列照樣要寫。** 窗內命中不足
   `ev_min_hits` 時算不出實質年化，此時 `evaluate_rate()` **仍回一列**並填上實際
   `hits`：「一次都沒掃到（0）」與「掃到了但不夠（3 < 5）」意義完全不同，
   回 `None` 會把兩者壓成同一個空白。**08-19 那張掛了 34.2 小時沒成交的單落下來
   就長這樣**——而那正是驗收條件 4 要靠的那一列。

### 部署驗證

- **正式 DB 的副本上先跑過一次初始化**：新表建起來、索引在、
  **9 張舊表列數一格沒動**（`loan_offers` 4174、`market_snapshots` 529……）
- 部署後容器 healthy，`repost_comparisons` 已在正式 DB 建起來（0 列）
- 測試 650 → **675**（單元＋功能），整合 26 項另計

### ⚠ 還沒驗到的

**驗收條件 1／2 要一個「場上有掛單」的輪次**，而部位 `464505426` 到期
（08-28 00:30）後截至 00:37 **尚未收回**——08-26 那次也逾期了 24 分鐘。
資金一回來就會掛單，那是第一個機會。條件 4 只能等 08-19 那種長尾重現。

**「沒發生」不等於通過**（D044 自己立的規矩），所以狀態維持 🟡。

> **→ 後續（08-29）**：資金確實在 00:46 回來、也掛了單，但**那張單在下一輪
> 巡檢之前就成交了**，所以「場上有掛單的輪次」始終沒有出現。
> 這一段當時寫的「資金一回來就會掛單，那是第一個機會」**猜錯了機制**
> ——會掛單沒錯，但單子撐不到下一輪。結果見本檔 2026-08-29。


## 2026-08-29 —— M1-c 的驗收：0 列是對的，而「為什麼是 0」才是收穫

**無程式碼變更。** 這一則是 08-28 部署後 38 小時的驗收與量測。

### 驗收結果：兩條通過、兩條「測不到」

| 條件 | 結果 |
|---|---|
| 1 負向（沒單就不寫） | ✅ **225 輪都守住** |
| 1 正向（有單就寫一列） | 🔴 **測不到**——場上有單的輪次是 **0** |
| 2（`action` 與日誌一致） | 🔴 **測不到**——同上 |
| 3（行為零變化） | ✅ 已驗（六情境並排跑） |
| 4（長尾答得出候選價位） | ⏳ 等長尾重現 |

部署後 **228 輪**：餘額不足略過 225、掛出新單 3、**走到重掛判斷 0**。
**0 ERROR／0 WARNING／0 次寫入失敗。**

### 為什麼一輪都沒有：單子秒成交

| 掛出 | 等待 | 預估 | 高估 |
|---|---|---|---|
| 08-28 00:46 | **0.06h**（3.6 分） | 6.91h | **108.6×** |
| 08-28 02:07 | **0.00h**（11 秒） | 6.90h | — |
| 08-28 13:44 | **0.10h**（6 分） | 6.89h | **71.0×** |

於是每輪開頭只有兩種狀態：**單還沒掛**，或**單已經變成部位**。
中間那個「躺在場上等成交」的狀態，600 秒的巡檢間隔一次都沒抓到。

**D046 原本估「作用窗口約 12 輪」，實測是 0 輪——差一個數量級。**
M1-c 因此是**一台裝好但還沒被觸發過的儀器**。這不是拆掉它的理由
（它要等的正是市場走弱、單子掛不出去的那種輪次，也就是最需要重掛判準的時候），
但要誠實記著：**在目前這個「掛什麼都秒成交」的區間裡，它一列都不會長。**

### 順帶把兩份校準報告都重跑了

- **`wait_report.py`**：可校準樣本 5 → 7 段，**總倍數 3.9 → 5.4**，
  逐筆 1.6×～**108.6×**、中位數 11.3×，**七段全部高估、零段低估**。
  ⚠ 三個新樣本**全部落在年化 9.11% 這一個點**，帶寬更窄了；
  唯一的帶外實測仍是反方向那筆（08-19 掛 9.78%，34.20h 沒成交）。詳見 D045 追記
- **`hold_report.py`**：八筆已結束，**平均完成率 51.8% → 57.9%**（借到期 4 筆）。
  分組變成 4 vs 4（便宜組中位數 46.67h、昂貴組 13.92h），
  ⚠ 仍低於 D040 定的每組 5 筆，且便宜組是 1.84／45.08／48.26／48.39h
  ——**離散到中位數沒有代表性**這條警告沒有解除

### 使用者問的「怎麼還是掛一樣的利率」

查了 08-28 00:46 那一輪的 124 個候選，**曲線不是一個平台，是兩個峰**：

| | 年化 | 估計等待 | 實質年化 |
|---|---|---|---|
| 低價峰（選中） | 9.11% | 6.91h | **7.9673%** |
| 高價峰 | 11.31% | 21.13h | 7.8561% |

**低價峰只贏 1.42%**，而決定這個排序的正是 `W`——已知高估 5.4 倍的那一項。
純算術把倍數除掉，排序會翻轉（8.79% vs 10.17%）。

🔴 **但這不是改價的授權**，三個理由：
(1) 倍數只在 9.11%～9.50% 這個模型自己選出來的窄帶量到，帶外零樣本；
(2) 唯一的帶外實測是反方向的 08-19（9.78% 掛了 34.20h 沒成交）；
(3) 11.31% 高過當時可見簿子最高的 10.35%，掛上去是隊伍最末端，
而 `_queue_ahead()` 在那裡回 `None`（A2-a）。
**這正是 M2 要回答的題目**（D036：先建量測，再改參數）。


## 2026-08-29（續）—— 帶外樣本出現，順手把量測的兩個誠實度缺口補起來

**分支**：`feature/calibrate-test-doubles`

### 這一段的起點是使用者的一個問題，不是待辦清單

15:40:50 部位 `464715251` 收回（借了 **25.84h**，完成率 54%），15:51 重新掛單
`5096173429` @ **年化 10.95%**（日利率 0.0003）、345.02 USD——**模型第一次選了
9.11% 以外的價位，而且落在 D045 說「零樣本」的帶外**。同時
`repost_comparisons` 開始長：0 列 → 9 列，**M1-c 那台儀器第一次被觸發**。

使用者問的是：掛這麼高真的有機會成交嗎，會不會等太久、又被提前還款，兩頭落空。

### 查出來的三件事

**1. 有機會，不是死單。** 16 天 384 根 1 小時 K 線裡有 **48 根**的最高成交利率
碰到 ≥10.95%，平均 **每 8 小時一次**（對照 9.11% 是每 1.9 小時）。掃到我們這格
的那 48 根中位成交量 583 萬 USD／小時，而排在我們前面 100～275 萬——掃到通常吃得到。

**2. 🔴 等待高估倍數不是常數，是一個駝峰，而七個校準樣本全落在峰頂。**
把模型對整條候選階梯的 `W` 反推出來（`W = P × (r ÷ eff − 1)`）跟 K 線掃單頻率
並排：8.0% 是 1.0×、**9.14～9.55% 是 4.2×**、10.95% 只有 1.4×、11.3% 回到 1.0×。
**這推翻了 D045 本文那句推論的前提**（「W 被系統性放大 → 選出的價位會偏低」
只有在偏誤均勻時才成立）。完整敘述與表格見 DECISIONS.md 的 **D045 追記**。

**3. 取捨算得出分水嶺：借出時間 > 7 小時，高價就贏。**
代數上是 `借出時間 > W_高 − 2 × W_低`。16 筆已結束部位有 9 筆超過 7h（56%）。
反過來「躺多久才不划算」：用實測平均 17.9h 是 **18.9 小時**、用中位 11.6h 是
**12.6 小時**——**目前唯一有資格當 A2-b 判準的候選數字**（D046 說往上調價
從頭到尾沒有判準）。⚠ K 線頻率是必要條件不是充分條件，08-19 那筆 9.78%
（頻率說 2.6h、實際掛 34.2h）就是反例。

### 順手修掉的兩個量測誠實度缺口

**`describe_spell()` 把「還在計時」講成「被下一張單取代」。** 那筆 10.95% 的單
還躺在場上、才等不到兩小時，報告卻說它被取代了——**偏偏它就是撐起上面整段
分析的那個帶外樣本**。「有沒有下一段」在 `build_spells()` 裡本來就查得到
（最後一段的結束點取 `now`），只是沒被記下來。加 `WaitSpell.replaced` 與
`WaitSummary.ongoing`，敘述分兩種講法。測試 688 → 691。

**B6：七支測試替身實打校正**（TASKS.md 的 B6，D027 追記）。三個公開端點 ＋
四個唯讀私有端點各打一次，**七支裡四支跟替身不一樣**：ticker 是 17 欄不是
4 欄且 [13][14] 真的是 `None`；**offer 陣列是 21 欄而替身寫 16**，[10]=STATUS
真實是 `'ACTIVE'`；`fetch_balance` 指定了 `type='funding'`，`info` 裡照樣混著
`exchange`／`UST`；可用餘額是八位小數 `0.00175079`。

**最值得記的是 16 vs 21**：`_parse_offers()` 的 docstring 從 08-16 起就寫著
「21 欄」——**程式碼知道真相、寫進了註解，卻沒有流進替身，而兩邊沒有人去對過**。
所以 D027 補了第 4 點：註解裡寫下的真實形狀，要有一條測試直接吃未加工的真實列。
測試 691 → **699**。

⚠ `funding_credits`／`funding_loans` 這次抄不到（打的當下沒有部位在生息，
回空陣列），`submit`／`cancel` 是寫入端點不動真錢抄不到——B6 維持部分完成。


## 2026-08-29（續二）—— 那張 10.95% 成交了，而它一次回答了三個懸著的問題

**起點是「查一下交易」**，結果交易狀態已經比 STATUS.md 記的往前走了一步。

### 一、10.95% 成交：5.19 小時，帶外樣本第一次落地

`5096173429` 15:40:56 掛出、**20:52:13 成交**（部位 `464812689`）。
上一段（續）寫的時候它還躺在場上，整段駝峰推論靠的是反推值。

| | 模型估的 | 實際 | 倍數 |
|---|---|---|---|
| 平均（策略用的那個） | 11.81h | 5.19h | 2.3× |
| 中位數 | 5.50h | 5.19h | **1.06×** |
| 上一段反推的 K 線掃單頻率 | 8.0h | 5.19h | 1.5× |

**駝峰站住了**：9.11% 那批 29×～108.6×，10.95% 只有 2.3×。
上一段那句「把 W 除以 5.4 在兩端都會錯」從推論變成量測。

### 二、順著查下去，發現問的問題可能一直是錯的

D045 從頭到尾在問「W 該除以多少」——本文 3.9 倍、續一 5.4 倍、
續二駝峰。**三次都在問除數，沒有人問過「為什麼用平均」。**

`estimate_wait()` 一次算三個統計量、`offer_wait_forecasts` 三個都存了，
但策略只用 `mean`、報告也只印 `mean`。八個樣本並排：

| 統計量 | 總量比 | 逐筆中位 | 最好 | 最差 |
|---|---|---|---|---|
| 平均（策略正在用） | 4.25× | 10.2× | 1.6× | 108.6× |
| **中位數** | **1.67×** | **5.1×** | **0.9×** | **9.0×** |
| 四分之三 | 6.58× | 16.3× | 2.5× | 165.1× |

**八段全數中位數較準。** 離散度差更多：平均橫跨快兩個數量級，中位數只有 10 倍。

⚠ **沒有改策略**，只把三個數字並排進報告（PR #49）。離線試算換統計量會把
選中價位移動約 2 個百分點，那是 M2 的題目——先改參數再建量測正是 D036 的錯誤。

### 三、hold_report 那句「還比不出來」一直在暗示一件不會發生的事

分組門檻取已結束部位的利率中位數 = 9.11%，而 9.11% 同時是眾數（16 筆裡 10 筆），
`<` 把整叢掃進昂貴組 → 便宜組 1 筆／昂貴組 15 筆。
報告原本只說「要兩組各 3 筆才算得準」——**讀起來像「再等幾筆就會好」，而那是錯的**。
模型每選一次同樣的價位，就同時把中位數釘在原地、又往昂貴組加一筆。
新增 `RateSplit.degenerate` 把這件事講出來。**說「還不夠」跟說「這樣分下去
永遠不夠」是兩件事。**

順帶分開兩個「相同」：`0.00024972` 與 `0.00024971` 的年化都印成 9.11%，
但 `<` 只看日利率——只講「完全相等」那個數字，讀的人會照逐筆數出多一筆
然後以為報告算錯。

### 四、趁部位在生息，把 B6 的 `funding_credits` 補打完

B6 當初抄不到就是因為打的當下沒有部位。這次趁 `464812689` 還在生息補打一次
唯讀查詢，抓到三件替身不知道的事：**22 欄**、`[21]='tBTCUSD'`、
**`[8]=RATE_TYPE` 真實是 `'FIXED'` 而替身留 `None`**（D027 那個坑的第三次現身）。

**這是 B6 第一次做到「整條鏈路」**：交易所原始列 → `_parse_positions()` → DB，
四個欄位逐一對上。🟡 `funding_loans` 仍抄不到（要「借走但還沒用掉」的時刻）。

### 五、寫在開獎之前的一條預測

部位還在生息，結果還不知道。**這筆 10.95% 要借滿 25.0 小時**才追平 9.11%
那批的實得年化 9.07%（`9.07 × 5.19 ÷ (10.95 − 9.07)`）。
歷史 13 筆已結束 credit 部位裡只有 5 筆（38%）借那麼久。
**輸了尤其要記**——推導與但書見 D045。

### 順帶抓到的：STATUS 的測試數是錯的

STATUS 寫「測試 725 項（699＋26）」，實測 `pytest tests/` 在 main 上是 **712**，
**差 13**。手抄進文件的數字會漂，這次改成寫「要用時重跑」。

**產出**：PR #48（B6 補打，712 → 714）、PR #49（兩支報告，714 → 719），
兩個都全綠、**都還沒合併**。文件更新 STATUS ＋ DECISIONS（D045 三則追記、
D027 一則追記）＋ 本檔 ＋ TASKS。


## 2026-08-29（續三）—— 開獎：掛高輸了，而輸法指出我們一直在調錯的那一項

**22:50:57 部位 `464812689` 被提前還款，只借了 1.98 小時（完成率 4%）。**
上一段（續二）寫的驗收條件是「借滿 25.0 小時才追平」——**❌ 差了 12 倍**。

### 一、結果本身

| | 值 |
|---|---|
| 名目年化 | 10.95%（**至今最高**） |
| 等待 `W` | 5.19h |
| 持有 `P` | **1.98h** |
| **實得年化** | **3.02%**（**至今最低**，前一個最低 5.26%） |

**等待時間是持有時間的 2.6 倍。** 9.11% 那批實得約 9.07%，這一筆差了三倍。

### 二、🔴 真正的收穫：誤差幾乎全部來自 `P`，不是 `W`

D045 從 08-26 到 08-29 寫了本文加三則追記，**全部在講 `W`**。把這一筆拆開：

| 用什麼算 | 實質年化 |
|---|---|
| 模型當時算的（`W`=mean 11.81、`P`=48） | 8.79% |
| **`W` 換成實際 5.19、`P` 仍假設 48** | **9.88%** |
| `W` 用中位數預估 5.50、`P` 仍假設 48 | 9.82% |
| `W` 與 `P` 都用實際 | **3.02%** |

**就算 `W` 估得完全正確，只要 `P` 還寫死 48，仍會算出 9.88%。**
`W` 的誤差約 1 個百分點，`P` 約 7 個。

**三則追記都沒錯，但它們一直在調整比較小的那一項。** 真正的槓桿是 D1
（`P` 寫死 48、實測完成率 35.3%），而 D1 從 08-22 量測完就標著 `[~] 等 M2`
沒再動過。**STATUS 的「接下來三步」因此把 M2 升為第一。**

⚠ 仍然是一筆。方向與「越貴借越短」一致（最高名目、第二短持有），
但 9.11% 那一叢的持有從 0.50h 橫跨到 48.39h——**組內變異遠大於組間**。

### 三、我今天稍早寫錯的一句話，當天就被推翻了

續二在 `hold_report` 加的退化提示寫著「要比得出來只能等模型選到**別的價位**
（像 08-29 的 10.95%）」。**那筆收回後落進昂貴組**，便宜組還是 1 筆。

便宜組收的是 `< pivot`，「別的」不夠，**只有「更低」有用**；更高的價位只會
讓失衡更嚴重。已改並補迴歸測試（用的正是那筆的形狀）。
**錯的不是數字，是那句話指的方向**——同一族的錯誤 D038 記過一次：
數字對得上不等於機制猜對了。

### 四、`W` 與 `P` 從來沒有被乘起來過

`hold_report` 只有 `P`、`wait_report` 只有 `W`，**乘起來那個數字每次都是手算的**
（今天手算了兩次）。於是「名目 10.95% 最高、實得 3.02% 最低」這種事
**沒有任何一份報告看得出來**。

新增 `WaitSpell.realized_effective` 與 `WaitSummary.realized_effective`
（時間加權 `Σ(r×P) ÷ Σ(W+P)`）。正式資料：**12 段已完成一輪，實得年化 7.99%**。
兩個誠實度界線寫進程式：仍在生息中的不列入（`P` 是會長的下界）、
沒等到成交的期間不在分母裡（所以 7.99% 偏樂觀）。

### 這次做對的一件事

**條件寫在開獎之前。** 如果是事後才算 25.0 小時這個門檻，很難不被挑成剛好贏的
那個。D044 立的規矩第一次真的擋住了東西——輸的結果被完整記下來，
而不是被重新敘述成「其實也還好」。

**產出**：PR #48 ／ #49 已合併（測試 712 → 719），PR #50 待審（→ 726）。
文件更新 STATUS ＋ DECISIONS（D045 開獎一節）＋ CHANGELOG ＋ 本檔 ＋ TASKS。

---

## 2026-08-30 —— 掛越久沒成交，模型對自己越有信心（D047）

**起點是一個很普通的問題**：「下一步要做什麼」。讀完 STATUS ＋ TASKS 之後
先去看正式環境對不對得上，結果三件事跟文件不一樣：PR #50 已經合併（文件寫「待審」）、
`repost_comparisons` 從 31 列長到 **111 列**、`pricing_decisions` 從「60 小時 2 列」
變成一天 72 列。**而 08-29 22:51 掛出的那張 10.91%，掛了 13.24 小時還沒成交。**

### 一、本來只是想確認它有沒有在降價，結果看到反過來的東西

把 `pricing_decisions` 逐小時攤開：

| 時間 | `W` 平均 | 實質年化 |
|---|---|---|
| 08-29 22:00 | 9.95h | 9.03% |
| 08-30 00:00 | 9.43h | 9.11% |
| 08-30 08:00 | 7.84h | 9.37% |
| **08-30 12:05** | **7.33h** | **9.46%** |

**15 個小時區間單調上升，一次都沒回頭**，而選中的價位完全沒變
（`0.00029879`，逐位元相同）。同一份快照裡最近 385 分鐘的 10,000 筆成交
**中位數是年化 5.47%、最高只有 10.00%**——**沒有一筆碰到我們掛的價位。**

市場明明在離開，模型卻越算越好看。

### 二、成因：右設限的補值被窗尾夾住

`estimate_wait()` 對等不到命中的起點記「至少等到窗尾」，而
**離窗尾越近的起點，這個數字越小**。尾端的乾旱每多長一小時，就多灌進一個
**很小**的「等待」把平均往下拉——**乾旱靠自己的新鮮度替自己脫罪。**

離線把窗尾逐步往回退（唯讀，沒改程式），重跑的數字與 DB 逐列吻合：

| 窗尾往回 | `W` 平均 | 右設限根數 | 實質年化 |
|---|---|---|---|
| 16h（乾旱剛開始） | 10.52h | 0 | **8.95%** |
| 8h | 8.54h | 8 | 9.26% |
| **0h（現在）** | **7.33h** | **16** | **9.46%** |

**命中次數全程都是 43**——變的只有設限那一段怎麼被記帳。

### 三、它解釋了一件本來被歸成「巧合」的事

`repost_comparisons` **111 列全部是 `hold_matched`**，候選利率與場上利率
逐位元相同。08-29 的 STATUS 把這寫成「情境還沒出現」。
**不是**——是這個正回饋圈把它擋掉了：
市場離開 → 乾旱累積 → `W` 被拉低 → 這個價位算起來更好 → 更不可能改掛。

**M1-c 的驗收條件 2 因此不是「還沒被觸發」，是「被擋住」。**

### 四、它與 D045 不衝突，因為兩者看的是不同半邊的資料

D045 的八個校準樣本**全部是等到成交的期間**（結論：高估 4.25 倍），
而這個機制**只活在右設限的期間裡**（方向相反）。兩件事各自為真。
🔴 **`P` 寫死 48、以及本則，兩項都在把高利率評得過高，而它們是相乘的。**

### 五、刻意先不修

離線試過三種補值（現行／丟棄／補一整窗），選中價位 10.91%／10.91%／10.42%。
**換一個補值就是換一個拍出來的常數，正是 `target_queue_usd` 的死法（D032）。**
所以今天的產出是**把機制與量測寫下來**，程式一行沒動。

### 六、M2 因此拿到第二條「可以跑」的驗收標準

原本只有「跑 `orderbook_depth` 要重現賣在區間底部」。今天補上：
同一份 168 根 K 線、`P=48`、現行補值 → **必須選出 10.91%**；
`P` 換成 16.93h → **必須選出 9.09%**。
第一個數字與正式日誌相符，所以這不是自己跟自己對答案。

### 場上那張單刻意不動

機會成本累計 **0.0569 USD**，離 08-19 那個 34.20 小時的紀錄還有距離。
**它是第一段「有預估值、而且實際等待已經超過預估」的右設限期間**
（掛出時預估平均 9.95h／中位數 4.50h）——過去八個校準樣本全是高估，
這是第一個反方向的。對 M2 的價值高過那 0.06 USD。

**產出**：DECISIONS 新增 **D047**（含索引列）、TASKS 新增 **D5** 並補上 M2 的
第二條驗收標準與原料清單、STATUS 改寫（順帶修掉三處過期敘述）、本檔。
**程式碼零變更**，下一步是分支 `feature/backtest-replay`（M2 第 1 步：重播器）。

---

## 2026-08-30（續）—— M2 第 1 步：重播器，以及一條沒有人跑過的驗收標準

D047 寫完之後直接開 `feature/backtest-replay` 做 M2 的第 1 步：
**餵歷史 K 線，回答「當時這個策略會掛什麼價」。**

### 一、三條界線先寫在 docstring 裡，再寫程式

1. **重播呼叫的是策略本尊**（`strategy.choose_rate()`），不是它的副本。
   D035 那次回測是手寫的一次性腳本，於是「回測說會選 A、正式環境選了 B」
   永遠分不清是策略不同還是回測寫錯。
2. **重播點看不到未來**——切窗在 `replay()` 裡做，不靠呼叫端自律。
   **偷看未來的回測一定會很好看，而且看起來完全正常。**
3. **唯一被轉的旋鈕是 `P`，而且轉的是策略自己在讀的那個屬性**
   （`assumed_hold_hours()` 暫時改 `offer_period` 再還原）。
   一旦重播自己算一次 `r × P ÷ (W + P)`，第 1 條就沒了。

### 二、驗收 25/25，但驗收標準本身出了問題

跑 `--verify` 對照 `pricing_decisions`：**正式紀錄 25 列，逐點一致 25 列。**

但 PLAN.md 給 M2 寫的驗收標準是「拿它跑 `orderbook_depth`，要能重現賣在區間底部」，
而那條**跑不起來**：`orderbook_depth` 沒有 `choose_rate()`（它吃簿子不吃 K 線），
而且「賣在區間底部」是 08-16～17 的結論，`market_snapshots` **08-23 才寫第一列**。

🔴 **驗收標準是 08-17 寫的，比它依賴的那張表早了六天，而十三天內沒有人回頭確認
它可不可跑。** 這是 D026「靜默失效」在文件層的第一次現身——
**一條沒有人跑過的驗收標準，跟沒有驗收標準的差別只在於它看起來像有。**
換掉的理由與界線寫成 **D048**，並誠實記著：換上的這條**比原本那條弱**
（抓不到「策略本身錯了」，因為重播與正式環境跑同一條路徑，會一起錯）。

### 三、寫測試的時候自己踩了一次「換掉自變數」

D047 的釘子要「把同樣長的窗往回滑 16 小時」，我第一版寫成 `candles[:-16]`，
而 fixture 只有 168 根——於是那不是「往回滑」，是**把窗從 168 縮成 152**。
答案差了半個百分點（9.48% vs 8.95%），而測試只會說「數字對不上」。
**第一直覺是重播器算錯，實際上是測試自己換掉了自變數。**
fixture 因此改存 200 根，理由寫在 fixture 的 docstring 裡。

### 四、順手掉出來的一個觀察

403 個重播點裡有 356 點選出了價位，而**那 356 點只選出 11 個相異價位**
——候選集每輪 132 個，實際被選中的其實非常集中。這與 D047 的正回饋圈
是同一件事的兩個面向，但**還沒有量到什麼程度，先記著。**

**產出**：新增 `core/backtest.py`、`scripts/backtest_report.py`、
`tests/unit/test_backtest.py`（19 項）、`tests/unit/test_backtest_regression.py`
（11 項迴歸釘子）、`tests/unit/candles_2026_08_30.py`（200 根真實 K 線）。
**測試 726 → 756，全數通過。** 文件更新 DECISIONS（D048）＋ TASKS ＋ PLAN ＋ 本檔。
**機器人的行為零變更**：新檔案沒有被 `main.py` 匯入，正式容器一行都沒動
（驗證期間複製進去的兩支檔案已移除，容器回到與映像一致）。

---

## 2026-08-30（續二）—— 成交模擬：D1 終於有答案，而答案有三層

M2 第 1 步只能說「模型會選什麼價」。第 2 步接上成交模擬，才說得出
「**那個選擇值多少**」——也就是 D1 從 08-22 起等到現在的東西。

### 一、先驗證地基，再蓋上去

整個第 2 步都站在一條規則上：「第一根 `high >= 掛單利率` 的 K 線就是成交」。
**所以先拿 `wait_report` 配對好的 12 筆真實成交回頭跑一次**，才決定要不要繼續：

| | 值 |
|---|---|
| 實際等待合計 | 19.45h |
| 模擬等待合計 | 18.15h |
| **總量比** | **0.93×** |

對照組：策略自己的 `estimate_wait()` 在同一批樣本上是 **4.25 倍高估**。
**模擬器準了一個數量級**——這才是它能當裁判的理由，不是「因為它是回測」。
規則若對不上，上面的實得年化全部是假的，**而且會看起來完全正常**。

### 二、D1 的答案，三層都要講

1. ✅ **方向**：假設 `P=48` 的實得年化 **6.57%**，假設 16.93h 是 **7.26%**。
   四種「實際借多久」的假設下都是同一個方向。
2. 🔴 **跨時段站不住**：切一半，**前半反過來**（48h 7.21% vs 16.93h 7.05%）。
3. 🔴 **細掃是雜訊**：14h 最高（7.38%），但 20h 7.28%、12h 7.23%，
   曲線不單調、高低差不到 0.5 個百分點，而樣本只有 16.8 天、十幾次循環。

**第 2、3 條與第 1 條寫進同一份測試檔**（`Test這個方向還不足以拿來改參數`）
——這個專案有六個決策互相推翻的紀錄，形狀每次都一樣：
**一個時間切片上的結論被當成通則。** 第 1 條讀起來太像「答案找到了」。

### 三、最違反直覺的那個結果

**就算每一筆都真的借滿 48 小時，假設 48h 仍然不是最好的**
（7.27% vs 假設 16.93h 的 7.61%）。

機制：較小的假設 `P` 讓策略更怕等待，而那正好補償了 `W` 被低估（D047）。
**兩個錯誤部分互相抵銷。**

🔴 **所以「把 48 改成實測值」會拆掉抵銷的一半。** D1 從 08-22 就寫著
「不可以直接換」，理由是「逐筆變異太大」；**今天多了一個更硬的理由**，
而且它把 D047 與 D1 綁成了同一件事：不能各改各的。

### 四、實作時被自己咬了一口

第一版的 `empirical_hold()` 是個帶 `state["index"]` 的閉包，於是
**同一個實例跑第二次會從序列中間接下去**——同一組設定先後跑出
**7.26% 與 6.70%**，兩個數字看起來都很正常。
「依序循環而不是隨機抽」的重點本來就是**可重跑**，有狀態的實作正好毀掉它。
改成把「第幾次循環」當參數傳進去。已加測試釘住。

### 五、數字看起來比 `wait_report` 低，那不是變差

`run_policy()` 的分母**含空等與空轉**（沒等到成交、以及策略不掛單的每一個小時
都以 0% 計入）。`wait_report` 印的 7.99% 就是因為少了這一塊才偏樂觀。
**6.5%～7.4% 是分母變誠實之後的數字。**

**產出**：新增 `core/fill_simulation.py`、
`tests/unit/test_fill_simulation.py`（23 項）、
`tests/unit/test_fill_simulation_regression.py`（12 項迴歸釘子）、
`tests/unit/candles_full_history.py`（403 根真實 K 線 ＋ 12 筆真實成交）；
`scripts/backtest_report.py` 加上 `--validate-fill`／`--simulate`／`--hold-model`。
**測試 756 → 791，全數通過。機器人行為零變更。**
文件更新 DECISIONS（D049）＋ TASKS（D1 的三層答案）＋ PLAN ＋ 本檔。
