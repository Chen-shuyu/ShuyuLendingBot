# STATUS —— 現在是什麼狀態

> **這是唯一一份「現在」。** 寫下來的當下就取代前一份，舊的整段搬進
> [archive/PLAN-history.md](archive/PLAN-history.md)。
>
> **硬上限 120 行。** 超過就代表有東西該往下層搬，不是把上限調大。
>
> 最後更新：**2026-08-26 22:0x**（main `d6e3c80`，無程式碼變更；驗收與分析）

## 一句話

機器人**真金運作中**（2026-08-15 上線），部位 `464505426` 生息中
（08-26 00:30 成交 @ 年化 9.11%，到期 **08-28 00:30**）。
**M1 完成**：D043 的五條驗收條件在 08-26 00:14 那一輪**全部通過**。
本日的分析結論是一個新的：**「掛出去那一刻預估要等多久」第一次拿去對「實際等了多久」，
五筆全部高估，平均高估 3.9 倍**——而那一項正是期望值算式裡懲罰高利率的那一項。
**還是不動價**：要先有校準報告，才談改公式（D036）。

## 資金與部位

| 項目 | 狀態 |
|---|---|
| 部位 `464505426` | **生息中** @ 年化 9.11%，08-26 00:30 成交、到期 **08-28 00:30**（已滿 21.3h／44%） |
| 融資錢包可用餘額 | 0.07 USD（曝險上限由錢包餘額鎖住，其餘資金在現貨錢包） |
| 場上掛單 | **無**（資金全在部位裡，每輪走餘額守門檻「本輪略過」） |
| 累計績效 | 首次成交至今 242.3h：資金使用率 **80.6%**、實質年化（毛）**6.80%**（**08-26 21:5x 手算**，含仍在生息的第八筆） |
| 實測完成率 | **51.8%**（七筆已結束，中位數 20.97h）——模型仍假設 100%，D1 的落差沒消失。⚠ D1 的分組門檻**已經跨過**，但便宜組那三筆是 1.84／45.08／48.39h，離散到中位數沒有代表性（見下方） |

⚠ **所有績效數字都是手算的**：`earnings_daily` 至今 **0 筆**（P2-2 還沒接資料源）。

## 運行中的東西

- **策略 `expected_value`**（D035／D038）：掛在哪個價位由「利率 × 借出期間 ÷
  (等待 + 借出期間)」最大的那一個決定，等待時間每輪從 1 小時 K 線重估
- **部署**：Podman ＋ systemd Quadlet，巡檢間隔 600 秒。
  **健康狀態不寫在這裡**（寫下來的當下就開始過期）——隨時可查：
  `podman ps` 或 `python3 scripts/healthcheck.py`
- **市場資料落地**（D042，08-23 17:20 上線）：每輪把簿子摘要、成交摘要寫進
  `market_snapshots`，K 線一根一列寫進 `market_candles`。**這張表只存觀測**
- **決策落地**（D043，08-24 22:48 上線）：策略每真的評估過一輪就往
  `pricing_decisions` 寫一列——選中的價位、候選集、當時的窗。
  **寫在日誌那一行的旁邊**，於是日誌行就是 DB 那一列的鄰行
- **測試 650 項**（單元＋功能 624、整合 26）
- **已知限制**（都是刻意的，不是疏漏）：公式假設借滿 48h 但實測完成率 51.8%
  （D040，等 M2 才改）、**等待估計高估 3.9 倍**（D045，同樣等 M2）、天期寫死 2 天

## 接下來三步

1. **D045 等待估計校準報告**（`scripts/wait_report.py`）——量測算式裡的 `W`，
   與 `hold_report.py` 量測 `P` 成對。**只量測，不改策略**
2. **P4-1／B6 測試替身校正** → 再接 **P2-2 `earnings_daily`（Bitfinex ledger）**。
   B6 要排在前面：ledger 是下一個回應解析的重災區，D027 記過的坑、D041 的兩個 bug 都栽在這
3. **M2 回測工具** → 第 2 期的策略決策（D1 改公式那一半、A2-b、天期、`ev_window_hours`）

完整路線圖見 [PLAN.md](PLAN.md)，待辦清單見 [TASKS.md](TASKS.md)。

## ✅ D043 驗收通過（08-26 00:14 那一輪），M1 完成

部位 `464372858` 於 **00:14:07** 收回（到期 08-25 23:50，逾期 24 分），
`pricing_decisions` 的第一列就落在那一輪。五條條件（原文在 D043 最後一節）逐條實測：

| # | 條件 | 實測 |
|---|---|---|
| 1 | 出現第一列 | ✅ `id=1`、`decided_at` 00:14:12 |
| 2 | `snapshot_id` 指得回同一輪 | ✅ 327 → `captured_at` **字串完全相同** |
| 3 | `chosen_rate` 等於日誌那一行 | ✅ 0.00024971＝年化 9.1144%，連等待分佈五個數字都對得上 |
| 4 | 候選陣列長度＝`candidate_count` | ✅ 115＝115，已排序，選中值在集合內 |
| 5 | 資金再借出後不再多一列 | ✅ 00:30:51 成交後**又跑 127 輪，仍是 2 列** |

第二列（00:24:19）是掛單還在場上時的重新評估，**符合設計**（每評估過一輪就一列），
那一輪走 `_plans_match()` 容差維持不動，沒有重掛。

**🟡 D044／B5 仍未觸發**：最可能觸發的一刻（00:14 重新掛單）已經過去——
當時餘額 344.72436、掛單 344.72，**剛好沒有超**。全份日誌自 08-22 起 0 ERROR／0 WARNING。
照 D044 自己寫的話：**「沒發生」不等於通過**，狀態維持 🟡。

### 兩件今天才第一次算出來的事

**一、`pricing_decisions` 幾乎不會長**：60 小時只寫了 2 列，因為資金 98% 的時間鎖在
部位裡（其餘 127 輪全走餘額守門檻）。照這個節奏一年約 500 列。**不擋 M2**
（M2 的主要輸入是 `market_snapshots` 454 列、`market_candles` 316 根，那兩張正常在長），
但「靠 `pricing_decisions` 累積樣本看策略行為」要好幾個月才有量。

**二、市場三天翻了三次面**（08-24「轉弱」→ 08-25「翻上去」→ 08-26「又回落」）：
08-25 那個 11.19% 的 high 中位，到 08-26 只剩 9.82%、≥11% 從 14h 掉到 2h。
**這反而強化了「不要動 `ev_window_hours`」**——08-25 算出來的 48h 窗選 11.23%，
追的是一個只活了一天的尖峰。三個方向全部丟給 M2。

**D1 的分組門檻已經跨過，但比預先寫的警告還虛**：便宜組確實變 3 筆，中位數 45.08h
對昂貴組 13.92h（3.2 倍，方向符合）——**但那三筆是 1.84／45.08／48.39h**，
中位數完全靠兩筆借到期撐著。TASKS 預先寫的兩條警告成立，再加第三條：
**組內離散到中位數沒有代表性**。

## 文件怎麼讀（三層）

| 層 | 讀什麼 | 什麼時候讀 |
|---|---|---|
| **L1 現在** | **本檔** ＋ [TASKS.md](TASKS.md) | **每次對話開場只讀這兩份** |
| **L2 為什麼** | [DECISIONS.md](DECISIONS.md)（含索引表）、[ARCHITECTURE.md](ARCHITECTURE.md)、[PLAN.md](PLAN.md) | 要改動、要查某個決策的理由時 |
| **L3 當時發生什麼** | [PROGRESS.md](PROGRESS.md)（最近 7 個工作日 ＋ 全期索引）、[CHANGELOG.md](CHANGELOG.md)（上線後） | 想知道某天發生什麼 |
| | **[archive/](archive/)** | 更早的歷史。**入口是 [archive/README.md](archive/README.md)**，那裡有完整的「想查什麼→去哪裡」對照表 |

**程式碼註解裡的 `D0xx` 一律指向 [DECISIONS.md](DECISIONS.md)**
（要數幾處：`grep -roE '\bD[0-9]{3}\b' --include='*.py' --exclude-dir=tests . | wc -l`
——**不寫死數字**，嵌在文件裡的快照每開一條分支就過期一次，理由同 ARCHITECTURE 的測試數）。

## 維護規則

1. **本檔硬上限 120 行**，超過就往下層搬
2. **一件事只在一個地方講完整**：DECISIONS.md 是唯一的完整敘述處，
   其他檔案只留「一行結論 ＋ D 編號」
3. **PR 合併後只更新三處**：本檔 ＋ CHANGELOG ＋（若有決策）DECISIONS
4. **PROGRESS.md 每月 1 號切檔**，舊月份進 `archive/`
