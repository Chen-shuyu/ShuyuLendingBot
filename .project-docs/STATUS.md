# STATUS —— 現在是什麼狀態

> **這是唯一一份「現在」。** 寫下來的當下就取代前一份，舊的整段搬進
> [archive/PLAN-history.md](archive/PLAN-history.md)。
>
> **硬上限 120 行。** 超過就代表有東西該往下層搬，不是把上限調大。
>
> 最後更新：**2026-08-30 12:1x**（main `67b2229`，PR #48／#49／**#50 都已合併**）

## 一句話

機器人**真金運作中**（2026-08-15 上線）。08-29 掛高的實驗開獎輸了
（10.95% 只借 1.98h、**實得年化 3.02%**），而輸法指出誤差約 **7 個百分點來自 `P`**、
約 1 個來自 `W`——**真正的槓桿是 D1，不是 D045 一路在調的 `W`**。
🔴 **08-30 又量出第三個誤差源（D047）**：那張 10.91% 掛了 13 小時沒成交，
而模型算出來的實質年化在這 15 個小時區間**單調上升 9.03% → 9.46%**
——**乾旱期會讓等待估計自己變好看**，因為右設限的補值被窗尾夾住。
它與 `P` 同向、且是相乘的，並形成一個**對抗降價的正回饋圈**。
下一步仍是 **M2**（現在有三個題目在等它），但 **A2-b 提前到 P2-2 之前**。

## 資金與部位

| 項目 | 狀態 |
|---|---|
| 部位 | **無**——`464812689` 已於 08-29 22:50:57 收回（借 1.98h、完成率 **4%**） |
| 場上掛單 | **`5096500219` @ 年化 10.91%、345.02 USD**，08-29 22:51 掛出，**已閒置 13.24 小時**（機會成本 0.0569 USD）。**刻意不動**，理由見 D047 最後一節 |
| **實得年化** | **7.99%**（12 段已完成一輪，時間加權，`wait_report` 直接印）。⚠ 偏樂觀：沒等到成交的期間不在分母裡 |
| 實測完成率 | **35.3%**（**17 筆**已結束，中位數 11.61h、借到期 4 筆）——模型仍假設 100% |
| 等待估計 | 已成交那半**平均高估 4.25 倍、中位數只有 1.67 倍**（8 樣本）；**沒成交那半反過來被低估**（D047） |

⚠ **所有績效數字都是手算或報告算的**：`earnings_daily` 至今 **0 筆**（P2-2 還沒接資料源）。

## 運行中的東西

- **策略 `expected_value`**（D035／D038）：掛在哪個價位由「利率 × 借出期間 ÷
  (等待 + 借出期間)」最大的那一個決定，等待時間每輪從 1 小時 K 線重估
- **部署**：Podman ＋ systemd Quadlet，巡檢間隔 600 秒。
  **健康狀態不寫在這裡**（寫下來的當下就開始過期）——隨時可查：
  `podman ps` 或 `podman exec shuyu-lending-bot python3 scripts/healthcheck.py`
- **三張落地表**（M1 完整了）：
  - `market_snapshots`（**970 列**）＋ `market_candles`（**403 根**）（D042）——只存觀測
  - `pricing_decisions`（**118 列**，D043）——**選出來的價位怎麼來的**。
    ⚠ 這裡原本寫「60 小時只寫 2 列，別指望靠它累積樣本」——**那句話已經過期**：
    場上有單的每一輪都寫，08-30 一天就 72 列
  - `repost_comparisons`（**111 列**，D046）——「保住 vs 改掛」的反事實。
    **`action` 100% 是 `hold_matched`**，成因見下方驗收表
- **兩份校準報告**（隨時可跑，唯讀開 DB，只量測不決策）：`hold_report.py` 量 `P`
  （D040）、`wait_report.py` 量 `W`（D045）**並印出兩者相乘的實得年化**
- **測試**：**要用時重跑 `pytest tests/`**——手抄的數字會漂（上一版寫 725、實測差 13）
- **已知限制**（都是刻意的，不是疏漏）：🔴 **`P` 寫死 48 而實測完成率只有 35.3%**
  （D040／D1，最大的一項）、🔴 **乾旱會讓 `W` 自己變好看**（D047，第三項）、
  **等待估計高估是個駝峰**、**`W` 用平均而中位數明顯較準**（D045 追記，現在有三個論據）、
  **往上調價沒有判準**（D046）、天期寫死 2 天——**六項都等 M2，不是疏漏**

## 接下來三步

1. **M2 回測工具**。理由是量出來的：`P` 那一項的誤差約 7 個百分點、`W` 約 1 個，
   而 **D1「改那個 48」卡在 M2**，08-22 之後沒動過。**現在有三個題目在等它**：
   D1 那個 48、`W` 用哪個統計量、**D047 的右設限補值**。
   🔴 **離線試算（08-30 的 K 線）：`P` 從 48 換成實測 16.93h，選中價位 10.91% → 9.09%。**
   ⚠ **仍不要繞過 M2 直接改**：逐筆從 0.50h 到 48.56h，換成另一個常數只是
   `target_queue_usd` 的死法（D032）。**D047 那三個補值方式也是同一個道理。**
2. **A2-b 重掛政策**（D046，**08-30 從第三提到第二**）——理由是 D047：
   那個正回饋圈正在把「該降價」這件事擋在偵測不到的地方，而機會成本正在跑錶。
   已有門檻候選（躺超過 **12.6～18.9 小時**就輸給簿子底部，D045 追記），
   **場上那張單現在正落在這條帶內**。仍要等 M2 驗過。
3. **P2-2 `earnings_daily`（Bitfinex ledger）**。B6 這個前置已於 08-29 做掉**八支**
   ——它抓到的五件事有四件是「欄數／型別／混進別的東西」，
   **同一族的坑在 ledger 幾乎一定會再出現一次**

完整路線圖見 [PLAN.md](PLAN.md)，待辦清單見 [TASKS.md](TASKS.md)。

## 🟡 兩件「還沒被觸發」的驗收

**驗收條件寫在合併當下，而「沒發生」不等於通過**——這是 D044 自己立的規矩。

| 項目 | 狀態 | 還缺什麼 |
|---|---|---|
| **M1-c**（D046） | 條件 1 與條件 3 **✅ 全數已驗**（111 列）。**條件 2 仍測不到，但 08-30 查出了原因** | 候選利率與場上利率**逐位元相同**（都是 `0.00029879`），連 08-29 那個 10.91% ≠ 10.95% 的差距都消失了。🔴 **這不是「情境還沒出現」，是 D047 的正回饋圈正在把它擋掉**——市場離開 → 乾旱累積 → `W` 被拉低 → 這個價位算起來更好 → 更不可能改掛 |
| **D044／B5**（餘額不足不停機） | 🟡 仍未觸發 | 最可能觸發的一刻（08-26 00:14 重新掛單）已過去，當時餘額 344.72436、掛單 344.72，**剛好沒有超**。日誌自 08-17 起 0 ERROR |

**M1-b（D043）的五條驗收已於 2026-08-26 00:14 全數通過**，逐條實測見
[DECISIONS.md](DECISIONS.md) 的 D043「驗收結果」一節（那裡比這裡詳細）。

## 兩個還沒解決的判讀陷阱

- **D1 的分組塌成 1 筆／16 筆，而且不會自己好**：門檻是中位數，
  而 9.11% 同時是眾數（17 筆裡 10 筆）。🔴 **多等幾筆不會改善**——模型每選一次
  同樣的價位，就同時把中位數釘在原地、又往昂貴組加一筆。
  **只有「更低」有用**，更高的價位只會讓失衡更嚴重（已加迴歸測試，PR #50）
- **`W` 的兩半要分開讀**：已成交那半是高估（駝峰，8 樣本中位數 8 次全勝），
  **沒成交那半反而被低估**（D047）——**D045 的校準樣本依定義只看得到前半**。
  兩件事各自為真，不能用一個倍數轉述

## 文件怎麼讀（三層）

| 層 | 讀什麼 | 什麼時候讀 |
|---|---|---|
| **L1 現在** | **本檔** ＋ [TASKS.md](TASKS.md) | **每次對話開場只讀這兩份** |
| **L2 為什麼** | [DECISIONS.md](DECISIONS.md)（含索引表）、[ARCHITECTURE.md](ARCHITECTURE.md)、[PLAN.md](PLAN.md) | 要改動、要查某個決策的理由時 |
| **L3 當時發生什麼** | [PROGRESS.md](PROGRESS.md)（最近 7 個工作日 ＋ 全期索引）、[CHANGELOG.md](CHANGELOG.md)（上線後） | 想知道某天發生什麼 |
| | **[archive/](archive/)** | 更早的歷史。**入口是 [archive/README.md](archive/README.md)** |

**程式碼註解裡的 `D0xx` 一律指向 [DECISIONS.md](DECISIONS.md)**
（要數幾處：`grep -roE '\bD[0-9]{3}\b' --include='*.py' --exclude-dir=tests . | wc -l`
——**不寫死數字**，嵌在文件裡的快照每開一條分支就過期一次）。

## 維護規則

1. **本檔硬上限 120 行**，超過就往下層搬
2. **一件事只在一個地方講完整**：DECISIONS.md 是唯一的完整敘述處，
   其他檔案只留「一行結論 ＋ D 編號」
3. **PR 合併後只更新三處**：本檔 ＋ CHANGELOG ＋（若有決策）DECISIONS
4. **PROGRESS.md 每月 1 號切檔**，舊月份進 `archive/`
