# STATUS —— 現在是什麼狀態

> **這是唯一一份「現在」。** 寫下來的當下就取代前一份。
>
> **硬上限 120 行。** 超過就代表有東西該往下層搬，不是把上限調大。
>
> 最後更新：**2026-09-05**（main，最後一次程式變更仍是 PR #59／D056）

## 一句話

**機器人真金運作中，而且它終於賺錢了**——實得年化（帳本）**5.72% → 7.75%**、
資金利用率 **98.8%**。D056（`assumed_hold_hours: 12`）跑滿 5 天 4 個完整循環，
🟢 **有效（D060）**。但同一批資料同時說了三件事：
🔴 **它有效的理由是錯的**——`P=12` 被實測否定，8.76～9.00% 那一帶**借滿 48h**；
🔴 **選價現在是由浮點雜訊決定的（D061）**——前兩名差 4×10⁻⁵ pp，
而平手規則系統性偏向便宜那邊；
🔴 **目標函數自我矛盾（D062）**——用常數 `P` 最佳化 `r`，而 `P` 是 `r` 的函式，
**連回測裁判都一起被污染**；
🔴 **所有樣本都是模型自己選出來的（D063）**——量再久也不會有新資訊。

🔴 **瓶頸換人了**：問題不再是「借不出去」，是「賣太便宜」。空窗已接近 0，
**唯一剩下的槓桿是價格**（手續費 15% 是常數，改不動）。

## 資金與部位

| 項目 | 狀態 |
|---|---|
| 部位 | **`465110649`**——345.29 USD @ 年化 **8.76%**，09-04 02:30 開倉。**健康狀態不寫在這裡**（寫下來就開始過期） |
| 場上掛單 | 無（錢在部位裡） |
| **實得年化（帳本）** | **7.75%**（09-01～09-04 四整天）／D056 之前 **5.72%**。⚠ **不是控制實驗**，中間隔著市場變化 |
| 手續費對帳 | 帳本淨 ÷ 推算毛 = **81.2%**，與抽 15% 一致。**這是第一次有人對過** |
| 實測持有 | D056 後結清 3 筆：**48.33h／48.19h（都借滿）／1.43h**；第 4 筆 39.3h 生息中。**假設值是 12h** |
| 換手空窗 | **0.68h／0.26h／0.13h**（D056 之前是 3.6h／38.1h／24.7h） |
| 等待估計 | 12 個樣本**全部高估**：`mean` 中位 **9.0×**、`median` 中位 **3.9×**；**而 12 個裡有 10 個擠在 8.76～9.50%** |

算實得年化：`python3 scripts/sync_earnings.py --since 2026-08-15 --principal 345.29`
（🔴 **本金要手打**，理由與修法見 TASKS 第 4 順位）

## 運行中的東西

- **策略 `expected_value`**（D035／D038）：掛在「利率 × 借出期間 ÷ (等待 + 借出期間)」
  最大的那個價位。**`P` 目前 12 小時**（D056），合約天期 2 天。
- **部署**：Podman ＋ systemd Quadlet，巡檢 600 秒；帳本每 6 小時同步、日結一天一則。
  隨時可查：`podman ps` 或 `podman exec shuyu-lending-bot python3 scripts/healthcheck.py`
- **三張落地表**：`market_snapshots` ＋ `market_candles`（D042）、
  `pricing_decisions`（D043）、`repost_comparisons`（D046）。**列數不寫死在這裡**
- **四份工具**：`hold_report.py`／`wait_report.py`（🔴 **D057 缺陷待修**）、
  `backtest_report.py`（🔴 **`--simulate` 目前不能當裁判，見 D062**）——**唯讀**；
  `sync_earnings.py`（D051，**唯一會寫 DB 的**）
- **測試**：**要用時重跑 `pytest tests/`**——手抄的數字會漂
- **已知限制**（都是刻意的，不是疏漏）：**往上調價沒有判準**（D046／D050／D054）、
  🔴 **比較政策一律要跨起跑點**（D054）、🔴 **模型只在自己選中的價位上取樣**（D063）

## 接下來做什麼：五個順位（完整清單見 [TASKS.md](TASKS.md)）

**D059 的車道判準仍然有效**（「會不會改變送到交易所的東西」），
**但車道內容已被重排**——理由在 D063：`W` 旋鈕化的價值被「高價位零樣本」卡住。

| # | 做什麼 | 動不動到場上的單 |
|---|---|---|
| **0** | 修 D057 幽靈樣本（**＋拆單重複**）、修兩處過期註解 | 不會（唯讀） |
| **1** | **高原容差 ＋ 平手偏高**（D061） | 🔴 **會**——但**不依賴 `P(r)`**，是唯一可以先做的定價改動 |
| **2** | `rate_dependent_hold()` **＋同時修 `empirical_hold()`**（D062） | 不會（純模擬側） |
| **3** | **探索**：每 N 次換手刻意掛一次高價；M1-c 換問題（D063） | 🔴 會（第一版只取樣，不改決策） |
| **4** | `principal_avg` 要有值、`sync_earnings` 加毛／淨對帳 | 不會 |
| **5** | `W` 旋鈕化（原甲-2，**已降級**） | 不會（預設＝現行行為） |

🔴 **第 2 順位的兩件事不可以分開做**：`empirical_hold()` 假設 `P ⊥ r`，
**用它去檢驗「`P` 是不是 `r` 的函式」是用結論證明結論。**

**一個尺度提醒**：本金 345 USD、20 天總利息 **1.76 USD**。每一項的效果都以「分」計，
**量測雜訊比效應大**——所以排序看的是「哪一項縮短拿到答案的時間」。

## 🟡 兩件「還沒被觸發」的驗收

| 項目 | 狀態 | 還缺什麼 |
|---|---|---|
| **M1-c**（D046） | 🟡 **儀器開始長了（0 → 187 列），但 186 列零資訊**——候選恆等於場上價。條件 2 唯一那次觸發是 D056 部署自己引發的 | 🔴 **不是再等**，是**換它問的問題**（D063） |
| **D044／B5**（餘額不足不停機） | 🟡 仍未觸發 | 日誌自 08-22 起 0 ERROR |

## 四個判讀陷阱（完整敘述在 D061／D062／D063／D047）

- 🔴 **不要用第 6 位有效數字挑價位**：`W` 的誤差以「倍」計（D061）。
- 🔴 **不要拿 `--simulate` 當裁判**，在 `empirical_hold()` 修好之前（D062）。
- 🔴 **不要把「量了很久」當成「知道很多」**：樣本全落在模型自己選的窄帶（D063）。
- **分組前要先數相異值**：17 筆只有 7 個相異利率，浮點雜訊會切出假分組（D058）。

## 文件怎麼讀（三層）

| 層 | 讀什麼 | 什麼時候讀 |
|---|---|---|
| **L1 現在** | **本檔** ＋ [TASKS.md](TASKS.md) | **每次對話開場只讀這兩份** |
| **L2 為什麼** | [DECISIONS.md](DECISIONS.md)（含索引表）、[ARCHITECTURE.md](ARCHITECTURE.md)、[PLAN.md](PLAN.md) | 要改動、要查某個決策的理由時 |
| **L3 當時發生什麼** | [PROGRESS.md](PROGRESS.md)、[CHANGELOG.md](CHANGELOG.md)、**[archive/](archive/)** | 想知道某天發生什麼 |

**程式碼註解裡的 `D0xx` 一律指向 [DECISIONS.md](DECISIONS.md)。**

## 維護規則

1. **本檔硬上限 120 行**，超過就往下層搬
2. **一件事只在一個地方講完整**：DECISIONS.md 是唯一的完整敘述處
3. **PR 合併後只更新三處**：本檔 ＋ CHANGELOG ＋（若有決策）DECISIONS
4. **PROGRESS.md 每月 1 號切檔**，舊月份進 `archive/`（**2026-09-05 已切**）
