[pytest]
# pythonpath 讓測試能直接 import 專案根目錄下的 main / modules / db 等套件，
# 不必在每個測試檔手動動 sys.path（需要 pytest >= 7）。
pythonpath = .
testpaths = tests
# 測試檔內用 @pytest.mark.live 標記會實際連外網的測試，方便本機以
# `pytest -m "not live"` 全離線執行。
markers =
    live: 會實際連線 Bitfinex 公開端點的測試（連不上時自動 skip）
