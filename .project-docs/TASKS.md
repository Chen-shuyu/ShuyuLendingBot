# TASKS —— 還沒做的事

> **這份只放「還沒做的事」。** 現在什麼狀態看 [STATUS.md](STATUS.md)，
> 完整路線圖看 [PLAN.md](PLAN.md)，某件事當初為什麼那樣決定看 [DECISIONS.md](DECISIONS.md)。
>
> **已完成的項目與歷史分析全部保留在
> [archive/TASKS-history.md](archive/TASKS-history.md)**（原本 1,363 行的完整內容，
> 一個字都沒刪）。2026-08-23 重整前，這份文件裡有 73 個已完成項與 13 個未完成項混在
> 同一頁，而其中一個區塊還叫「🔴 下一步・最高優先」，內容是三週前的事。
>
> 勾選標記：`[ ]` 未開始／`[~]` **部分完成**（做了但目標沒真的達成，說明寫在項目底下）。
>
> **分期對應 [PLAN.md](PLAN.md) 的路線圖**（2026-08-17 重排，理由見 D036）。

## 進行中

**目前沒有進行中的分支**：`feature/persist-pricing-decision`（M1-b 決策落地，
見 [DECISIONS.md](DECISIONS.md) 的 **D043**）已隨 PR #43 合併部署（08-24 22:48）；
`fix/b5-insufficient-balance-classification`（B5）已隨 PR #44 合併部署（08-24 23:29）。
M1-b 的放行條件是 2026-08-23 23:04 由 D040／D041 雙雙驗收通過解除的，
證據見 [PROGRESS.md](PROGRESS.md) 的 2026-08-24。

**D043 已於 2026-08-26 00:14 驗收通過（五條全數）**，M1 因此完成——
逐條實測見 [DECISIONS.md](DECISIONS.md) 的 D043「驗收結果」一節。
**D044／B5 仍未觸發**，維持 🟡（最可能觸發的一刻已經過去，餘額剛好沒有超）。

**下一條分支：`feature/measure-wait-accuracy`（D045 等待估計校準報告）**
——起因是「為什麼老是掛同一個利率」這個問題，查出期望值曲線是平的、
而等待估計五筆全部高估（平均 3.9 倍）。**這一項只量測，不改策略。**

---

## 第 1 期：量測基礎建設（下一個主要工程）

> **這一期是為了讓後面的決策不再互相推翻**（D036）。沒有它，第 2 期只會變成
> 第七個被推翻的決策。

- [x] **M1　決策所需的資料落地** —— **三塊全部落地**：M1-a（D042）、M1-b（D043）
      已驗收通過，**M1-c（D046）於 2026-08-28 部署**（PR #46）。
      ⚠ M1-c 的驗收條件 1／2 要等正式環境出現「場上有掛單」的輪次才驗得到
  - **✅ M1-a 市場快照落地**：每輪把簿子摘要、成交摘要、K 線寫進 DB。
    **拆成兩張表而不是原本寫的一張**——`market_snapshots` 每輪一列、
    `market_candles` 一根 K 一列且主鍵天然去重（K 線每小時才換一根、巡檢 600 秒一輪，
    塞同一張表等於一天寫三萬多列去講 24 根 K 的事）。完整理由見 D042。
    **成本是量出來的**：一列約 1.2 KB、一年約 68 MB
    ——原本這裡寫的「每輪幾百位元組」是還沒把簿子曲線算進去時的估計。
  - **✅ M1-b 決策落地**（2026-08-24，D043，分支 `feature/persist-pricing-decision`）：
    新表 `pricing_decisions`，策略每真的評估過一輪就一列——選中的價位與它的等待分佈、
    對照組、候選集的價位與實質年化兩排、當時的窗長與那個已知錯的 `hold_hours_assumed`。
    **寫入點緊貼 `_log_pricing_rationale()`**，於是日誌那一行就是 DB 那一列的鄰行
    ——D041 擔心的「沒有鄰行會反駁」是靠位置接回來的，不是靠承諾。
    **沒評估過的輪次一列都不寫**（2026-08-24 那天 130 輪就該是 0 列）。
    成本實測一列 4,137 位元組、一年約 50 MB（最壞情況 207 MB）。
    測試 618 → **650 項**。**✅ 2026-08-26 00:14 驗收通過**（五條逐條實測見 D043）。
    ⚠ **這張表幾乎不會長**：60 小時只寫了 2 列，因為資金 98% 的時間鎖在部位裡
    （其餘 127 輪全走餘額守門檻），一年約 500 列。不擋 M2（M2 吃的是
    `market_snapshots` 與 `market_candles`），但別指望靠它累積樣本。
  - [x] **✅ M1-c 重掛反事實落地**（2026-08-28 完成，PR #46，見 **D046** 的
    「第一段實作結果」一節）——新表 `repost_comparisons`，一列 418 位元組。
    測試 650 → 675。**條件 3（行為零變化）已驗**：舊版與新版引擎在六個情境下
    掛出去的單完全相同。
    🔴 **2026-08-29 追記：條件 1 的正向那半與條件 2「測不到」**——部署後 228 輪，
    **場上有單的輪次是 0**（三張單全在一個巡檢週期內成交，等待 0.00～0.10h）。
    `repost_comparisons` 至今 0 列，**而那是正確的**（條件 1 的負向那半反而因此
    在 225 輪上驗過了）。**M1-c 是一台裝好但還沒被觸發過的儀器**，
    要等市場走弱或我們掛高才會開始長——見 D046「驗收狀況」。
    以下為當初的規劃紀錄，保留備查。
    **只落資料，不改任何行為。** 起因是使用者提的
    「每十分鐘比一次，該調高就砍掉重掛」。查證後有四個事實：
    (1) **每十分鐘確實已經在比了**；
    (2) **但往上調價完全沒有判準**——`_cheaper_repost_is_not_worth_it()` 第一行就
    `if candidate.rate >= live_rate: return None`，只管往下，往上是「差超過 2% 就直接重掛」；
    (3) D034 說「往上調多賺的利息是確定的」，**那句話的前提是會成交**
    ——08-19 掛 9.78% 在場 34.2 小時沒成交，反例已經有了；
    (4) **機會成本每輪都在算但算完就丟**：`_log_idle_time()` 算出
    `idle_hours` 與 `forgone` 之後回傳 `None`，沒有任何判斷式讀得到。
    - **要做的**：`_log_idle_time()` 回傳算出來的值；每一輪有場上掛單時落一列
      「保住 vs 改掛」的並排比較（兩條路的實質年化、當初的預估、
      照現在的規則會不會動它）。
    - **驗收的第 3 條是「行為零變化」**：部署前後同一份市場資料下掛出去的單要完全相同。
    - **為什麼先做這個而不是直接改規則**：沒有這批反事實資料，
      第二段（A2-b）就只能拍門檻，而那是 `target_queue_usd` 的死法（D032）。
  - 08-21 給了它更明確的形狀：現在靠「掛在可見簿子之外、等陣發掃單」賺錢，
    而決定成敗的三個量——**掃單多久來一次、掃多深、借款人多快跑掉**
    ——第三個由 D040 落地，前兩個的原始資料由 M1-a 落地。
    **前兩個已於 2026-08-24（K 線）與 2026-08-25（簿子）第一次算出來**：
    簿子底部 54.5 小時被掃空 40 次（約 1.4h 一次）、單次最大跳幅 4.76pp。
    數字見 [PROGRESS.md](PROGRESS.md) 的 2026-08-25（深夜）第三節。
    ⚠ **算出來不等於這一項完成**：它們仍是描述性統計，
    「掛這個價位會等多久」要 M2 才答得準。
- [x] **D045　等待估計校準報告** —— **已完成**（2026-08-26，分支 `feature/measure-wait-accuracy`）
  - **與 D040 的 `hold_report.py` 成對**：算式 `r × P ÷ (W + P)` 裡，
    D040 量的是 `P`（實際借多久），這一項量的是 `W`（實際等多久）。
    **兩項到這裡都有自己的校準報告。**
  - **已量到的**：五筆有預估值的掛單**全部高估**，平均 7.09h 對實際 1.80h＝**3.9 倍**
    （逐筆 1.6×～28.7×）。`W` 正是算式裡懲罰高利率的那一項，所以模型系統性偏向低價。
  - **⚠ 還不能拿它去改價**：五筆全落在 9.11%～9.50% 這個模型自己選出來的窄帶，
    9.5% 以上零樣本；而唯一的反例正在高價端（08-19 掛 9.78%，**34.2h 沒成交**）。
  - **只量測，不改 `estimate_wait()`**：高估倍數從 1.6× 到 108.6×，
    拿任何一個代表值去除都是拍新數字。改法是 M2 的題目。
  - **🔴 2026-08-29 晚間：問題可能一直問錯了。** D045 三次都在問「W 該除以多少」，
    但 `estimate_wait()` 一次算三個統計量、DB 三個都存了，策略卻只用 `mean`。
    八個樣本並排後 **`median` 8/8 全數較準**（總量比 1.67× 對 4.25×、
    最差 9.0× 對 108.6×）。**也許不是要除以一個數字，是選錯了統計量**
    ——報告已把三個並排（PR #49），**選哪一個仍是 M2 的題目**。
  - **實作時被正式資料抓到一個會靜默吃掉樣本的合併 bug**（三張各自成交過的
    9.50% 被併成一段，五個校準樣本剩兩個）。詳見 D045 的驗收結果一節。
  - **可以隨時查**：`python3 scripts/wait_report.py`

- [~] **M2　回測工具** —— **第 1 步（重播器）與第 2 步（成交模擬）都在 2026-08-30
      完成**（分支 `feature/backtest-replay`、`feature/backtest-fill-simulation`）。
      **第 3 步（「當時可達到的最佳」）仍未做，那一項與 M3 是同一件事**
  - 餵歷史 K 線與 M1 落地的資料，對**任一策略**回答：「如果當時跑的是這個策略，
    會掛什麼價、多久成交、實質年化多少」。D035 的回測是手寫的一次性腳本，
    這一項是把它變成專案的常設能力。
  - 🔴 **原本的驗收標準跑不起來，已於 2026-08-30 換掉（見 DECISIONS.md D048）**：
    PLAN.md 寫的是「拿它跑 `orderbook_depth`，要能重現賣在區間底部」，但
    (1) `orderbook_depth` 沒有 `choose_rate()`、(2) 那個結論是 08-16～17 的，
    而 `market_snapshots` **08-23 才寫第一列**。
    **驗收標準比它依賴的那張表早寫了六天，而十三天內沒有人回頭確認它可不可跑。**
  - **換上的那一條（跑得起來）**：`scripts/backtest_report.py --verify`
    ——拿 `pricing_decisions` 裡正式環境真的做過的決策逐點對照。
    **08-30 第一次跑：25 列全數一致。**
    ⚠ **它比原本那條弱**：只證明工具沒偏離正式環境，**抓不到「策略本身錯了」**
    （重播與正式環境跑同一條路徑，兩邊會一起錯）。真正能否證策略的驗收等第 2 步。
  - **✅ 第 1 步已完成的東西**：`core/backtest.py`（`replay()`／`replay_at()`／
    `sweep_hold_hours()`）、`scripts/backtest_report.py`（`--verify`／`--sweep`／
    `--hold-hours`）、30 項單元測試（含 3 組迴歸釘子，資料是 200 根真實 K 線）。
    三條刻意的界線寫在 `core/backtest.py` 的 docstring 裡：**重播呼叫策略本尊**、
    **重播點看不到未來**、**唯一被轉的旋鈕是 `P` 而且轉的是策略自己讀的那個屬性**。
  - **✅ 第 2 步（成交模擬）已完成**（`core/fill_simulation.py`，D049）：
    成交規則「第一根 `high >= rate` 就成交」**拿 12 筆真實成交驗過，
    總量比 0.93×**（對照組：`estimate_wait()` 是 4.25 倍高估）。
    `run_policy()` 算整段歷史的時間加權實得年化，**空等與空轉都進分母**。
  - **⬜ 第 3 步**：「當時可達到的最佳」——那對數字是 M3 要的。
  - 它要回答的題目已經在排隊：**D1 那個 48**、**A2-b 的降價時機**、
    **`ev_window_hours` 的敏感度**、**`W` 用平均還是中位數**（D045 第三則追記），
    以及 **2026-08-30 新增的 D5「右設限補值」**（D047）。
    每一個都是「在多長的等待／持有之後，長期期望值才最高」，
    而每一個都不可以拿單筆成交當依據。
  - **🟢 2026-08-30 補上第二條驗收標準（有已知答案的迴歸釘子）**：同一份 168 根
    K 線、`P=48`、現行右設限補值 → 必須選出 **10.91%**；`P` 換成 16.93h → 必須
    選出 **9.09%**。兩個數字都是 08-30 離線試算出來的，且第一個與正式日誌相符。
    **驗收標準要能被跑，不能只是一句話。**
  - **原料已經夠開工**：`market_candles` 403 根（≈16.8 天）、`market_snapshots`
    970 列、`pricing_decisions` 118 列、`repost_comparisons` 111 列。
- [ ] **M3　成效量測**
  - 「實際拿到的年化」對照「當時可達到的最佳」，兩個數字並列。
    沒有這一對數字，「策略變好了」永遠只是說法。
  - 目前所有績效數字都是**手算**的（`earnings_daily` 至今 0 筆）。

---

## 第 2 期：策略的剩餘工作

> 做在第 1 期的工具之上，**不再靠即時快照**。

- [~] **D1　期望值公式假設借滿 48 小時，但六筆有五筆提前還款**
      —— **量測那一半 2026-08-22 完成**（D040，分支 `feature/measure-actual-hold-time`）。
      **改公式那一半仍未做，等 M2 回測工具。**
  - 🔴 **2026-08-29 升級為最大的那一項誤差。** 掛高實驗開獎（10.95% 借 1.98h、
    實得 3.02%）後把誤差拆開：**`P` 那一項約 7 個百分點、`W` 約 1 個**
    ——就算 `W` 估得完全正確，只要 `P` 還寫死 48 仍會算出 9.88%。
    **D045 的本文加三則追記全部在講 `W`，也就是比較小的那一項。**
    推導見 DECISIONS.md 的 D045「開獎」一節。
  - **🟡 2026-08-30：M2 第 2 步跑出第一個答案（D049），而它有三層**：
    (1) ✅ **方向**——假設 `P=48` 的實得年化低於假設較小的 `P`
    （6.57% vs 7.26%），且在四種「實際借多久」的假設下都成立；
    (2) 🔴 **跨時段站不住**——切半之後**前半的結論反過來**
    （前半 48h 較好 7.21% vs 7.05%）；
    (3) 🔴 **細掃出來的最佳值是雜訊**——14h 最高但曲線不單調、高低差不到
    0.5 個百分點，而樣本只有 16.8 天、十幾次循環。
  - 🔴 **而且多了一個更硬的「不要直接改」的理由**：**就算實際持有真的是 48h，
    假設 48h 仍然不是最好的**（7.27% vs 假設 16.93h 的 7.61%）。
    機制是**兩個錯誤部分互相抵銷**——較小的假設 `P` 讓策略更怕等待，
    正好補償 `W` 被低估（D047 的乾旱回饋圈）。
    **單獨改 48 會拆掉抵銷的一半，所以 D047 與 D1 必須一起處理。**
  - ⚠ **仍然不可以直接把 48 換成實測值**：完成率平均 35.3% 但逐筆從
    0.50h 到 48.56h，拿平均去填就是 `target_queue_usd` 的死法（D032）。
  - **🟢 2026-08-30 晚間：證據夠了（D055）。** D049 列的兩條「不能改」的理由
    **都是單一起跑點造成的假象**（D054 證明那種比較會被相位運氣主導）。
    跨起跑點重做之後：**切半三段全部同向**、**從 48h 往下單調上升**、
    **8～20h 是一片高原**（差不到 0.2pp），而**實測平均 16.93h 與中位數 11.61h
    都落在高原裡**。第二條獨立證據：單純把價位往下平移，最佳點在 **−10%**，
    而換 `P` 讓選中價位下移約 **16%**——**兩條獨立路徑同一個量級**。
  - **建議：`offer_period` 從 2 天改成 12 小時（0.5 天）**，在高原中間、
    接近實測中位數。⚠ **這是定價路徑，要使用者拍板。**
  - ⚠ **仍然沒被消掉的保留**：只有 16.8 天；模擬成交偏快 0.93×；
    **`P` 一直在替 `W` 的偏差擦屁股（D047），改它是調補償項不是修根因**
    ——這一條不會因為證據變強而消失。
  - **量測結果（2026-08-24 更新）**：已結束**六筆**的中位數 **13.92h**、
    **平均完成率 43.6%**，而模型假設 48h（完成率 100%）。
    08-23 收回的那筆借了 **48.56 小時（101%）**，是至今第二筆借到期的，
    把 08-22 當時的 6.87h／32.1% 拉回了一些——**但方向沒變，分子仍被高估**。
    最短的一筆只借了 2.33 小時（佔 5%）。
  - **「越貴借越短」目前還比不出來**：分組後便宜組只有 2 筆，
    而中位數要兩組各至少 3 筆才算得準（D040 決定五）。逐筆看得到形狀，
    但**不夠下判斷**——第一版報告差點用一個插值出來的 23.46h 宣告假設成立。
    **2026-08-24 複查仍是 2 與 4 筆**：08-23 新增的那筆落在昂貴組，
    便宜組一筆都沒增加，所以這一項離「比得出來」沒有變近。
  - **🟡 2026-08-25 深夜：第七筆一收回，門檻就會第一次被跨過。**
    9.11% 落在已結束部位利率中位數 9.50% 之下 → 便宜組變 **3 筆**，
    屆時算得出「便宜組中位數 45.08h vs 昂貴組 13.92h」。
    ⚠ **兩條警告要先寫在前面，免得看到 3.2 倍就宣告假設成立**：
    (1) 這是很虛的 3，D040 的門檻是「算得準」的下限，不是「可下判斷」的下限；
    (2) **有共變因子**——9.11% 那筆之所以借滿 48h，很可能是因為期間市場漲到 11.3%，
    借款人沒理由提前還掉一筆便宜錢，**機制不見得是「貴」，可能是「市場漲」**。
    第一版 D040 報告差點用一個插值出來的 23.46h 宣告假設成立，同一個坑不要踩第二次。
  - **誠實要點**：08-21 的結果（使用率 88.8%、實質年化 8.44%）**並不支持**
    「選 9.50% 太高」。被證實的只有公式的分子是錯的，**不是決策是錯的**。
  - **可以隨時查**：`python3 scripts/hold_report.py`

- [~] **A2　重掛守門檻（D034）已退化成「永遠擋下往下調價」**
      —— **2026-08-18 拆成兩件事，見 D037**；**A2-a 已於 2026-08-20 完成（D039）**，
      A2-b 仍在等 M1／M2。標 `[~]` 是因為「新策略下的重掛政策」還沒有主人：
      現在擋著往下調價的只剩 `_plans_match()` 的 2% 利率容差。
      **2026-08-20 的 30 輪現場資料**（兩個排隊金額 30/30 完全相同）
      已把 D037 的推論變成量測：
      **A2-a（現在做，是 bug）** 讓 `_queue_ahead()` 在候選價位越出可見範圍時
      回傳 `None`／明確標記越界，上層改為**棄權**而不是偷偷否決；
      **A2-b（等 M1-c ＋ M2）** 新策略下的重掛政策該長怎樣，要在回測工具上跑過才定。
      **🔴 2026-08-30：M2 比過六種政策，答案是否定的（D050）**——
      實得年化最好與最差差不到 0.05 個百分點，連「候選一變就跟」這個
      成本為零的上界對照也沒有贏。**而這不是因為沒有機會作用**（等待佔整段
      歷史 30.1%、最長一段 38.5 小時），是**候選價位是個落後訊號**：
      最長那段空掛裡它**前 32 小時完全沒動**，越過 2% 容差時那段等待已走完 83%。
      成因是結構性的——**定價用 168 小時的窗，反映不了十幾個小時的乾旱**。
      **🟡 2026-08-30 已經量了（D054）**：三個候選訊號裡兩個**量不了**
      （`market_snapshots` 08-23 才開始寫，而 5 段長等待有 3 段在那之前——**又是 D048
      那個形狀**）；第三個「我的價位最近被掃到幾次」**偵測得非常好**
      （5/5 命中、0/10 誤報、門檻 1.5×～4× 都一樣）。
      🔴 **但拿它去降價是賠錢的**：跨 22 個起跑點平均 **−1.97 個百分點**，勝率 7/22。
      機制是「降價把較差的利率鎖住最多 48 小時，而省下來的只是一段等待」
      ——**贏很多次、輸很大次**。
      **偵測得好不等於知道該做什麼**，A2-b 因此仍然沒有可以上線的政策。
      **🟢 2026-08-26 由使用者的提案給了它明確的內容，見 D046**：往上調價
      **目前完全沒有判準**（只管往下的那道守門檻第一行就 `return None`），
      而該用的訊號是**市場價位的走向**，不是閒置時間——
      ⚠ **「等太久」是成交比預期慢的證據，它支持降價不支持漲價**，
      兩個訊號混在一起會在市場走弱時去漲價，正好是最糟的時機。
      還要處理**棘輪**（連漲時一路追高、永遠不成交）與**空窗／排隊位置**的成本。
      作用窗口很短（成交的單在場中位數只有 2.05 小時 ≈ 12 輪），
      **價值集中在 08-19 那種 34 小時的長尾**，設計時要拿長尾當主場景。
      **⚠️ A2-a 修好會鬆開目前唯一擋著降價的東西，所以要連同「閒置時間量測」一起做**
      （先量測、有資料再談門檻——直接拍一個「超過 N 小時就降價」就是 `target_queue_usd` 的死法）。
  - 實測：候選價位年化 9.96%，而**簿子可見的最高檔只有 7.21%**——
    候選價位高過整個可見範圍，於是 `_queue_ahead()` 對任何價位都回傳同一個
    截斷總額（5,381,114 USD）。
  - **2026-08-25 挖到它的成因側**：簿子固定截斷在 250 檔（`book_truncated` 永遠是 1），
    而 250 檔是「最便宜的 250 個相異價位」，所以**可見範圍會隨底下供給的厚薄呼吸**。
    325 輪裡有 26 輪（8.0%）可見總額超過 1 億 USD，最高 2.74 億——查證不是資料錯誤，
    是**一筆約 1.96 億 USD 的 30 天期供給常駐在年化 11.33～11.35%**，
    底下被掃薄時才進得了可見範圍。
    **推論：`book_highest_rate` 的變動有一部分是簿子密度的假象，不是市場訊號。**
  - 兩邊的等待估計因此完全相同，`利息 ÷ (等待 + 借出期間)` 的分母約掉，
    判準退化成純粹比利率——**只要候選價位比場上那張低，就一定被擋**。
  - 實跑確認：往下調價 10% → 擋下，訊息是
    `前方 5,381,114 → 5,381,114 USD`（兩個數字一樣就是證據）。
  - 後果：**場上那張單的利率永遠不會被往下調超過 2%**（2% 以內由
    `_plans_match` 的容差吸收）。市場真的走弱時機器人不會跟著下來，會一直空掛。
  - **這不是有人決定的行為，是 D035 的改動連帶造成的。** 要嘛改用策略的 K 線
    等待估計，要嘛明講「新策略下不做往下重掛」並拿掉這段死邏輯。
  - **🔴 2026-08-18 的成交不能拿來當作「這個 bug 其實幫了忙」的證據**（D037）：
    當天每一輪擋下重掛的是 `_plans_match()` 的 **2% 利率容差**（候選價位始終在
    0.0002729 附近，168 小時窗把短期下跌平滑掉了），**守門檻整天沒被觸發過**。
    結果漂亮不等於這段程式碼是對的。
  - **一個永遠給同一個答案的判斷式，等於沒有判斷式**——差別只在它看起來像有。
    這是 D026「靜默失效」的第四次現身。

- [~] **P1-5　定價與天期改用期望值計算，取代人工旋鈕**（2026-08-16 新增，見 DECISIONS.md D032；
      **2026-08-17 由 D035 大幅改寫、升級為最高優先，並於同日完成利率那一維**）
  - **✅ 利率維度已完成**（2026-08-17，`strategies/expected_value.py`，見 D035 的「實作」節）。
    驗收：同一份真實市場資料下舊策略 0 筆、新策略年化 9.96%（實質 9.35%）。測試 493 → 512 項。
  - **⬜ 天期維度仍未做**：`offer_period` 還是設定檔裡寫死的 2。比較天期要一輪打三次
    K 線端點（`p2`／`p7`／`p30`）並各自估等待，是另一次改動的份量。**這是 P1-5 的剩餘部分。**
  - **白話**：使用者的原話是「什麼價位掛出去比較好？掛不出去的時間成本又是多少？
    這些計算應該寫在程式內部」。目前 `target_queue_usd = 1_000_000`
    這個數字**是人在對話裡手算出來再填進設定檔的**，程式自己不知道時間成本，
    市場一變它就過期，而程式沒有辦法察覺。
  - **🔴 2026-08-17 改寫（D035）：問題比原本寫的嚴重，而且第 3 題的資料源要換。**
    - 原本以為 `target_queue_usd` 只是「會過期」；實測是**模型的自變數選錯了**。
      這個市場的成交是**陣發掃單**（最近 30 天有 90.0% 的小時曾觸及年化 8% 以上，
      單根 1 小時 K 振幅動輒 5 個百分點），排隊位置模型假設的「需求穩定地從前端吃過來」
      不成立。**站在最前面不會更快成交，只保證用最低價成交。**
    - 所以第 3 題**不該再問「前面那些錢多久被吃完」**（那是排隊模型的問題），
      要改問「**掛在這個利率，多久會遇到一次掃到這裡的需求**」。
    - **資料源改用 1 小時 K**（`/v2/candles/trade:1h:fUSD:p2/hist`），不是 `/v2/trades`。
      理由：K 線的 `HIGH` 直接就是「那一小時的需求掃到多高」，
      一次呼叫可取 5000 根（涵蓋 7 個月），而 `/v2/trades` 一次只涵蓋約 4 小時
      ——**樣本窗太短正是 D035 第一個錯誤結論的成因**。
    - 回測（判定：某根 K 的 `HIGH` ≥ 掛單利率就算被掃到；實質年化用 D034 的
      `r × 48 ÷ (等待 + 48)`）：5.50% → 實質 5.44%；8.00% → 7.90%；9.00% → 8.76%；
      **9.75% → 9.23%（7 天窗最佳）**；10.50% → 8.45%（7 天）／**9.61%（30 天最佳）**。
      **現行策略給出 5.47%，差約 4 個百分點。**
    - **`minimum_rate` 年化 8% 維持不動**，它擋掉的正是「賣在區間底部」。
  - **策略每輪要能自己回答四個問題**（前兩個已經有了）：
    1. 市場利率水準在哪？（已有：讀訂單簿）
    2. 掛在候選價位，前面排多少錢？（已有：`describe_queue()`——**降級為觀測用途**）
    3. **掛在這個利率，多久會遇到一次掃到這裡的需求？**（缺）——讀 1 小時 K 的 `HIGH`
       分佈算命中頻率（**2026-08-17 由 D035 改寫，原本寫的是「讀近期成交算流量速率」**）。
    4. **等這段時間值不值得？**（缺）——比較等待成本與利率收益，選期望值最高的檔位。
  - **算式在 D030 已經成形，只是還沒進程式**：空轉的年化是 0%，
    以 344 USD 計，空轉一天損失 0.074 USD，而利率多爭取 1 個百分點一天只多賺 0.008 USD
    ——**空轉一天要跑 9 天才補得回來**。
  - **同一套計算也要回答天期**：D030 第二節已有長期證據（2 天 6.47%／7 天 9.45%／
    30 天 10.47%），但溢價會大幅變動，**寫死任何天期都是錯的架構**。
    天期與利率是同一個期望值問題的兩個維度，要在同一處算，
    不該一個寫在程式裡、一個寫在設定檔裡。
  - **排在 P1-4 後面**：期望值算得再準，重掛邏輯還會撤掉即將成交的單子的話就兌現不了。
  - **實作前需要更多校準樣本**：目前只有一個（預估 0.48～0.79 小時、**實際 1.35 小時**，
    方向對但比保守估計久 1.7 倍）。一個樣本不足以定參數——
    這正是「估算要進程式並持續校準」的理由，而不是再手算一次填進去。

- [~] **P1-2　天期調整——原本的論據不成立，重新定位**
  - **原規劃寫「7 天期比 2 天期高 0.5 個百分點，幾乎是白撿」——那個數字取自含爆發桶的窗**，
    與 D027 記過的統計陷阱是同一個。剔除爆發後，當日 2 天期與 7 天期的常態成交價
    **完全一樣**（都是 0.000250）。
  - **但用 998 天日 K 重算，長天期確實較優**（滾動模擬毛利年化：2 天 6.47%、
    7 天 9.45%、30 天 10.47%），原因是結構性的——86% 的供給都擠在 2 天期，
    競爭把價格壓下去。詳見 DECISIONS.md D030 的天期分析。
  - **溢價會大幅變動**（2023-Q4 +4.34 個百分點、2026-Q3 只剩 +0.20），
    所以**寫死任何一個天期都是錯的架構**，正解是每輪比較各天期的實質年化再選。
  - **這次仍掛 2 天期，理由與定價無關**：驗證階段不要把資金鎖住。
    「成交 → 記錄 → 通知」整條鏈一次都沒跑通過，先確認它會動再談天期最佳化。
  - **下一步是使用者提的分桶實驗**（150 掛短天期 ＋ 其餘掛 30 天期，跑一個月）。
    注意那個實驗**分不出策略勝負**——一個月只有 1 個 30 天期樣本，
    而 D030 的結論是從 33 個獨立週期算出來的。它的價值在實務驗證與累積第一批成交資料。

- [ ] **D3　價格目標會因為一根 K 滾出 168 小時窗而自己跳一階**
  - 2026-08-20 10:12：候選價位數 111 → 110，選中的目標同時 9.78% → 9.50%，
    而當時的市場常態價與一小時前相同（都是 8.00%）。
  - 當天整條事件鏈（改價 → 守門檻 → 重掛 → 成交）的起點就是這個。
  - **而我們無法直接證實**——是哪一根 K 滾出去、當時簿子長什麼樣，都沒有存下來。
    **這是 M1 的又一個理由**，也連著「`ev_window_hours` 的敏感度」那一項。

- [ ] **D5　乾旱期會讓等待估計自己變好看**（2026-08-30 新增，見 DECISIONS.md **D047**）
  - **白話**：同一個價位、同一份市場，**掛越久沒成交，模型算出來的實質年化越高**。
    08-29 22:51 那張 10.91% 掛了 13.24 小時沒成交，而 `pricing_decisions` 的
    15 個小時區間**單調上升 9.03% → 9.46%**，一次都沒回頭。
  - **成因**：`estimate_wait()` 對等不到命中的起點記「至少等到窗尾」
    （`(total - start) * candle_hours`），而**離窗尾越近的起點這個數字越小**。
    尾端的乾旱每多一小時，就多灌進一個很小的「等待」把平均往下拉。
  - 🔴 **它形成一個對抗降價的正回饋圈**：市場離開 → 乾旱在窗尾累積 → `W` 被拉低
    → 這個價位算起來更好 → 更不可能改掛 → 乾旱繼續長。
    **M1-c 的驗收條件 2 測不到，就是被這個圈擋掉的**（111 列全是 `hold_matched`，
    候選利率與場上利率逐位元相同）。
  - ⚠ **與 D045 的「高估 4.25 倍」不衝突**：D045 的八個校準樣本**全部是等到成交的
    期間**，而這個機制**只活在右設限的期間裡**。兩件事各自為真，**不能用一個倍數
    轉述整件事**。
  - ⚠ **不要直接換一個補值**：離線試過補成「一整窗」，選中價位 10.91% → 10.42%
    （實質 9.46% → 7.97%）。**換一個拍出來的常數就是 `target_queue_usd` 的死法**
    （D032）——真實等待是被右設限的，要的是存活分析那一類的處理。**這是 M2 的題目。**
  - **順手落下的觀察**：16 小時的乾旱讓 `mean` 走了 3.19 小時、`median` 只走了 1 小時
    ——**中位數對窗尾補值幾乎免疫**。這是「該用哪個統計量」的**第三個獨立論據**。
  - **樣本數是 1**：08-19 那段 34.20 小時的空掛早於 D038，沒有留下預估值。

- [ ] **`ev_window_hours` 的敏感度** —— **2026-08-25 拿到第一組雙向證據**
  - 目前 168 小時是拍板的：7 天窗最佳解年化 9.75%、30 天窗 10.50%，
    **我選了 7 天但沒有證據說它比較好**。
  - 這是 D036 三個問題裡唯一還沒過關的旋鈕——它對第 3 題
    （「過期時程式怎麼察覺？」）的答案仍然是「不會」。
  - **2026-08-25 的量測**（294 根真實 K 線離線跑 `choose_rate()`，沒有改程式）：

    | 窗長 | 選中價位 | 實質年化 | 平均等待 | 命中 | 右設限 |
    |---|---|---|---|---|---|
    | 24h | 沒有候選（命中數不足 `ev_min_hits`） | — | — | — | — |
    | 48h | **11.23%** | 9.93% | 6.27h | 14 | 0.0% |
    | 72h | 11.24% | 8.44% | 15.88h | 13 | 6.9% |
    | **168h（現行）** | **9.11%** | 7.81% | 8.04h | **66** | 0.0% |

  - **雙向**才是這次的重點：08-24 記的是「168 小時窗把**轉弱**平滑掉了」，
    08-25 市場翻上去之後是同一個機制**反方向再演一次**。
    問題不是「窗對壞消息反應慢」，是**窗就是慢**。
  - ⚠ **但 168h 的答案不是沒有根據**：它選的 9.11% 在窗內有 66 次命中、0% 右設限，
    而 48h 的 11.23% 只有 14 次。**這是「過度貼合 vs. 反應遲鈍」的標準取捨，
    不是換一個數字能解決的**——正因為如此，它仍然是 M2 的題目，不是現在拍板的理由。
  - 🔴 **不要因為上面這張表去改它**：全部的錢是 **0.04 USD**（344.57 USD 兩天，
    9.11% 對 11.23%）。先改參數再建量測正是 D036 記下的那個錯誤。
  - **🟢 2026-08-26：第三個方向出現，而它站在「不要改」那一邊。**
    08-25 那個 11.19% 的 high 中位，到 08-26 只剩 9.82%、≥11% 從 14h 掉到 2h
    ——**48h 窗當時選的 11.23%，追的是一個只活了一天的尖峰**。
    三天三個方向（轉弱／翻上去／又回落）本身就是答案：**這個旋鈕不能靠看盤決定。**

---

## 第 3 期：收益要看得見

- [~] **P2-2　`earnings_daily` 接上真實資料源** —— **2026-08-30 完成資料路徑**
      （D051，分支 `feature/ledger-earnings`）。**`earnings_daily` 不再是空的**：
      12 天、合計 0.75517665 USD。**只剩「要不要接進主迴圈」還沒決定。**
  - 🔴 **它推翻的東西比它新增的多**：帳本是這個專案第一條「交易所自己說的錢」，
    而錨放下去之後，**每一層推論都偏樂觀**——
    `wait_report` **7.99%** → 回測 **6.54%** → **帳本 5.34%**。
    三個算式各自都對，**錯的是每一個都少扣一點東西，而方向全部相同**。
  - **B6 的預測中了**（「ledger 是下一個重災區」）：`[2]` 官方文件標成 placeholder
    但**實測是錢包名稱**、數值全是字串（D027 第四次現身）、
    **帳本裡混著轉帳與幣別兌換**。
  - ⚠ **兩種偷懶法都會算錯**：「加總」得到 343.71（差八千倍）、「只取正數」
    得到 528.01。因為**同一筆轉帳出現兩列、正負相反、掛在不同錢包**。
    分類要同時看 `description` ＋ `wallet`。
  - **可以隨時查／重跑**：`python3 scripts/sync_earnings.py --write --since 2026-08-15 --principal 344.31`
    （用 `set_daily_earning()` 覆蓋語意，實測連跑兩次合計不變）
  - **✅ 已接進主迴圈**（2026-08-30，D052）：`earnings_sync_hours: 6`，
    跑在 `run_forever()` 的迴圈層、**不在 `run_once()` 裡面**
    ——後者有五條提早 `raise SkipCycleError` 的路，而正式環境幾乎每輪都走其中一條，
    放進去等於永遠不執行（**D038 踩過的同一課**）。
    **吞掉所有 `Exception`**：報表不可以把一台在管真金的機器人弄停。
  - **⬜ 還沒做的**：平均本金仍是呼叫端給的常數——帳本只看得到餘額，
    而餘額含已賺到的利息、也含還掛在場上沒借出去的錢，**猜一個就又變成推論**。

- [x] **P2-4　交易面通知擴充：掛單／成交／日結** —— **2026-08-30 全部完成**。
      掛單那一半 08-16（D029）、成交與收回同日（D030）、
      **日結摘要 08-30（D053）**——它等的利息資料源由 D051／D052 補上了。
      判準是**資料不是時鐘**（`earnings_daily` 出現沒推過的日期就推），
      狀態存新表 `bot_kv`（**不是往 `bot_state` 加欄位**——這個專案沒有
      migration 機制，`CREATE TABLE IF NOT EXISTS` 對既有表加欄位完全沒效果）。
  - **已完成**：掛單上線、掛單消失、掛單被拒三種事件。
    **設計比原規劃更省額度**：原本寫「掛單內容與上一輪不同才推」，實作時發現
    **那個門檻擋不住 FRR 漂移**——利率每輪都會有小數點後幾位的差異，
    等於又變回每輪一則。改為**只推「場上有沒有我們的單」這個狀態的轉換**，
    利率的日常微調只寫日誌。一天通常 0～2 則。
  - **意外收穫**：「掛單已不在場上」這則正是目前唯一能察覺「錢可能借出去了」的訊號，
    等於在沒有 P2-1 的情況下先拿到了它一部分的價值。**訊息刻意不寫死成「成交」**
    ——餘額歸零也可能是資金被搬走，猜錯一次這個管道就不會再被相信。
  - **✅ 全部做完**：成交明細（P2-1）與每日摘要（P2-2 → D053）都已上線。
  - 以下為原始規劃紀錄，保留備查。
  - **要的是什麼**：現在只有「系統壞掉」會通知，使用者要的是
    「機器人幫我掛了什麼單、成交了什麼單」這類交易面的能見度。
  - **硬限制先講：每月 200 則 ≈ 每天 6.6 則。** 巡檢間隔 600 秒等於一天 144 輪，
    **每輪推一則的話 1.4 天就把整月額度燒光**，之後真正的故障告警一則都送不出去。
    D024 決定「例行巡檢不推 LINE」正是這個理由，這一項不能把它推翻，
    只能在額度內重新分配。
  - **所以只能這樣設計**（「有變化才推」是必要條件，不是優化）：
    - **成交**：每筆推一則（依賴 P2-1 的部位查詢能力）。這是最值得花額度的事件。
    - **掛單**：只在**內容與上一輪不同時**才推（利率、金額、筆數任一改變）。
      每輪都在原價重掛屬於無事發生，寫日誌就好。
    - **掛單失敗**：每次都推（本來就罕見）。
    - **每日摘要**：固定一天一則——當天掛了幾輪、目前在場上的單、已借出多少、
      當日利息（利息數字依賴 P2-2）。
  - **額度試算**：日結 30 則/月 ＋ 成交（以目前規模估每天 1～3 筆）30～90 則/月
    ＋ 系統告警預留 30 則 ≈ 90～150 則，落在 200 內但**餘裕不大**。
    所以掛單通知一定要走「有變化才推」。
  - **如果之後想看得更細**：正解是換掉管道（例如 Telegram bot，沒有月額度），
    或維持「細節寫日誌 ＋ 每日摘要」。**不要用提高推播頻率的方式解**——
    額度是硬的，超過就是整個月都靜音。
  - **依賴關係**：格式照 P2-3；成交事件依賴 P2-1；利息數字依賴 P2-2。
    **掛單／掛單失敗兩類不依賴任何一項，可以先做**——這是這一項裡最快能讓
    使用者感覺到差別的部分。

- [ ] **P4-1　排在 P2-2 之前：測試替身校正（B6）**
  - ledger 是下一個回應解析的重災區，而 D027 記過的坑就是
    「測試看到的世界比真實世界乾淨」。**D041 的兩個 bug 都栽在這裡**，
    所以這一項要排在 P2-2 動手之前。詳見下方 B6。

---

## 第 4 期：規模門檻與品質債（不擋前面）

> **P3-1／P3-2 要資金超過 500 USD 才有意義**——目前規模下年收益都是
> +0.00 USD（D030 第六節）。

- [~] **P3-1　spread 階梯改成「百分位區間分佈」，不能是乘法遞增**
      —— **地雷已於 2026-08-16 拆除**（`spread_count: 3 → 1`），但**正解尚未實作**。
      344 USD 最多只拆得出 2 筆（單筆最低 150），而第 2 筆會被 `spread_step_pct`
      乘到 0.000288、簿子頂端才 0.000270——等於一半的錢掛成死單。
      這個規模下正確答案是「不拆單」，不是「改成百分位分佈」。
      **資金超過 500 USD 時再回來做真正的分佈版本。**
  - **白話**：現在的設定是把錢拆成 3 筆，第 2 筆利率乘 1.15、第 3 筆乘 1.32。
    問題是整個市場的利率帶總共才 8.7%～10%，寬度只有 15%——**第 2 筆就直接飛出市場了**。
  - **目前 160 USD 只夠掛 1 筆所以還沒踩到，但金額一超過 300 USD，
    第 2、3 筆就會變成永遠掛不出去的死單**——等於三分之二的錢在空轉。
    這是「錢一轉回來就會踩到」的地雷。
  - **改法**：三筆分別掛第 50／70／85 百分位（原寫 70／85／95，隨 P1-1 一起下修）。

- [ ] **P3-2　`maxtolend` 由「單輪量控版」升級為「含已放貸的真實總曝險版」**（同上降級）
      —— 查詢能力已經有了（`get_active_positions()` 隨 P2-1 一起做好，
      `Repository.open_positions()` 可直接取得目前在生息的部位），
      只差把它接進策略層的上限計算。這個規模下曝險仍由融資錢包餘額鎖住，不急。
  - **白話**：現在的風控只管「這一輪掛出去多少」，不管「總共已經借出去多少」。
    目前靠融資錢包只放 160 USD 鎖住風險，錢變多、或同時有好幾筆在放貸時才會真正需要。
  - **技術面**：需每輪查 `private_post_auth_r_funding_credits_symbol`（已被借走）與
    `private_post_auth_r_funding_loans_symbol`（已出借未被借走）。
    **與 P2-1 共用同一份查詢結果**，兩項一起做最省事。

- [~] **B6：把既有的測試替身與測試資料，逐一對照真實回應校正**（見 DECISIONS.md D027）
      —— **八支已校正**：七支於 2026-08-29 白天（分支 `feature/calibrate-test-doubles`），
      `funding_credits` 於同日晚間趁部位生息補打（PR #48，分支
      `feature/calibrate-funding-credits`）。**`funding_loans` ＋ 兩支寫入端點仍抄不到**，
      維持「部分完成」。
  - **背景**：實單第一天的兩個 bug（D025、D026）都通過了完整測試套件，
    而且各自都有一條測試「正在守護那個性質」。漏掉的原因相同——
    **測試看到的世界比真實世界乾淨**（輸入是手打的兩位小數、替身回傳原生型別）。
  - **✅ 已校正（2026-08-29 實打）**：`public_get_ticker_symbol`、
    `public_get_book_symbol_precision`、`public_get_trades_symbol_hist`、
    `fetch_balance`、`private_post_auth_r_funding_offers_symbol`。
    新增五組 `REAL_*` 常數，每組配一條**直接吃未加工真實列**的測試
    ——「照抄」與「照抄後還是綠的」是兩件事。測試 691 → 699。
  - **抓到四個替身原本不知道的事實**（完整敘述見 DECISIONS.md **D027 的追記**）：
    ticker 是 17 欄不是 4 欄且 [13][14] 真的是 `None`；**offer 陣列是 21 欄，
    替身寫 16**，而 [10]=STATUS 真實是 `'ACTIVE'`；`fetch_balance` 指定了
    `type='funding'`，`info` 裡照樣混著別的錢包；可用餘額是八位小數。
  - **✅ `funding_credits` 已於 2026-08-29 晚間補打**（部位 `464812689` 生息中時）。
    新增 `REAL_FUNDING_CREDIT` ＋ `FUNDING_CREDIT_FIELDS = 22`，抓到三件事：
    **22 欄**、`[21]=POSITION_PAIR` 是 `'tBTCUSD'`、
    **`[8]=RATE_TYPE` 真實是 `'FIXED'` 而替身留 `None`**（D027 那個坑的第三次現身）。
    **這是 B6 第一次做到「整條鏈路」**：原始列 → `_parse_positions()` → DB 四欄逐一對上。
  - **🟡 還沒處理，兩個原因各自不同**：
    - `funding_loans`：要「已被借走但還沒用掉」的時刻，補打當下錢已經被借走了，
      回空陣列。**下次遇到那個時刻補打一次**即可（`kind='loan'` 的部位歷史上出現過 3 次）。
    - `*_w_funding_offer_submit` ／ `_cancel`：**寫入端點，不動真錢就抄不到回應**。
      D027 已預先寫明不採用「改成整合測試打真實 API」，這兩支只能維持手寫，
      要靠日誌與實單事後核對。
  - **對 P2-2 的意義**：ledger 是下一個回應解析的重災區，而這次抓到的五件事
    有四件是「欄數／型別／混進別的東西」——**同一族的坑在 ledger 幾乎一定會再出現一次**。

- [ ] **P5-2　確認 `systemd/bfx-lending-bot.service` 的去留**
      —— D016 已決定正式路線改用 Quadlet（`systemd/shuyu-lending-bot.container`），
      這個舊檔案維持本機測試用途，但去留一直沒定案。

---

## 雜務（不擋任何事）

- [ ] 清理已合併完成的殘留分支（本地與遠端）——**見工作計畫 P5-1**。
      **2026-08-23 重新盤點：本地 39 條、遠端 38 條，其中 37 條已完全合併進
      `origin/main`**（08-16 時是 23 條，兩週又長了 14 條）。
      另有 `fix/m1-frr-and-loop` 未被 main 完全包含，刪除前要先確認內容確實已進 main。
      （2026-07-27 使用者選擇先不處理）

---

## 已完成與歷史

**2026-08-23 重整之後完成的項目，紀錄在 PROGRESS ／ CHANGELOG ／ DECISIONS**
（archive 那份是重整當下的凍結快照，不再往裡面加東西）：

| 項目 | 完成 | 紀錄在哪 |
|---|---|---|
| **C2　`docs_only` 只認 `.project-docs/`** | 2026-08-23 | 改為文件白名單（`.project-docs/`、`README.md`、`LICENSE`、`.gitignore`、`.gitattributes`），fail-open 不變。見 [PROGRESS.md](PROGRESS.md) 2026-08-23（續二）第六節——含兩組對照實驗，以及「本機 grep 是 ugrep、`-q` 忽略 `-v`」那個差點被當成程式錯誤的坑 |
| **M1-a　市場快照落地** | 2026-08-23 | [DECISIONS.md](DECISIONS.md) **D042** |
| **M1-b　決策落地** | 2026-08-24 | [DECISIONS.md](DECISIONS.md) **D043**（M1 仍是 `[~]`：正式環境驗收待 08-25 部位收回） |
| **B5　餘額不足被當成認證失敗而停機** | 2026-08-24 | [DECISIONS.md](DECISIONS.md) **D044**（起因是 D025 的附帶發現）。⚠ **正式環境尚未觸發過，而它可能永遠不觸發**——「沒發生」不等於驗收通過，五條條件在 D044 最後一節 |

---

**重整之前的完整歷史全部保留在
[archive/TASKS-history.md](archive/TASKS-history.md)**，包含：

| 原本的區塊 | 行數 | 內容 |
|---|---:|---|
| 進行中（歷史部分） | ~220 | 每日狀態、PR #34～#37 的部署驗證、08-20 成交盤查 |
| 🔍 程式／文件盤查結果（08-17） | 198 | A1～A3、B7～B9、C1～C2 的完整診斷 |
| 🎯 工作計畫（08-16，依優先級） | 372 | 優先級 1～5 的完整分析（**排序已由 PLAN.md 路線圖取代**，內容仍是權威） |
| 🔴 市場定價驗證（08-16） | 67 | 「FRR 高過市場成交天花板」的實測——**已由 D030→D035 解決** |
| 🔴 下一步・最高優先 | 302 | 2026-08-15 首次實單的三個發現、08-02 部署盤查 A1～A6／B1～B3 |
| 待處理（M1～M4） | 148 | 四個 milestone 的逐項驗收 |
| 基礎建設待辦（使用者端） | 11 | LINE Channel、API Key 權限、起始金額——**三項都已完成** |
| 已完成 | 25 | git 設定、`.gitignore`、volume 掛載等 |

**搬移時做的兩件事**（都記在 archive 的開頭）：

1. **未完成項先撿出來才搬**——上面每一個 `[ ]` / `[~]` 都是從那些區塊裡逐字搬出來的，
   一個都沒有留在 archive 裡等人去挖
2. **兩處重複的條目合併**：「待處理（M1～M4）」裡的 `maxtolend` 與
   `earnings_daily` 兩項，與 P3-2／P2-2 是同一件事，只保留後者
