# STATUS —— 現在是什麼狀態

> **這是唯一一份「現在」。** 寫下來的當下就取代前一份，舊的整段搬進
> [archive/PLAN-history.md](archive/PLAN-history.md)。
>
> **硬上限 120 行。** 超過就代表有東西該往下層搬，不是把上限調大。
>
> 最後更新：**2026-08-23 17:30**（main `76fe844`，PR #41 已部署）

## 一句話

機器人**真金運作中**（2026-08-15 上線），目前 344.41 USD 借出中。
**M1-a 市場快照落地已上線並驗收通過**——DB 第一次存下市場長什麼樣（D042）。
下一步是**等部位收回**，那一刻同時解鎖 D040／D041 的驗收與 M1-b。

## 資金與部位

| 項目 | 狀態 |
|---|---|
| 部位 `464242253` | **借出中** @ 年化 9.50%、2 天期，08-21 22:31 起，**最晚 08-23 22:31 到期** |
| 融資錢包可用餘額 | 0.17 USD（曝險上限由錢包餘額鎖住，其餘資金在現貨錢包） |
| 場上掛單 | **無**——PR #41 就是趁這個零成本視窗部署的（17:20 重啟，沒動到任何單） |
| 累計績效 | 首次成交至今 136.6h：資金使用率 66.4%、實質年化 5.00%（**08-22 12:07 快照，手算**） |

⚠ **所有績效數字都是手算的**：`earnings_daily` 至今 **0 筆**（P2-2 還沒接資料源）。

## 運行中的東西

- **策略 `expected_value`**（D035／D038）：掛在哪個價位由「利率 × 借出期間 ÷
  (等待 + 借出期間)」最大的那一個決定，等待時間每輪從 1 小時 K 線重估
- **部署**：Podman ＋ systemd Quadlet，巡檢間隔 600 秒。
  **健康狀態不寫在這裡**（寫下來的當下就開始過期）——隨時可查：
  `podman ps` 或 `python3 scripts/healthcheck.py`
- **市場資料落地**（D042，08-23 17:20 上線）：每輪把簿子摘要、成交摘要寫進
  `market_snapshots`，K 線一根一列寫進 `market_candles`。
  **只存觀測、不存決策**——本輪選中的價位（M1-b）等 D041 驗收後才進來
- **測試 618 項**（單元＋功能 592、整合 26）
- **已知限制**（都是刻意的，不是疏漏）：公式假設借滿 48h 但實測完成率只有 32.1%
  （D040，要等 M2 才改）、天期寫死 2 天、**M1-b 決策尚未落地所以還答不出 D3**

## 接下來三步

1. **等部位收回**（最晚今晚 22:31）—— 這一件事同時解鎖三樣：D040 的
   `_log_hold_times()`、D041 的正式環境驗收、以及 **M1-b 決策落地的放行條件**
2. **M1-b 決策落地** —— 存下本輪選中的價位與候選集，沒有它就答不出 D3
3. **P2-2 `earnings_daily` 接資料源**（Bitfinex ledger）→ **M2 回測工具**
   → 第 2 期的策略決策（D1 改公式那一半、A2-b、天期、`ev_window_hours`）

完整路線圖見 [PLAN.md](PLAN.md)，待辦清單見 [TASKS.md](TASKS.md)。

## 🔴 待驗證：D040 與 D041 的關鍵路徑仍未被走到

**D040（PR #36）與 D041（PR #37）都已部署，但關鍵路徑一次都沒被走到**
——連續第三、第四次「部署完成 ≠ 新程式碼被執行過」。

**M1-a（PR #41）不在這個名單上**：它 17:21:01 的第一輪就寫進
`market_snapshots` 1 列、`market_candles` 240 根，**沒有一個欄位是 NULL**
——這是連續四次之後的第一次立刻兌現。

驗收條件（寫死在這裡，免得又用「部署成功」代替）：

- **D040**：部位收回，`_log_hold_times()` 才會跑
- **D041**：資金回來 → 策略真的評估過一輪（日誌出現「期望值定價」）→
  下一輪又掉回門檻以下，**而那一輪不再重播上一輪的定價決策**

兩個都在等同一件事：**部位 `464242253` 收回**。

⚠ **今天 15:19 與 17:20 兩次重啟都把 `last_evaluation` 清空了**，所以現在日誌
沒有重播定價決策**不能當成 D041 驗收通過**——它只是還沒有機會犯錯。
D041 自己的反證正是「新行程的清單本來就是空的」。

## 文件怎麼讀（三層）

| 層 | 讀什麼 | 什麼時候讀 |
|---|---|---|
| **L1 現在** | **本檔** ＋ [TASKS.md](TASKS.md) | **每次對話開場只讀這兩份** |
| **L2 為什麼** | [DECISIONS.md](DECISIONS.md)（含索引表）、[ARCHITECTURE.md](ARCHITECTURE.md)、[PLAN.md](PLAN.md) | 要改動、要查某個決策的理由時 |
| **L3 當時發生什麼** | [PROGRESS.md](PROGRESS.md)（最近 7 個工作日 ＋ 全期索引）、[CHANGELOG.md](CHANGELOG.md)（上線後） | 想知道某天發生什麼 |
| | **[archive/](archive/)** | 更早的歷史。**入口是 [archive/README.md](archive/README.md)**，那裡有完整的「想查什麼→去哪裡」對照表 |

**程式碼註解裡的 `D0xx` 一律指向 [DECISIONS.md](DECISIONS.md)**
（要數幾處：`grep -roE '\bD[0-9]{3}\b' --include='*.py' --exclude-dir=tests . | wc -l`
——**不寫死數字**，嵌在文件裡的快照每開一條分支就過期一次，理由同 ARCHITECTURE 的測試數）。

## 維護規則

1. **本檔硬上限 120 行**，超過就往下層搬
2. **一件事只在一個地方講完整**：DECISIONS.md 是唯一的完整敘述處，
   其他檔案只留「一行結論 ＋ D 編號」
3. **PR 合併後只更新三處**：本檔 ＋ CHANGELOG ＋（若有決策）DECISIONS
4. **PROGRESS.md 每月 1 號切檔**，舊月份進 `archive/`
