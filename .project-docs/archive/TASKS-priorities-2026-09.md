# TASKS 的六個順位（2026-09-05～09-06，已完成／已決定，歸檔）

> **這是從 [../TASKS.md](../TASKS.md) 搬過來的**，2026-09-06 歸檔。**一個字都沒刪。**
>
> 六個順位在 2026-09-05 一天內全部收掉（八個 PR，測試 865 → 935），
> 09-06 驗收通過並由 **D067** 決定停止改動。
>
> **每一項的完整敘述在 [../DECISIONS.md](../DECISIONS.md) 的 D060～D067**，
> 逐項的變更在 [../CHANGELOG.md](../CHANGELOG.md)。
> **這裡保留的是「當時的待辦長什麼樣」**，不是權威敘述。

## 🅾 第 0 順位：唯讀，先做，是後面每一項的輸入

- [x] **0-1　修掉持有時間量測的幽靈樣本**（**D057**）—— ✅ **2026-09-05 完成**
      （分支 `fix/measurement-ghost-samples`）。🔴 **抓到第三個家**：`OBSERVED_HOLD_HOURS`
      這個寫死的常數裡有三個 0.50h 是 `loan` 的列，**整個回測用了它六天**。
      🔴 **而 D054 的「零誤報」因此不成立了**（0/10 → 1/9），詳見 CHANGELOG。
  - `hold_report.py` ／ `wait_report.py` 取樣時濾掉 `kind='loan'`。
  - ⚠ **不要在 `Repository.all_positions()` 裡濾**——那支的 docstring 已寫明
    「刻意不在 SQL 裡過濾，否則上層永遠不會發現自己只看到一部分」。
  - ⚠ **`kind='loan'` 的列要留著**：它記錄真實發生過的狀態轉換，
    而且 B6 還缺 `funding_loans` 的真實回應校正。**刪資料不是修法。**
  - 🔴 **順帶處理同一個病的第二種長相**：`464670446/447/448` 是同一筆錢
    拆成三張單，三列各記 11.6h。**17 列其實約 15 段獨立放貸。**
    D057 修的是 `loan`／`credit` 重複，這一項修的是**拆單重複**。

- [x] **0-2　修掉兩處已經過期的註解** —— ✅ **2026-09-05 完成**（同一支分支）
  - [`strategies/expected_value.py`](../strategies/expected_value.py) 的
    「實測平均持有 16.93h、中位數 11.61h」→ 現為 **22.60h／11.62h**，
    而且 **D056 之後在選中價位上是 48h**（D060）。
  - `config.yaml` 的 `assumed_hold_hours` 註解同樣要補上 D060 的實測。

- [x] **0-3　`--verify` 已經靜默失效了** —— ✅ **2026-09-05 當天發現當天修掉**
      （分支 `fix/verify-uses-recorded-config`，**D064**）。**40 列 → 40/40 一致。**
      修法不只是用回 `hold_hours_assumed`：**整組旋鈕都存下來**（`pricing_knobs_json`），
      否則下次加旋鈕還會再壞一次。順帶補上這個專案原本沒有的 migration 機制。
  - 現況：**40 列裡 34 列不一致**，而那是「正常的」——重播用**現在的**
    `config.yaml`（`P=12`）去重跑當初用 `P=48` 做的決策。
  - 🔴 **M2 唯一那條可跑的驗收標準，在 D056 改設定的當下就失效了，
    而沒有人發現**（08-30 → 09-05 共六天）。**這是 D048 的同一個病**：
    驗收標準寫下來之後沒有人回頭確認它還成不成立。
  - **修法現成**：`pricing_decisions` 每一列都存著當時的 `hold_hours_assumed`，
    重播時**用那一列自己的值**，不要用現在的設定。
  - ⚠ **不要只是把它改成「允許不一致」**——那等於把儀器關掉。

---

## 🅰 第 1 順位：讓「選價」不再由雜訊決定（D061）

- [~] **1-1　高原容差 ＋ 平手偏高** —— **2026-09-05 已實作，等使用者拍板部署**
      （分支 `feature/plateau-tolerance`）。🔴 **設定值是 0.5 不是 1.0**：跨 113 個
      起跑點量出來 1.0% 贏 94/113 卻**平均是負的**，符號在持有模型與切半之間都會翻。
      完整量測見 D061 的「實作結果」一節。
  - `choose_rate()` 從「取最大」改成
    **「落在最佳值 x% 以內的候選中，取價格最高的那一個」**。
  - 新設定 `ev_plateau_tolerance_pct`，**預設值不是零**
    ——零就是現行行為，而現行行為已經被證明是在挑雜訊。
  - **驗收釘子（拿正式 DB 的最新一窗）**：現行行為選 **8.7600%**
    （第二名 8.9279%，差 4×10⁻⁵ pp）；容差 1% 之下應選 **8.93%**。
  - 🔴 **這一項會改變送到交易所的單（丙道）**，但**不依賴 `P(r)` 的答案**
    ——不論 `P` 最後是多少，「不要用第 6 位有效數字挑價位」都成立。
    **它是唯一可以先做的定價改動。**
  - ⚠ **要一起想清楚重掛的交互作用**：容差讓選價變穩定（同一窗永遠同一答案），
    這對 `_cheaper_repost_is_not_worth_it()` 是好事，但**要驗**，不要假設。

---

## 🅱 第 2 順位：把 `P` 變成 `P(r)`，並同時修掉裁判（D062）

- [x] **2-1　`rate_dependent_hold()` ＋ 修 `empirical_hold()`，同一批做** —— ✅ **2026-09-05 完成**
      （分支 `feature/rate-dependent-hold`）。🟢 **結論沒有翻**：兩個裁判都指向 `P=12`，
      換裁判動 0.065pp、換假設動 1.267pp。🔴 **但「結案」是有條件的**——選價在分界線
      底下，那一階從沒被跨過；**是 D056 把作業點壓到線下的**。詳見 D062 的實作結果。
  - **接縫早就在了**：`HoldModel` 的簽章就是 `(rate, cycle_index) -> float`。
  - 🔴 **兩件事不可以分開做**：`empirical_hold()` 假設 `P ⊥ r`，
    **用它去檢驗「`P` 是不是 `r` 的函式」是用結論證明結論**。
    做完前者而不修後者，等於做完之後仍然沒有東西能裁判它。
  - **純模擬側，機器人碰不到**（甲道）。
  - **先問「策略會不會因此改變決策」，再問「哪一個比較賺」。**
  - ⚠ **修正的方向對「賣便宜」是有利的**（`P` 隨 `r` 遞減 → 現在的模擬器
    高估了高價位）。**這一項不是為了漲價，是為了讓「該不該漲價」第一次有意義。**

- [x] **2-2　`P` 的網格掃描要重跑，而且要在修好的裁判上跑** —— ✅ **2026-09-05 完成**
      （跨 114 個起跑點，兩個裁判並排）。結果見 D062。
  - 🔴 **一律走 `run_policy_across_starts()`**（D054 的硬規矩）；
    **平均／勝率／最差三個一起印**。
  - 現行資料的參考點（**修裁判之前的數字，不可信，只當迴歸釘子**）：
    `P` 填 12／24／48／72 → 選價 8.76／8.93／9.40／10.22%。

---

## 🅲 第 3 順位：加入探索（D063）—— 🔴 **2026-09-05 決定不做，見 D067**

> **不是因為有風險**（錢的風險是每年 0.3 美元），
> **是它在目前的規模與循環速度下答不出問題**：
> 「每 20 次換手探索 1 次」湊到有檢定力的樣本數要 **50 個月**。
> **重啟條件寫在 D067**（本金 10 倍／循環變快／實得年化掉破 7%／想改方法就走整段 A-B）。

> **2026-09-05 追記**：第 2 順位跑完之後，這一項的地位變了。
> `P(r)` 之所以改不了決策，**是因為選中的價位從來沒有跨過分界線**（年化 9.31%）
> ——而探索正是唯一會讓它跨過去的東西。**前面每一項都證明了「現在的作業點很穩」，
> 而沒有一項告訴我們「作業點該不該移動」。只有這一項會。**

- [~] **3-1　每 N 次換手刻意掛一次高價** —— 🔴 **2026-09-05 決定不做（D067）**：
      檢定力差 20 倍。**成本估對了，檢定力沒算過。**
  - 取樣位置建議用**窗內 high 的 p75**（目前約 10.5%）。
  - **成本算得出來**：多等 8h、20 次換手探索 1 次 → 年化影響 **< 0.1pp**。
  - ⚠ **20 是拍的**，必須是設定、必須能關掉。
  - 🔴 **第一版不要用它去改任何決策**——先讓它產生樣本。
  - **它的產出是 `P(r)` 與 `W(r)` 在高價端的樣本**，也就是第 2 順位缺的東西。

- [~] **3-2　M1-c 換它問的問題** —— 🔴 **2026-09-05 查證後決定不做，併進 3-1**
  - **沒被掛出去的那個價位，它的結果是不可觀測的。** 換一個候選進去，落下來的
    仍然只是**模型對那個價位的意見**——而那正是它沒被選中的原因。
    **把模型的意見存進 DB，不會讓它變成證據。**
  - 「事後用 K 線檢查會不會成交」**已經有人在做**：`--simulate`（成交規則拿
    12 筆真實成交驗過，總量比 0.93×）。**D061 與 D066 的表就是這樣算的。**
  - M1-c **真正無可取代的只有四個欄位**（`live_offer_id`／`live_idle_hours`／
    `live_queue_ahead`／`candidate_queue_ahead`，來自訂單簿與即時掛單狀態）
    ——**而它們在那 186 列裡是有值的，沒有壞掉。**
  - **完整敘述見 D063 的 2026-09-05 追記。**

---

## 🅳 第 4 順位：讓裁判可用

- [x] **4-1　`earnings_daily.principal_avg` 要有值** —— ✅ **2026-09-05 完成**（**D065**）。
      分母是**已部署資金**不是本金；只填今天那一列（其餘維持 NULL）。
  - 現況：[`core/bot_engine.py`](../core/bot_engine.py) 傳 `principal_avg=None`，
    09-01 起全是 NULL，實得年化只能手打一個會漂的數字（344.31 → 345.29）。
  - 🔴 **那支 docstring 的理由對「本金」成立，但實得年化的分母不是本金**
    ——是**已部署資金**（錢包 ＋ 場上單 ＋ 部位），而**那是觀測值不是推論**。
    空窗計入分母正是我們要懲罰的東西。
  - ⚠ **要改的是 docstring 的主張，不只是那一行參數**。

- [x] **4-2　`sync_earnings` 加毛／淨對帳** —— ✅ **2026-09-05 完成**（**D065**）。
      實測 **80.3%** 對抽成參考線 85%。
  - 把 `funding_positions` 逐日攤成推算毛利息，與帳本淨利息並排。
  - 現況 **81.2%**（與抽 15% 一致），**但沒有任何東西在看它**
    ——08-16／08-20／08-21／08-31 缺列是靠人眼發現的。

---

## 🅴 第 5 順位：`W` 旋鈕化（原甲-2，已降級）

- [x] **5-1　`ev_wait_statistic` ＋ `ev_censoring`** —— ✅ **2026-09-05 完成**（**D066**）。
      🔴 **`median` 輸 1.0pp**——把 D045 追記的方向反過來了。**KM 沒做**：六個可比較的
      記帳法都指向「不要改」，先不加第七個。順帶發現**現行記帳法比整批丟掉還低估**。
  - **預設值＝現行行為，行為零變化。**
  - 🔴 **為什麼降到第 5 順位**（D063）：在**目前選中的價位**上 `W` 的誤差已經
    無關緊要（估 1.66h、實際 0.13～0.67h）；在**候選的高價位**上仍是決定性的
    （10.22% 估 `W=9.51h`），**而那一半一個校準樣本都沒有**。
    **它的價值被第 3 順位卡住。**
  - 最新校準（12 個樣本，全部是成交的那半）：
    `mean` 中位高估 **9.0×**、`median` 中位高估 **3.9×**。
  - **KM 那一支要寫進 docstring 的兩條界線**：起點重疊 → 樣本不獨立；
    RMST 仍被窗長夾住。
  - **驗收用現成的迴歸釘子**：D047 的離線表 ＋ D1 那對。

**不排進上面五個順位的**：丙-1（`W` 換預設值 ＋ `P` 重調）——
在 `P(r)` 定形之前，那組 `(W 變體, P)` 是在一個已知被污染的裁判前面調參數。
A2-b 的進場守門與 M2 第 3 步／M3 同樣不該插隊。完整敘述仍在下面各自的分期裡。

---
