# PROGRESS

> **這份是 L3「當時發生了什麼」，不是現況。**
> 現在什麼狀態看 [STATUS.md](STATUS.md)，還沒做的事看 [TASKS.md](TASKS.md)。
>
> **本檔只保留 2026-09 起的紀錄。** 更早的完整紀錄在 `archive/`：
>
> - [archive/PROGRESS-2026-08-17~08-31.md](archive/PROGRESS-2026-08-17~08-31.md)
>   —— 真金上線後第一個完整策略週期（D035 → D059，2,643 行）
> - [archive/PROGRESS-2026-07-26~08-16.md](archive/PROGRESS-2026-07-26~08-16.md)
>   —— M1～M4 建置期、真金上線當天、08-16 第一筆成交（D033／D034）
>
> **往後每月 1 號切檔**，舊月份進 `archive/`。
>
> **下面那張索引表涵蓋全部日期**，不論內文搬到哪裡去了。

## 全部紀錄索引（每天一行）

| 日期 | 段落 | 這一天發生了什麼 | 在哪裡 |
|---|---|---|---|
| 2026-07-26 | — | 接手專案、建立 `.project-docs/` 六份文件、M1／M2 起步（D007／D008） | → archive |
| 2026-07-27 | — | M3 資料與可觀測（D013）；修正 git 作者設定 | → archive |
| 2026-08-01 | — | M4 測試套件 227 項、CI 拿掉 `\|\| true`；修好部署的主機端目錄問題 | → archive |
| 2026-08-02 | — | 容器可靠性四項（D016）→ 兩項沒生效 → 改由 systemd Quadlet 接管（D017） | → archive |
| 2026-08-09 | — | 修好 CI 的容器生命週期斷言 | → archive |
| 2026-08-09 | 續 | 程式碼層三項 A3／A4／A5 | → archive |
| 2026-08-09 | 續 | 失效告警 B2 與「不健康就處理」A6 | → archive |
| 2026-08-15 | — | M4 分層搬遷 | → archive |
| 2026-08-15 | 續 | 金鑰檔位置與掛載強化 | → archive |
| 2026-08-15 | 續 | B4：失效告警改三分法（同分支 `deploy/m4-secrets-hardening`） | → archive |
| 2026-08-15 | 續 | LINE Messaging API 接上，M4 完成 | → archive |
| 2026-08-15 | 續 | 首次實單：被拒單、找到金額四捨五入的 bug | → archive |
| 2026-08-15 | 續 | 第一筆真單掛出，隨即抓到取消掛單失效 | → archive |
| 2026-08-15 | 收工 | 實單第一天總結 | → archive |
| 2026-08-16 | — | 掛單 12 小時零成交，查出定價基準用錯 | → archive |
| 2026-08-16 | 續 | 時區統一為 CST | → archive |
| 2026-08-16 | 續 | 盤點待辦並排出工作計畫（直接更新 main，未開分支） | → archive |
| 2026-08-16 | 續 | 新增三項待辦：CI 部署過濾與 LINE 通知規劃 | → archive |
| 2026-08-16 | 續 | P1-3 實作：純文件變更不再觸發部署重啟（同分支） | → archive |
| 2026-08-16 | 續 | P2-3／P2-4：訊息格式統一與交易面通知 | → archive |
| 2026-08-16 | 續 | P1-1／P2-1：訂單簿定價與成交偵測 | → archive |
| 2026-08-16 | 續 | 第一筆成交，以及它險些被自己取消掉 | → archive |
| 2026-08-16 | 夜間第二段 | 用半價借出去了，定價鏈補上成交資料源（D033） | → archive |
| 2026-08-16 | 夜間第三段 | 查證推翻了 P1-4 的原規劃，重掛判準改用推導式（D034） | → archive |
| 2026-08-17 | — | 空轉一整天，查證推翻「市場跌了」的第一印象（D035） | → 08 月檔 |
| 2026-08-17 | 收尾 | PR #31 合併後的盤查與路線圖重排（D036） | → 08 月檔 |
| 2026-08-17 | 深夜 | A1：定價決策進日誌，並修掉一個講錯理由的出口 | → 08 月檔 |
| 2026-08-18 | — | 🎉 期望值策略拿到第一份正面證據；PR #32 與 A1 合併上線 | → 08 月檔 |
| 2026-08-19 | — | 等待估計問錯了問題；閒置時間第一次被量出來（D038） | → 08 月檔 |
| 2026-08-19 | 收尾 | PR #33 合併部署，並補齊 B7／B8 文件對齊 | → 08 月檔 |
| 2026-08-20 | — | 🎉 第二筆正常成交；A2-a／A3 完成 | → 08 月檔 |
| 2026-08-21 | — | PR #34 部署完成；合併後盤查抓到 D4 | → 08 月檔 |
| 2026-08-21 | 續 | PR #35 部署驗證 | → 08 月檔 |
| 2026-08-22 | — | 8/21 起的交易盤點（上線以來最好的一段），以及 D1 的量測落地 | → 08 月檔 |
| 2026-08-23 | — | PR #36 部署驗證；查證下一步時挖出兩個跨輪殘留狀態（D041） | → 08 月檔 |
| 2026-08-23 | 續 | PR #37 合併與部署驗證，以及一次全文件對齊盤查 | → 08 月檔 |
| 2026-08-23 | 續二 | M1-a 市場快照落地（第一次存下市場長什麼樣）；C2 白名單修好 | → 08 月檔 |
| 2026-08-24 | — | 🎉 D040／D041 第一次真實驗收通過（130 輪無殘留）；算出「掃單多久來一次」 | → 08 月檔 |
| 2026-08-24 | 續 | M1-b 決策落地（D043）：DB 第一次存下「我們怎麼想」 | → 08 月檔 |
| 2026-08-24 | 收工 | 盤點文件排出優先序，修掉 B5（D044）：餘額不足不再整夜停機 | → 08 月檔 |
| 2026-08-25 | 深夜 | 市場兩天內翻轉，08-24 的結論失效；第一次去算簿子（掃單 40 次）；`ev_window_hours` 拿到雙向證據 | → 08 月檔 |
| 2026-08-26 | — | **D043 五條驗收全數通過，M1 完成**；查出期望值曲線是平的、等待估計五筆全部高估 3.9 倍（D045） | → 08 月檔 |
| 2026-08-26 | 續 | D045 實作：`wait_report.py` 上線；正式資料抓到一個會靜默吃掉樣本的合併 bug | → 08 月檔 |
| 2026-08-26 | 收工 | 使用者提案帶出 D046：**往上調價從頭到尾沒有判準**；排出 M1-c 反事實落地 | → 08 月檔 |
| 2026-08-28 | — | **M1-c 落地，M1 完成**：新表 `repost_comparisons`；行為零變化用「舊版 vs 新版並排跑」驗出來 | → 08 月檔 |
| 2026-08-29 | — | M1-c 驗收：228 輪 **0 列，而那是正確的**——單子全秒成交，要觀測的狀態一輪沒出現；D045 倍數 3.9 → **5.4** | → 08 月檔 |
| 2026-08-29 | 續 | 部位收回、模型第一次選 **10.95%**；查出等待高估倍數是**駝峰**、樣本全在峰頂；修掉報告誤述；**B6 七支替身實打校正** | → 08 月檔 |
| 2026-08-29 | 續二 | **10.95% 成交了（等 5.19h）**——駝峰被證實；查出 **`median` 8/8 比策略用的 `mean` 準**；hold_report 的分界**不會自己好**；B6 補打 `funding_credits` | → 08 月檔 |
| 2026-08-29 | 續三 | **開獎：掛高輸了**——借 1.98h、實得 **3.02%**（名目最高、實得最低）。誤差約 7 個百分點來自 `P`、1 個來自 `W`，**D045 一路在調小的那一項**；實得年化終於進報告 | → 08 月檔 |
| 2026-08-30 | — | **第三個誤差源被量出來（D047）**：那張 10.91% 掛 13 小時沒成交，而模型算出的實質年化**單調上升 9.03% → 9.46%**——乾旱期會讓等待估計自己變好看；M2 拿到第二條可跑的驗收標準 | → 08 月檔 |
| 2026-08-30 | 續 | **M2 第 1 步：重播器上線**（`core/backtest.py`）。**驗收 25/25 對上正式紀錄**；同時查出 **PLAN 原本的驗收標準比它依賴的那張表早寫了六天、根本跑不起來（D048）**；測試 726 → **756** | → 08 月檔 |
| 2026-08-30 | 續二 | **M2 第 2 步：成交模擬上線（D049）**。成交規則拿 12 筆真實成交驗過**總量比 0.93×**；**D1 第一次有答案**，但方向切半就翻、細掃是雜訊。🔴 **就算 `P` 真的是 48，假設 48 仍不是最好的**——那個 48 在替 `W` 的偏差擦屁股；測試 756 → **791** | → 08 月檔 |
| 2026-08-30 | 續三 | **A2-b 用 M2 比過六種重掛政策，答案是否定的（D050）**：分不出勝負，而成因是**候選價位是落後訊號**（最長那段空掛裡前 32 小時完全沒動）。順帶**推翻了 D047 的普遍性**——乾旱夠久之後方向會掉頭；測試 791 → **804** | → 08 月檔 |
| 2026-08-30 | 續四 | **P2-2：帳本接上了（D051）**，`earnings_daily` 不再是空的。🔴 **而它把前面每個績效數字都往下修**：7.99% → 6.54% → **帳本 5.34%**。B6 的預測中了（`[2]` 不是 placeholder、值是字串、混著轉帳）；測試 804 → **827** | → 08 月檔 |
| 2026-08-30 | 續五 | **帳本同步接進主迴圈（D052）**：放迴圈層不放 `run_once()`（後者幾乎每輪提早 return），並吞掉所有例外。⚠ **寬鬆的 `except` 當場咬我一口**——漏一行 import，機器人照跑、測試全綠；測試 827 → **835** | → 08 月檔 |
| 2026-08-30 | 收工 | **日結收益摘要上線（D053），P2-4 全部完成**。判準是**資料不是時鐘**、狀態存新表 `bot_kv`（加欄位對既有表沒效果）、**刻意不寫「實得年化」四個字**；測試 835 → **844** | → 08 月檔 |
| 2026-08-30 | 收工二 | **D050 的訊號量出來了（D054）**：偵測 5/5、零誤報、門檻不敏感，**但拿它降價賠 1.97pp**。🔴 **附帶推翻 D049／D050 的比較方式**——單一起跑點會被相位運氣主導，同一個政策符號會相反；測試 844 → **851** | → 08 月檔 |
| 2026-08-30 | 收工三 | 🟢 **D1 的證據夠了（D055）**：用跨起跑點重做，D049 那兩條「不能改」的理由**都是相位假象**——切半三段同向、曲線單調、**8～20h 一片高原**，而實測持有就落在裡面。第二條獨立證據（純平移最佳 −10%）指向同一量級；測試 851 → **859** | → 08 月檔 |
| 2026-08-30 | 收工四 | 🟢 **D1 上線了（D056）**：`assumed_hold_hours: 12`，選中價位 10.91% → **9.09%**。⚠ **D055 建議的改法本身是錯的**——`offer_period` 身兼「合約天期」與「算式的 P」兩職，而合約天期是整數、交易所最短 2 天，**先拆開才改得動**；測試 859 → **865** | → 08 月檔 |
| 2026-08-31 | — | 🟢 **D056 吃到第一筆**：部署當場把躺 24.1h 的 10.91% 換成 **9.00%**，**33 分鐘成交**（n=1，仍在生息）。🔴 **查出量測缺陷（D057）**：`loan` 與 `credit` 是同一筆錢的兩個狀態，**17 筆其實是 14 筆**、完成率 35.3% → **42.6%**。🟡 **使用者的假說立案（D058）**：`P` 可能是 `r` 的函式。🟢 **工作分三條車道（D059）**。**沒有改任何程式** | → 08 月檔 |
| 2026-09-05 | — | 🟢 **D056 五天實跑驗收（D060）**：實得年化 5.72% → **7.75%**、利用率 **98.8%**，但 `P=12` 被實測否定（借滿 48h）。🔴 **選價由浮點雜訊決定（D061）**：前兩名差 4×10⁻⁵ pp，而平手規則偏向便宜那邊。🔴 **目標函數自我矛盾（D062）**：`P` 是常數，卻依賴被最佳化的 `r`，連回測裁判一起被污染。🔴 **所有樣本都是模型自己選出來的（D063）**：M1-c 長了 187 列，186 列零資訊。**沒有改任何程式** | 本檔 |
| 2026-09-05 | 續 | 🟢 **一天合併八個 PR，把量測層整個補完**：#60 幽靈樣本（D057，**抓到第三個家**——`OBSERVED_HOLD_HOURS` 這個寫死的常數，回測用了它六天）、#61 高原容差（D061，**0.5 不是 1.0**）、#62 **三個報告端各自重算「誰被選中」**（會記下機器人沒掛過的價格，D026 家族第六次現身）、#63 **`--verify` 靜默失效六天**（D064，40 列 → **40/40 一致**，順帶補上 migration 機制）、#64 **`rate_dependent_hold()`（D062）——而結論沒有翻**、#65 **分母不是本金是已部署資金（D065）**、#66 **`W` 的兩個旋鈕（D066，`median` 輸 1.0pp）**、#67 把旋鈕寫進設定檔。🔴 **「結案」是有條件的**：選價在分界線底下，那一階從沒被跨過；**是 D056 把作業點壓到線下的**。測試 865 → **935** | 本檔 |
| 2026-09-06 | — | ✅ **D061 三條驗收全過**（實選 **8.9279%**，舊行為重播 8.7600%；4 列選價一致；零額外重掛），**順帶驗到 D064** 的第一列自帶旋鈕紀錄。🟢 **D067：決定停下來**——等更多單子在結構上不會改變答案，而原規劃的探索**檢定力差 20 倍**（要 50 個月）。🟡 **12 小時換手 3 次、持有 1.2～1.3h**（先前中位 16.29h），可能觸發重啟條件 2 但 **n=2 不算數**。**沒有改任何程式** | 本檔 |
| 2026-09-06 | 續 | 🔴 **我自己種下的 CI 缺陷，當天咬回來**：歸檔時用了中文檔名，`git diff` 把非 ASCII 路徑**加引號**，於是一次純文件推送觸發部署、重啟容器（**D068**，沒有損害但那是運氣）。修法 `core.quotePath=false`，**白名單一個字沒改**。🔴 **同一症狀第三次，但病因完全不同**——前兩次是涵蓋範圍。⚠ **閘門 fail-open，失效的樣子就是「多部署一次」**，與 D064 同形狀 | 本檔 |
| 2026-09-12 | — | 🔴 **一週績效複查，抓到一個被用了一週的錯數字**：STATUS 的「實得年化 7.75%」是拿**入帳日數**當分母算的，誠實的 7 日滾動是 **6.83%**、上線至今 **6.14%**——**D067 的重啟條件 3（跌破 7%）其實已成立兩週**。根因是 `sync_earnings.py` 沒讀回 `principal_avg`，照維運指令跑**根本印不出實得年化**。🔴 **9% 目標從一開始差一個 0.85**（抽成 15%）。**績效由利用率決定**：99.2%→81.6%→90.2%，理論值三週都對得上帳本（≤0.2pp）。🟢 **D069：天期 2 → 30、`P` 12 → 48，選價 9.125% → 10.500%**（高原，命中 46 次）。❌ 途中否決兩項：「躺太久降價」**回測否決**（不重掛最好，差 ≤0.08pp）、重啟條件 2 **明確否決**（中位 16.29h、24 段）。⚠ **K 線缺根讓等待低估 1.25 倍**，打平點 15.4h → **19.2h** | 本檔 |
| 2026-09-12 | 續 | 🟢 **D069 部署完成**（容器帶 `offer_period: 30`、healthy）。接著修週報，**又抓到兩個同一家族的缺陷**：① 曆日數少算一天（`(last-start).days` 沒 +1，而同一函式的空白日判斷用 `elapsed+1`——**同一份報告兩種天數**，年化 6.87% 印成 7.86%）；② **`expected_gross_interest()` 根本沒有窗參數**，帳本那半被 `--since` 篩了、推算那半沒有，`--since 2026-09-05` 印出淨毛比 **27.1%** 的假警報（正確 85.0%）——🔴 **它原本只在 `--since` 等於專案起點時才碰巧正確，而那是唯一被跑過的用法**。順帶做掉甲-2（利用率分解），三個窗驗過差 **0.00～0.06pp**。🔴 **這支報告以前一條測試都沒有**，已補 `test_sync_earnings.py`。測試 936 → **960** | 本檔 |
| 2026-09-19 | — | 🔴 **長天期第一週複查，而結論是「我用錯了判準」**（D070）。第一週 **5.13%**（利用率 90.2%→**51.1%**、名目 8.96%→**10.87%**、等待中位 0.13h→**14.55h**），照 D069 的硬停損 6.0% 該收掉。**使用者反駁：短天期測了一個月才換，長天期一週就判死刑不公平。** 去攤開短天期那 22 個 7 日窗——🔴 **它自己的第一週是 4.03%**，比長天期第一週（5.14%）還差，第二週才爬到 8.00%。**那條線在 08-23 就存在的話，短天期活不過第一週。** 根因：D069 寫的是「現有設定**成熟後**的區間 6.10%～8.02%」，**那三個字把前六個窗排除了，然後那條線被拿去判一個還沒成熟的設定**。🟢 **定案跑滿一個月到 10-14，一行程式不改**（代價 0.20 美元）。順帶量到：**截斷論證是反的**（上限放到 720h，三筆持有 2.40／32.17／25.36h，沒一筆碰到舊的 48h）、**`estimate_wait()` 偏誤方向隨天期翻轉**（天期 2 高估 1.09×、天期 30 **低估 3.1 倍**）、`market_rate()` 冷門天期觸發 298 次但**實際擋住 0 次**、**天期 30 只佔簿子 0.4%**（一句 SQL 就查得到，不必花一週真金）、**M1-c 從 0 列長到 221 列**（220 是 `hold_matched`） | 本檔 |

---

## 2026-09-05 —— 五天沒回來，而這五天正好是 D056 的第一組完整證據

**沒有改任何程式。** 這一段全部是查證：容器自 08-30 22:57 起 healthy、
日誌 0 ERROR、跑滿 5 天 4 個完整循環。四則決策 **D060／D061／D062／D063**
都是從這批資料讀出來的，沒有一項來自推論。

### 一、D056 有效，而且效果比預期大——但它的理由是錯的（D060）

| 項目 | D056 之前 | D056 之後（09-01～09-04 四整天） |
|---|---|---|
| **實得年化（帳本）** | 5.72% | **7.75%** |
| 換手空窗 | 3.6h／38.1h／24.7h | **0.68h／0.26h／0.13h** |
| 資金利用率 | 約 7 成 | **98.8%** |

**瓶頸換人了**：以前是「借不出去」，現在空窗接近 0，**唯一剩下的槓桿是價格**。
而 15% 手續費是常數（帳本淨 ÷ 推算毛 = **81.2%**，與 85% 一致），改不動。

**但 `assumed_hold_hours: 12` 被實測否定**：D056 後結清 3 筆，
**48.33h／48.19h 都借滿 2 天**、1 筆 1.43h；第 4 筆 `465110649` 已持有
39.3h 仍在生息。在 8.76～9.00% 這一帶，持有時間是 **48h，不是 12h**。

### 二、選中哪個價位，目前是由浮點雜訊決定的（D061）

拿正式 DB 的最新一窗跑 `expected_value.py` 本尊：

```
 8.7600%  實質=7.695058824   <== 選中
 8.9279%  實質=7.695017700   差 0.00004 個百分點
```

距最佳 1% 以內有 4 個候選，橫跨 8.74%～8.93%。而 `choose_rate()` 用
`effective > best_effective` ＋升冪掃描，**平手時系統性地選便宜的那一個**。
正式環境 09-04～09-05 在 8.76% 與 8.93% 之間來回跳，跳的就是這 4×10⁻⁵。

### 三、目標函數是自我矛盾的（D062）

同一窗、只改 `P`：

```
  掛價% |  P=12h  P=24h  P=48h  P=72h        P 假設 → 選中價
   8.76 |   7.69   8.19   8.47   8.56          12h → 8.76%
   8.93 |   7.69   8.26   8.58   8.70          24h → 8.93%
   9.40 |   6.96   8.00   8.64   8.88          48h → 9.40%
  10.22 |   4.32   6.11   7.70   8.44          72h → 10.22%
```

照實測把 `P` 設成 48h，策略就跳到 9.40%——**但 48h 是在 8.9% 觀察到的，
9.40% 的持有時間我們沒量過**。`rho(利率, 持有) = −0.383`（n=17，D058 的 −0.29 更新）。
**這不是「該填哪個數字」，是不動點問題。**

而 `--simulate` 現在不能當裁判：`empirical_hold()` 整包抽持有時間，
**等於給高價位配了一個它拿不到的持有時間**。

### 四、所有樣本都是模型自己選出來的（D063）

- 12 個等待校準樣本全部落在 **8.76～9.50%** 這條機器人自己選出來的窄帶；
  10% 以上只有 2 個意外樣本，而它們正好是 D058 的全部關鍵證據。
- `repost_comparisons` 從 0 列長到 **187 列**，但 **186 列是 `hold_matched`
  （候選＝場上同價）**，唯一 1 列 `repost` 是 D056 部署自己觸發的。
  **儀器在跑，資訊量是零。**

### 五、順帶查到的兩件小事

- `bot_engine.py:1166` 傳 `principal_avg=None`，所以 09-01 起 `earnings_daily`
  的本金全是 NULL，實得年化只能手打一個會漂的數字（344.31 → 345.29）。
- `expected_value.py` 那段「實測平均 16.93h／中位 11.61h」的註解已過期
  （現為 22.60h／11.62h）。

### 這一天沒做的事

**沒有跑測試，因為沒有改程式。** 甲-2（`W` 旋鈕化）刻意往後排，理由寫在 D063：
它的價值被「高價位沒有樣本」卡住。

---

## 2026-09-05（續）—— 一天五個 PR，而最有價值的是一個「否定」的結果

接續前一段的盤查，把五個順位裡的前三個做完。**測試 865 → 911。**

### PR #60 —— D057 的幽靈樣本，以及它的第三個家

修 `hold_report`／`wait_report` 的取樣是預期中的。**沒預期到的是第三個家**：
`fill_simulation.OBSERVED_HOLD_HOURS` 這個**寫死的常數**裡有三個 `0.50h` 是
`loan` 的列，佔一袋抽籤的 18%——**整個回測用了它六天**。

順帶：`wait_report` 有一筆**真的配錯了**（08-28 那段報「借 0.50h」，實際 11.62h，
差 23 倍），而 **D054 的「零誤報」不成立了**（0/10 → 1/9）。

**拆單刻意不合併**，只多印一行「獨立段數其實是 16」——上一次靠「同利率、
時間接近」合併，靜默吃掉了三個校準樣本（D045）。

### PR #61 → #62 —— 改對了一件事，同時弄壞另一件

#61 的高原容差設 **0.5 而不是 1.0**：跨 113 個起跑點，**1.0% 贏 94/113 卻平均是負的**，
符號在持有模型與切半之間都會翻。**D054 那個「只看勝率會得到相反結論」的陷阱第二次現身。**

🔴 **而 #61 上線當天就咬了一口。** 三個報告端都寫著
`chosen = max(last_evaluation, key=effective)`——**在容差存在之前那是對的**。
容差一加，`choose_rate()` 選高原上緣，三處還在報最高分：機器人會掛 **8.9279%**，
日誌／`offer_wait_forecasts`／`pricing_decisions` **一致地**說 8.76%。
**三個都錯成同一個值，所以看起來完全正常。**

**沒有造成損害**（資金鎖在部位裡，0 列壞資料），但
**879 條測試全綠，一條都沒比對過「報出來的」與「掛出去的」是不是同一個數字**。
**不是測試不夠多，是缺了那個不變式。**

### PR #63 —— 驗收工具靜默失效了六天（D064）

`--verify` 在 D056 改設定的當下就壞了：**40 列裡 34 列「不一致」，而那 34 列一列都沒錯**。

🔴 **它報的是「不一致」，跟「工具壞了」長得一模一樣**——`market_candles` 少存幾根 K
也會不一致，所以**沒有任何訊號告訴人「這次是另一回事」**。**D048 的同一個病第二次現身，
而且更難發現，因為它從頭到尾都有輸出。**

修法不只是用回 `hold_hours_assumed`：**整組旋鈕都存下來**（`pricing_knobs_json`），
否則下次加旋鈕還會再壞一次。順帶補上這個專案原本**沒有的 migration 機制**
——拿正式 DB 的副本實測過，197 列不變。**結果 40/40 全數一致。**

### PR #64 —— 而第 2 順位的答案是否定的（D062）

跨 114 個起跑點，兩個裁判並排：**都指向 `P=12`**。換裁判動 **0.065pp**，
換假設（12 對 48）動 **1.267pp**——**差 19.4 倍**。
而且**策略就算自己用 `P(r)` 也選同一個價位**（8.93%）。

🔴 **原因是結構性的**：選中的價位在分界線（年化 9.31%）**底下**，
**那一階從來沒有被跨過**。

**但「結案」是有條件的**：整段歷史只有 **2.8%** 的決策點跨過分界線（用現在的 `P=12`），
而正式環境 08-17 起真的掛出去的 16 張單有 **43.8%** 跨過（那時是 `P=48`）。

> **是 D056 把作業點壓到分界線底下，才讓 D062 的疑慮變成無關緊要的。**
> **D062 因此是 D063 的前置條件，不是一個可以獨立收掉的改動。**

### 今天學到的三件事

1. **「一致的三個錯誤」比一個錯誤難發現得多**（#62）。
2. **「驗收標準本身不會被驗收」**（#63）——而它失效時報的是它平常就會報的東西。
3. 🔴 **D054 的相位運氣第三次現身**：D062 那條迴歸測試第一次寫成單一起跑點時，
   量到「換裁判 0.24pp、換假設 0.36pp」，**看起來同一個量級，結論差點反過來**。
   跨 24 個起跑點才是 0.065 對 1.267。**測試裡因此寫死了 `assert len(starts) >= 20`。**

### 還沒做的

**第 3 順位（探索，D063）是唯一還會改變答案的一項**——前四項都在證明
「現在的作業點很穩」，沒有一項告訴我們「作業點該不該移動」。
它會真的掛高價出去，屬於丙道，**要使用者拍板**。

🔴 **2026-09-06 追記：這一段的結論被推翻了。** 探索**在目前的循環速度下
沒有檢定力**（每 20 次探索 1 次要 50 個月），而且**等更多資料在結構上
不會改變答案**。完整敘述見下一段與 **D067**。

---

## 2026-09-06 —— 驗收全過，然後決定停下來

**沒有改任何程式。** 兩件事：D061／D064 的驗收被觸發並通過，以及一個
「不要再改了」的決定（D067）。

### 一、D061 的三條驗收全過（順帶驗到 D064）

資金於 **09-06 02:39** 回到錢包，觸發 D061 上線後的第一次真實定價。

| # | 條件 | 結果 |
|---|---|---|
| 1 | 選中價位不是高原下緣 | ✅ **8.9279%**（4 列全部）。**同一窗用舊行為重播是 8.7600%** |
| 2 | 選價變穩定 | ✅ 4 列的**相異選價只有 1 個** |
| 3 | 沒有多出重掛 | ✅ 只有 1 列 `hold_matched`，**零 `repost`** |

**順帶驗到 D064**：那 4 列的 `pricing_knobs_json` 有值了（完整九個旋鈕）
——**第一列「自己記得當時設定」的紀錄**。在此之前 40 列全靠慣例回推。

⚠ **這三條驗的是行為，收益驗不出來也不打算驗**：+0.06pp 在 345 USD 上一年 0.2 美元。

### 二、D067：等更多單子不會改變答案

做完六個順位之後的實際問題：**是資料不夠，還是這件事到頂了？**
三條各自獨立的依據指向後者。

**依據一：新樣本不會落在缺資料的那一區。** 每月約 28 段，而 D056 之後產生的
每一段利率都是 8.76／8.93／9.00%——**昂貴組（≥9.31%）新增 0 段**。

**依據二（決定性）：連最有利的假設都改不了結論。**

| 「實際借多久」的假設 | 最佳的策略假設 `P` |
|---|---|
| 全部 17 段（現行） | **12h** |
| 只用 D056 之後那 3 段 | **12h** |
| **假設每一筆都借滿 48h** | **12h** |

🔴 **因為 `assumed_hold_hours` 不是一個對持有時間的估計，它是一個
「多討厭等待」的旋鈕。** 名字取錯了，而那個錯誤名字讓 **D055／D056／D062
三次都把它當成「要不要把估計改對」的問題**。

**依據三：沒有不花錢的替代路徑。** 窗內成交量中位數 **1.7 億 USD**，
我們的 345 USD 佔 **0.0002%**——這個規模下排隊位置完全不相干。

### 三、而原本規劃的探索，檢定力差 20 倍

D063 寫的是「每 20 次換手探索一次，年化影響 < 0.1pp」。
**成本估對了，檢定力沒算過。** 每組要約 **70 段**：

| 探索頻率 | 湊到 70 段要 |
|---|---|
| 每 20 次 1 次（**原規劃**） | **50 個月** |
| 每 1 次 1 次（等於換策略） | 2.5 個月 |

> **「灑一點探索」在這個循環速度下不是實驗，是四捨五入的誤差。**

### 四、🟡 一則要盯著的觀察

09-06 那半天**12 小時換手 3 次、持有 1.33h／1.18h**，而先前 18 段的中位是
**16.29h**。若成為常態，D067 的重啟條件 2 就成立，70 段不用等半年。

🔴 **但 n=2 不算數**——前面才剛記取過「借滿 48h 出現兩次就差點被當成常態」
的教訓（D060）。**而且短持有本身不是好消息**：每段賺得少、換手成本攤在更多次上。
判準（連續 7 天中位持有 < 4h 且段數 ≥ 40）寫在 D067 追記，**免得下次靠印象決定**。

### 接下來

**不改程式，讓它跑。** 維運日常一週不到 5 分鐘（看帳本、看容器），
四個重啟條件寫在 D067。**這是「在目前條件下答不出來」，不是「掛高價比較差」**
——那個問題仍然開著。

---

## 2026-09-06（續）—— 我自己種下的一個 CI 缺陷，當天就咬回來

**起因是上一段的歸檔動作。** 把 TASKS 的六個順位搬進 archive 時，
順手把檔名取成 `TASKS-六個順位-2026-09.md`——**而專案裡其他每一個檔案都是 ASCII。**

15:01 推上去，**CI 照常部署、重啟了容器**，而那次推送**只動了 `.project-docs/`**。

⚠ **沒有造成損害**（資金鎖在部位 `465247022` 裡、場上沒有掛單、重啟後 0 ERROR），
**但那是運氣不是設計**——若當時場上躺著一張單，那就是一次不必要的風險。

### 病因：`git diff --name-only` 會把非 ASCII 路徑加引號

```
.project-docs/CHANGELOG.md
.project-docs/archive/README.md
".project-docs/archive/TASKS-\345\205\255\345\200\213\351\240\206\344\275\215-2026-09.md"
```

白名單是 `^(\.project-docs/|README\.md$|…)`，**那個開頭的 `"` 讓最後一行對不上**。

修法：`git -c core.quotePath=false diff --name-only`。**白名單一個字都沒改。**

### 🔴 而它是同一症狀的第三次，前兩次的修法在這裡沒有用

工作流程的註解本來就寫著「白名單是踩出來的……**所以要修的是涵蓋範圍，不是規則本身**」
——**那句話對 08-19／08-23 那兩次是對的，對這一次是錯的。**
這次的路徑**本來就在涵蓋範圍裡**，是被**引號**擋在外面的。

> **同一個症狀有兩個完全不同的病因，
> 而第一個病因的修法會讓人以為第二次也是同一件事。**

### 它為什麼躲過檢查

閘門是 **fail-open** 的，所以**它失效的樣子就是「多部署一次」，跟正常行為長得一樣**
——**與 D064 完全同一個形狀**。這次是**剛好去看容器啟動時間**才發現的。

**沒有自動偵測，也沒有加測試**（閘門是 shell 腳本、跑在 GitHub Actions 裡，
現有 pytest 碰不到它）。**這是已知的空缺，不是疏漏。**

### 今天的第二個教訓

前一段剛寫下「**驗收標準本身不會被驗收**」（D064），而**幾小時後就用另一個形式再中一次**
——這次是「防止不必要部署的閘門」自己沒有被驗收。
**兩者都是 fail-open，兩者失效時都不會出聲。**


---

## 2026-09-12 —— 一週績效複查，然後放寬天期

**完整決策敘述在 [DECISIONS.md](DECISIONS.md) 的 D069，這裡只記當天的過程。**

### 一、本來只是要看這週績效

跑 `sync_earnings.py --since 2026-08-15`，帳本對帳正常（淨毛比 82.9%，沒有缺列）。
但它**沒有印出實得年化**——於是去追為什麼，結果發現 STATUS 上那個 7.75% 是手算的，
而且**分母用錯了**：用的是「有入帳的天數」，不是曆日。

**空白日不是缺帳**。帳本按每筆單子的 24 小時結算點入帳，所以 09-09 整天沒有列，
而 09-10 那筆 0.0784 USD 就是前一筆 26.26h 的全額。**空白日要留在分母裡。**

| | 舊算法 | 誠實算法 |
|---|---|---|
| 09-01～09-04 | 7.73% | （那四天剛好沒有空白日，所以一樣） |
| 最近 7 天 | — | **6.83%** |
| 最近 14 天 | — | **6.46%** |
| 上線至今 27 天 | — | **6.14%** |

### 二、為什麼工具沒印

`format_summary()` 只在 `principal` 有值時才印實得年化，而 `main()` 傳的是
`args.principal`——**它從來沒讀回 D065 落地的 `earnings_daily.principal_avg`**。
所以 D065 的追記「本金不必再手打了」**對機器人成立，對這支報告不成立**。

👉 **D048／D064 那個家族的第三次現身：報告端與寫入端各自為政。**
已列為 TASKS 的甲-1。

### 三、診斷：績效是被利用率決定的

金額加權重算三週（用時數加總會因為 08-28 的拆單算出 113%）：

| 期間 | 利用率 | 加權名目 | 理論淨 | 帳本 |
|---|---|---|---|---|
| 08-22～08-28 | 99.2% | 9.22% | 7.77% | 7.96% |
| 08-29～09-04 | 81.6% | 8.98% | 6.23% | 6.24% |
| 09-05～09-11 | 90.2% | 8.96% | 6.87% | 6.83% |

**三週都對得上（≤0.2pp）。名目價格幾乎沒動，變的全是利用率。**
本週的缺口幾乎全來自 **09-08 一段 16.20 小時的空手**——當時市場成交最高只有
7.30%，我們掛 8.93% 搆不到；策略重估 96 次都選同一個價；23:00 一波掃單才成交。

### 四、途中否決了兩項

- **「躺太久就降價」**：`--repost` 五種政策 7.27～7.35%，**最好的是不重掛**。
  這個否決有份量，因為成交模擬器**重現了 09-08**（16.20h 對 15.85h，0.98×）。
- **D067 重啟條件 2（循環變快）**：持有中位 16.29h、24 段。
  09-06 那個 n=2 沒有延續（09-08 借 26h、09-10 借滿 48h）。**結案。**

### 五、也講錯了一件事，當天更正

先是從 `trade_period_rates_json` 讀到「天期 7／30 比天期 2 高 2～4pp」。
**那個比較是錯的**——那是成交**加權中位數**的差，而我們根本不掛在中位數，
**我們掛在掃單高點**，而各天期的 K 線 `high` 中位數幾乎一樣（9.12% vs 9.24%）。

真正的槓桿是**長天期把等待攤平了**，於是策略敢挑更高的價。

### 六、然後踩到一個新的量測問題

天期 30 的 **168 根 K 線實際涵蓋 210 小時**——23% 的小時是缺的。
而 `estimate_wait()` 是數「幾根」再乘 1 小時，所以它**低估 1.25 倍**。

| | 未校正 | 校正後 |
|---|---|---|
| 預估等待 | 4.71h | **5.89h** |
| 打平 6.83% 需要借滿 | 15.4h | **19.2h** |

**19.2h 落在實測中位（16.29h）之上、平均（23.24h）之下。**
仍然做這個改動，理由是**截斷論證**：24 筆有 9 筆被 48h 上限砍斷，
KM 在 48.6h 仍有 37.5% 存活——**放寬天期本身就會把這個盲區打開**。

⚠ **刻意不修那個缺根偏誤**：它與 D045 量到的偏誤方向相反，
淨效果不能手算宣告。那是 `W` 的校準，M2 的題目。

### 今天的教訓

**「工具沒印出那個數字」比「工具印錯數字」更危險。**
D064 學到的是「驗收標準本身不會被驗收」；這次是它的鄰居——
**報告少印一行，人就會自己手算，而手算不會留下它用了什麼分母。**

---

## 2026-09-12（續）—— 修那個報告，又抓到兩個鄰居

**D069 部署完之後**（容器帶著 `offer_period: 30` 起來、healthy），
接著修第一節那個「報告不印實得年化」的缺陷。**修的時候試跑不同的 `--since`，
又抓到兩個同一家族的東西，而兩個都會直接害到我剛設的那條停損。**

### ① 曆日數少算一天

`elapsed = (last - start).days` 是相減，而曆日數是**含頭含尾**。
更糟的是**同一個函式裡的空白日判斷用的是 `elapsed + 1`**——同一份報告裡兩種天數。

09-05 → 09-12 其實是 8 天，算成 7 天讓年化從 6.87% 變成 **7.86%**。

👉 **這跟早上那個錯數字是同一個病的兩個版本**：一個用列數當分母，一個少算一天。
**「分母」在這支報告裡被弄錯過兩次。**

### ② 毛／淨對帳的兩半涵蓋不同期間

`expected_gross_interest()` **根本沒有窗參數**——它把所有部位無條件加總，
而帳本那半已經被 `--since` 篩過了。於是 `--since 2026-09-05` 印出
**淨毛比 27.1%** 與一句「🔴 比參考線低 57.9 個百分點」的**假警報**。

🔴 **它原本只在 `--since` 等於專案起點時才碰巧正確——而那正是唯一被跑過的用法。**
如果我沒有因為別的理由去跑一個短窗，這個缺陷會一直待在那裡，
**而且它每週都會對著一個健康的機器人喊狼來了。**

### 修完的驗收

| 窗 | 利用率 | 加權名目 | 利用率×名目×0.85 | 帳本實得 | 淨毛比 |
|---|---|---|---|---|---|
| 09-05 起 8 天 | 90.0% | 8.98% | 6.87% | **6.87%** | 85.0% |
| 08-29 起 15 天 | 86.1% | 8.98% | 6.57% | **6.57%** | 85.0% |
| 08-22 起 22 天 | 90.3% | 9.06% | 6.96% | **7.02%** | 85.7% |

**分解值與帳本差 0.00～0.06pp。** 順帶把甲-2（利用率那一行）一起做了
——它存在的理由是停損觸發時的第一個問題：**是借不出去，還是賣太便宜。**

### 今天真正的教訓

**這支報告以前一條測試都沒有**，而它是「唯一一條交易所自己說的錢」的出口。
三個缺陷能一起活著，不是因為它們難，是因為**沒有人在看它有沒有在說實話**。

新增的 `tests/unit/test_sync_earnings.py` 釘的**不是「算得對不對」，
是「該印的有沒有印出來」**——因為今天的因果鏈是：
**少印一行 → 人手算 → 手算不會留下它用了什麼分母 → 錯的數字被寫進 STATUS 一週。**

而其中一條特別值得留著：`test_兩邊對不上就要說對不上`。
**一個永遠說「對得上」的對帳段，跟沒有對帳段一樣**
——那正是 D064（驗收標準本身不會被驗收）與 D068（閘門 fail-open）的同一個形狀，
**今天是它的第三次現身，而這次是在我自己剛寫的程式碼裡。**


## 2026-09-19 —— 長天期第一週，以及一條在錯誤樣本上校準的停損線

**完整決策見 [DECISIONS.md](DECISIONS.md) 的 D070。這裡只記當天發生的順序。**

### 一、先跑數字，結論看起來很明確

跑 `sync_earnings.py` 三個窗（純新設定 09-14～09-19 6 天 **5.13%**、
09-13～09-19 7 天 **5.52%**、09-05 起 15 天 6.24%），分解全部對得上帳本。

利用率 **90.2% → 51.1%**，名目 **8.96% → 10.87%**——**D069 想要的那 2pp 確實拿到了**，
但等待從平均 1.89h 變成 **15.64h**（中位 0.13h → **14.55h**，112 倍）。

照 D069 寫的硬停損（跌破 6.0% 改回天期 2），該收掉。**我也這樣建議了。**

### 🔴 二、使用者反駁，而他是對的

> 「我們當初實際測試兩天期的，也測了一個月，我們才改變策略的不是嗎？
> 感覺可以等滿一個月後，再來看看績效？會不會比較公平？」

去把短天期那一個月的 22 個 7 日窗全部攤開：

```
08-17～08-23   4.03%  ← 第一週
08-18～08-24   4.97%
08-19～08-25   5.41%
08-20～08-26   5.64%
08-21～08-27   6.85%
08-22～08-28   7.93%
08-23～08-29   8.00%  ← 第二週就爬到最高
```

**短天期自己的第一週是 4.03%，比長天期的第一週（5.14%）還差。**
**那條 6.0% 的線如果在 08-23 就存在，短天期活不過第一週。**

🔴 **根因**：D069 第七節寫的是「現有設定**成熟後**的區間是 6.10%～8.02%，
6.0% 在歷史地板之下，不會誤觸發」。**「成熟後」三個字把前六個窗（4.03%～5.64%）
排除在外了**——然後那條線被拿去判一個**還沒成熟的設定**。

👉 **校準的樣本期間與適用的樣本期間不同，而那個「不同」躲在一個形容詞裡。**
這是 D048／D064／D068／D069 那個家族的再一次，只是這次不在程式裡，在判準裡。

### 三、順帶量到的四件事

1. 🔴 **D069 賭的截斷論證是反的**：上限從 48h 放到 720h，三筆持有
   **2.40／32.17／25.36h，沒有一筆碰到舊的 48h**。`hold_report` 同向
   （便宜組中位 45.08h vs 昂貴組 11.62h）——**決定持有時間的是價格，不是天期**（n=3）。
2. 🔴 **`estimate_wait()` 的偏誤方向隨天期翻轉**：天期 30 下四段全部低估、
   總量比 **0.32×（低估 3.1 倍）**，而 D069 只預告 1.25 倍；同一支函式在天期 2 下是**高估**。
3. **`market_rate()` 冷門天期樣本不足**觸發 **298 次**（前一個月 4 次），
   🔴 **但「當時錢是自由的而真的被擋住」0 次**——地雷，不是傷口。
   **那支函式的 docstring 早就寫過這件事，翻旋鈕時沒人回頭讀。**
4. 🔴 **天期 30 只佔簿子 0.4%、天期 2 佔 71.4%。**
   **`market_snapshots` 從 08-23 起就查得到——不必花一週真金去問它。**

另外 **M1-c 那台「裝好但沒被觸發過」的儀器終於開始長了**：
`repost_comparisons` 0 列 → **221 列**，而 **220/221 是 `hold_matched`**
——「每十分鐘比一次、該調高就砍掉重掛」的擔心，在這批資料上不成立。

### 四、定案

**維持天期 30 跑到 2026-10-14，期間一行程式都不改**（連上面第 3 點那個地雷也不修
——**測試期間改程式，正是會讓測試不公平的那件事**，跟使用者糾正我的是同一個道理）。
代價算過：若一直維持 5.13%，再跑 21 天與改回短天期相比差 **0.20 美元**。

**10-14 的判準已經先寫死在 D070 第七節**，對照組用「短天期整月 6.20%」
而不是它最好的那一週 8.00%——**挑對手最好的一週來比，跟挑自己最好的一週一樣是作弊。**
