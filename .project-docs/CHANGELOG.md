# CHANGELOG

> **這份是 L3「某個版本改了什麼」。** 現在什麼狀態看 [STATUS.md](STATUS.md)。
>
> **本檔保留真金上線（2026-08-15）之後的變更。**
> 上線前的 M1～M4 建置期完整紀錄在
> [archive/CHANGELOG-M1-M4.md](archive/CHANGELOG-M1-M4.md)。
>
> 條目由新到舊。`Deployed` 一則對應一次實際部署，寫在它所部署的變更條目上方。

## [Unreleased]

### Deployed（2026-08-30 22:57 CST，PR #59 → main）
🔴 **這是這一批唯一真的改變機器人定價行為的一次。**
- 部署後第一輪就照預期換單：取消 `5096500219`（年化 10.91%、已閒置 24.1 小時），
  **改掛 `5097273198` @ 年化 9.00%**（日利率 `0.00024657`）。
- **合約天期維持 2 天**（日誌：`利率 0.000247，天期 2 天`）。
- 日誌同時印出誠實度那一行：`已超出當初預估的四分之三分位，等待估計偏樂觀`。

### Changed（2026-08-30，PR #59，分支 `feature/split-assumed-hold-hours`）
- 🟢 **D1 收尾（D056）**：新增 `strategy.assumed_hold_hours`，**預設 12 小時**。
  `offer_period` 維持 2，只負責合約天期。
  **沒設新鍵就退回 `offer_period * 24`**——不設定的人行為完全不變。
- ⚠ **D055 建議的改法本身是錯的**：`offer_period` 身兼「合約條款」與「算式的 `P`」
  兩職，而前者是 `int(...)`、且 **Bitfinex 最短只接受 2 天**——
  「改成 0.5 天」會變成 `0` 並且掛不出去。**回測驗證了假設，沒有驗證改法。**
- **為什麼是 12**：實測平均 16.93h／中位數 11.61h，而回測跨 22 個起跑點顯示
  **8～20h 是一片高原**（差不到 0.2pp）。**高原的存在是敢動它的關鍵**
  ——`target_queue_usd` 那次沒有高原（D032）。
- 🔴 **保留**：這個 12 有一部分在補 `W` 的偏差（D047），**修了 `W` 要重調**
  ——寫進 `config.yaml` 的註解。退回只要把該鍵拿掉或設成 48。
- 測試 859 → **865**。

### Deployed（2026-08-30 13:4x CST，PR #58 → main）
- 部署成功。**行為零變更**——`offer_period` 一個字都沒動。

### Changed（2026-08-30，PR #58，分支 `feature/redo-d049-across-starts`）
- 🟢 **D1 的證據夠了（D055）**。D049 那兩條「不能改那個 48」的理由，
  **在跨起跑點重做之後都不成立**——它們是 D054 揭露的「相位運氣」造成的假象：
  - 切半：D049 說前半反過來 → **全段／前半／後半三段全部同向**（都贏約 1.0～1.1pp）
  - 細掃：D049 說是雜訊 → **從 48h 往下單調上升** 6.20→6.82→6.99→7.22→7.44→7.45
- 🟢 **而且有一片高原**：8／12／16／20h 分別 7.39／7.41／7.44／7.22%，
  **差不到 0.2 個百分點**。**這正是 `target_queue_usd` 失敗模式的反面**
  （那個數字沒有高原）。**實測持有的平均 16.93h 與中位數 11.61h 都落在高原裡。**
- 🟢 **第二條獨立證據**：不看任何訊號、單純把價位平移，**最佳點在 −10%**
  （+10% → 4.69%、0% → 6.20%、−10% → 7.17%、−30% → 6.53%），
  而換 `P` 讓選中價位下移約 **16%**——**兩條獨立路徑同一個量級**。
- 🔴 **收窄 D054 的推論**：「任何往下走的機制都賠錢」是**錯的**。
  輸的不是方向，是那兩個機制——它們**先燒掉一段等待才降**，降幅還由乾旱深度決定。
- **建議 `offer_period` 2 天 → 12 小時**，⚠ **未執行，等使用者拍板**（定價路徑）。
- 測試 851 → **859**。

### Deployed（2026-08-30 13:2x CST，PR #57 → main）
- 部署成功，機器人健康。**行為零變更**（新東西沒有被 `main.py` 匯入）。

### Added（2026-08-30，PR #57，分支 `feature/stale-signal`）
- **`stale_ratio()`**（D054）：「我已經等了常態間隔的幾倍」。
  5 段長等待 **5／5 發話（第 0～11h）**、10 段快速成交 **0／10 誤報**，
  而且**門檻 1.5×～4× 都是同一個結果**——不是挑出來的一個點，是一整片高原。
  對照組（現況用的候選價位）：5 段裡**只有 1 段發話，且在第 30h**。
- **`run_policy_across_starts()` ＋ `compare_across_starts()`**
  ——跨起跑點比較政策。`scripts/backtest_report.py` 加 `--signals`。
- 測試 844 → **851**。

### Changed（2026-08-30，PR #57，同分支）
- 🔴 **`stale_ratio` 拿來降價是賠錢的**：單跑一次 +0.70pp（9 種參數全贏），
  **跨 22 個起跑點平均 −1.97pp、勝率 7／22**。機制是「降價把較差的利率鎖住
  最多 48 小時，而省下來的只是一段等待」——**贏很多次、輸很大次**。
  ⚠ 只看勝率會得到相反的結論（`lookback=12` 贏 14／22，平均仍輸）。
- 🔴 **D049／D050 的比較方式被推翻**：同一個政策，**單一起跑點與跨起跑點平均
  的符號是相反的**。在只有十幾次循環的歷史上，改掛一次會把後面每次循環的
  進場時點整個推移。**此後比較政策一律用 `run_policy_across_starts()`。**
  ⚠ D049 的**方向**結論不受影響（換自變數不是換相位）；
  D050 的**結論**站得住但**理由要換掉**，索引列已加註。

### Deployed（2026-08-30 16:59 CST，PR #56 → main）
- **日結摘要在部署後第一輪就實際推送出去了**：日誌
  `已推送 2026-08-30 的日結收益摘要`，`bot_kv` 記下 `last_earnings_digest_date`。
- 場上那張單 `5096500219` **第六次原封不動**（閒置 18.14h、`hold_matched`）。

### Added（2026-08-30，PR #56，分支 `feature/daily-earnings-digest`）
- **日結收益摘要**（D053，**P2-4 至此全部完成**）。`CATEGORY_EARNINGS` 與 📊
  從 D029 就定義好，一直沒有東西可以放——因為 `earnings_daily` 一直是空的。
- **判準是資料不是時鐘**：`earnings_daily` 出現沒推過的日期就推。
  時鐘式的（「每天 10:00」）會在機器人剛好重啟時整天不推，
  也會在結息時間漂移時推到空摘要。**停機幾天回來只補最新的一則**，不連發。
- **新表 `bot_kv`** 記住推到哪一天。🔴 **不能往 `bot_state` 加欄位**——
  這個專案沒有 migration 機制，`CREATE TABLE IF NOT EXISTS` **對既有表新增欄位
  完全沒有效果，而且沒有任何錯誤訊息**（D026 那一族）。
- 測試 835 → **844**。

### Changed（2026-08-30，PR #56，同分支）
- 摘要裡的年化寫的是**「相對錢包餘額的年化」**，分母是帳本上的收盤餘額。
  🔴 **刻意不寫「實得年化」四個字**——那個詞在這個專案有明確定義（要扣掉沒借出去
  的時間），而這則訊息算不出來，寫了就會被當成實得年化在讀，
  **那條剛放下去的錨就又鬆了**。餘額取不到就整行略過，不印來歷不明的百分比。

### Deployed（2026-08-30 16:49 CST，PR #55 → main）
- **同步在部署後第一輪就跑起來了**：日誌 `帳本同步完成：27 列裡有 20 列利息，
  寫入 20 天、合計 1.40009027 USD`。`earnings_daily` 12 → **20 列**。
- 場上那張單 `5096500219` **第五次原封不動**（閒置 17.97h、`hold_matched`）。

### Added（2026-08-30，PR #55，分支 `feature/ledger-sync-in-loop`）
- **帳本同步接進主迴圈**（D052）：`engine.earnings_sync_hours`，預設 6，設 0 關掉。
  **跑在 `run_forever()` 的迴圈層，不在 `run_once()` 裡面**
  ——後者有五條提早 `raise SkipCycleError` 的路，正式環境幾乎每輪都走其中一條，
  放進去等於永遠不執行（**D038 踩過的同一課**）。
  **`run_once()` 本體一行未改**，定價／重掛／取消／下單四條路徑一行未碰。
- **吞掉所有 `Exception`**：報表不可以把一台在管真金的機器人弄停。
  也不累積 `consecutive_failures`（否則連三次就推「機器人可能壞了」）。
- 測試 827 → **835**（六條「壞掉不要爆」＋兩條「好的時候真的有寫」）。

### Fixed（2026-08-30，PR #55，同分支）
- ⚠ **寬鬆的 `except` 當場咬了一口**：實作時漏掉 `from core import earnings`，
  結果 **機器人照跑、日誌只有一行 warning、測試全綠**——`NameError` 被自己的
  `except Exception` 吞掉了。抓到它的是**兩條正向測試**，不是那六條防爆測試。
  🔴 **教訓寫進 D052：寬鬆的 `except` 一定要配一條證明 happy path 有效的測試**
  ——只測「壞掉不爆」的話，一個從來沒成功過的功能會完美通過所有測試。

### Deployed（2026-08-30 16:11 CST，PR #54 → main）
- 部署成功，**場上那張單 `5096500219` 第四次原封不動**（閒置 17.34h、`hold_matched`）。
- **`earnings_daily` 不再是空的**：12 列、合計 **0.75517665 USD**。

### Added（2026-08-30，PR #54，分支 `feature/ledger-earnings`）
- **P2-2：帳本接上了**（D051）。`get_funding_ledger()`／`_parse_ledger()`
  ＋ `core/earnings.py`（分類與每日彙總）＋ `scripts/sync_earnings.py`
  ＋ `Repository.set_daily_earning()`（覆蓋語意）。
  **預設只看不寫**，`--write` 才寫。**機器人行為零變更**（沒有被 `main.py` 匯入）。
- 測試 804 → **827**，含實打抄回來的四列真實帳本。

### Changed（2026-08-30，PR #54，同分支）
- 🔴 **前面每一個績效數字都被往下修**：帳本是這個專案第一條「交易所自己說的錢」，
  而錨放下去之後——`wait_report` **7.99%** → 回測 **6.54%** → **帳本 5.34%**。
  **三個算式各自都對，錯的是每一個都少扣一點東西，而少扣的方向全部相同。**
- **B6 的預測中了**（「ledger 是下一個回應解析的重災區」）：
  `[2]` 官方文件標成 placeholder **但實測是錢包名稱**、
  **數值全是字串**（D027 第四次現身）、**帳本裡混著轉帳與幣別兌換**。
  ⚠ **「加總」得 343.71（差八千倍）、「只取正數」得 528.01**——兩種偷懶法都不行，
  因為同一筆轉帳出現兩列、正負相反、掛在不同錢包。分類要同時看
  `description` ＋ `wallet`。

### Deployed（2026-08-30 15:02 CST，PR #53 → main）
- 部署成功，**場上那張單 `5096500219` 第三次原封不動**（閒置 16.20h、`hold_matched`）。

### Added（2026-08-30，PR #53，分支 `feature/repost-policy-simulation`）
- **重掛政策可以在模擬裡比了**（`RepostPolicy`，A2-b）：`never_repost`／
  `rate_tolerance`（**現況**）／`down_only`／`down_after_idle`／`follow_candidate`，
  以及一個「等待中途可以改掛」的等待模擬。`backtest_report.py` 加上 `--repost`。
  **機器人的重掛邏輯一行都沒有改。**
- 一條刻意的基準線保護：`repost_policy=None` 時**必須與 `simulate_fill()` 逐位元
  相同**，否則「加了政策之後變好了」會分不清是政策的功勞還是基準線被改掉了。
- 測試 791 → **804**。

### Changed（2026-08-30，PR #53，同分支）
- 🔴 **A2-b 的答案是否定的（D050）**：六種重掛政策的實得年化差不到
  **0.05 個百分點**，連重掛成本為零的上界對照也沒有贏。
  **而這不是因為沒有機會作用**——等待佔整段歷史 **30.1%**、最長一段 **38.5 小時**。
  成因是**候選價位是個落後訊號**：那段空掛裡它**前 32 小時完全沒動**，
  越過 2% 容差時等待已走完 83%（現在場上這段 16 小時，漂移 **0.00%**）。
  結構性原因是**定價用 168 小時的窗，反映不了十幾小時的乾旱**。
  **下一步不是挑門檻，是換訊號。**
- ⚠ **D047 補追記：它記的方向不是通則。** 第二段空掛的 `W` 走向相反
  （5.88 → 8.14，變難看）。機制是設限補值平均約 `k/2`，**小於典型等待時拉低、
  大於時拉高**——**「越等越有信心」只發生在乾旱的前半段**。
  🔴 **這讓問題更難處理不是更簡單**：模型最樂觀的時刻，正好是乾旱剛開始、
  **還來得及改掛**的那段。已加迴歸測試釘住兩個方向。

### Deployed（2026-08-30 14:37 CST，PR #52 → main `707c68f`）
- 部署成功，**場上那張單 `5096500219` 再一次原封不動**（閒置 15.77h、`hold_matched`）。
- 新工具在正式映像上實跑通過：`--validate-fill` **總量比 0.93×**、`--simulate` 五列都出得來。

### Added（2026-08-30，PR #52，分支 `feature/backtest-fill-simulation`）
- **M2 第 2 步：成交模擬**（`core/fill_simulation.py`，D049）。
  成交規則是「**第一根 `high >= 掛單利率` 的 K 線就是成交**」，時點取那根的中點
  （與 `estimate_wait()` 同一個約定）。**規則先被驗證才拿來用**：
  對 12 筆真實成交的**總量比 0.93×**（模擬 18.15h vs 實際 19.45h），
  而策略自己的 `estimate_wait()` 在同一批樣本上是 **4.25 倍高估**
  ——**模擬器準了一個數量級**，這才是它能當裁判的理由。
- **`run_policy()` 算整段歷史的時間加權實得年化**，`Σ(r × P) ÷ Σ(W + P)`，
  **空等與空轉都以 0% 計入分母**。`wait_report` 印的 7.99% 就是漏了這一塊才偏樂觀
  ——所以這裡的 6.5%～7.4% **不是變差，是分母變誠實了**。
- `scripts/backtest_report.py` 加上 `--validate-fill`／`--simulate`／`--hold-model`。
- 測試 756 → **791**（新增 23 項機制測試 ＋ 12 項迴歸釘子）。**機器人行為零變更。**

### Changed（2026-08-30，PR #52，同分支）
- **D1 從 `[~] 等 M2` 變成「有答案，但答案有三層」（D049）**：
  (1) ✅ 方向——假設 `P=48` 實得 **6.57%**、假設 16.93h 是 **7.26%**，
  四種「實際借多久」的假設下都一致；
  (2) 🔴 **跨時段站不住**——切一半，前半反過來（48h 7.21% vs 16.93h 7.05%）；
  (3) 🔴 **細掃是雜訊**——14h 最高但曲線不單調、高低差不到 0.5 個百分點，
  而樣本只有 16.8 天、十幾次循環。**挑最高那列就是 `target_queue_usd` 的死法。**
- 🔴 **多了一個更硬的「不要直接改那個 48」的理由**：**就算實際持有真的是 48h，
  假設 48h 仍然不是最好的**（7.27% vs 7.61%）。機制是**兩個錯誤部分互相抵銷**
  ——較小的假設 `P` 讓策略更怕等待，正好補償 `W` 被低估（D047）。
  **單獨改 48 會拆掉抵銷的一半，D047 與 D1 因此綁成同一件事。**

### Fixed（2026-08-30，PR #52，同分支）
- **持有模型是有狀態的，害回測跑兩次給不同答案**：第一版 `empirical_hold()`
  回傳帶 `state["index"]` 的閉包，同一個實例跑第二次會從序列中間接下去
  ——同一組設定先後跑出 **7.26% 與 6.70%**，**兩個數字看起來都很正常**。
  「依序循環而不是隨機抽」的重點本來就是**可重跑**，有狀態的實作正好毀掉它。
  改成把「第幾次循環」當參數傳進去（純函式），並加測試釘住。

### Deployed（2026-08-30 13:46 CST，PR #51 → main `3e874a1`）
- 部署成功，容器重啟後 **場上那張單 `5096500219` 原封不動**（同一個單號、
  同一個利率、`hold_matched`）——`bot_engine` 的取消只在「計畫不合」那條路上，
  沒有啟動時無條件取消，而候選利率與場上利率逐位元相同已連續 120+ 輪。
- 新工具在正式映像上實跑通過：`--verify` **26 列全數一致**。

### Added（2026-08-30，PR #51，分支 `feature/backtest-replay`）
- **M2 第 1 步：歷史重播器** —— `core/backtest.py`（`replay()`／`replay_at()`／
  `sweep_hold_hours()`）＋ `scripts/backtest_report.py`（`--verify`／`--sweep`／
  `--hold-hours`，唯讀開 DB）。餵歷史 K 線，回答「**當時這個策略會掛什麼價**」。
  **M2 完整要回答三件事，這一批只做第一件**；成交模擬與實質年化是第 2 步。
  三條刻意的界線寫在模組 docstring 裡：**重播呼叫策略本尊**（不是副本）、
  **重播點看不到未來**（切窗在 `replay()` 裡做，不靠呼叫端自律）、
  **唯一被轉的旋鈕是 `P` 且轉的是策略自己讀的那個屬性**。
- **迴歸釘子 11 項**（`tests/unit/test_backtest_regression.py`），資料是
  `market_candles` 倒出來的 **200 根真實 K 線**。期望值不是測試自己算的，
  是機器人寫進 DB 的那一列：`P=48` → 選中 **10.91%**／實質 **9.46%**／
  `W` 平均 **7.3273809523809526h**／候選 **132**（`pricing_decisions` 第 118 列）；
  `P=16.93` → **9.09%**（低 **1.82 個百分點**）；
  D047 的乾旱 → 窗往回滑 16h 是 **8.95%**，滑回來 **9.46%**，而選中價位一路沒變。
- 測試 726 → **756**。**機器人行為零變更**（新檔案沒有被 `main.py` 匯入）。

### Changed（2026-08-30，PR #51，同分支）
- 🔴 **M2 的驗收標準換掉了（D048）**：PLAN.md 原本寫「拿它跑 `orderbook_depth`，
  要能重現賣在區間底部」，而那條**跑不起來**——`orderbook_depth` 沒有
  `choose_rate()`（它吃簿子不吃 K 線），且那個結論是 08-16～17 的、
  `market_snapshots` **08-23T17:21 才寫第一列**。
  **驗收標準比它依賴的那張表早寫了六天，之後十三天沒有人回頭確認它可不可跑**
  ——D026「靜默失效」在文件層的第一次現身。
  換上 `--verify`（逐點對照 `pricing_decisions` 的正式紀錄），**08-30 首跑 25/25**。
  ⚠ **換上的這條比原本那條弱**：抓不到「策略本身錯了」（重播與正式環境跑同一條
  路徑，會一起錯）。這句話印在報告輸出裡，不只寫在 DECISIONS。

### Added（2026-08-30，直接推 main `1051544`，純文件）
- **D047：乾旱期會讓等待估計自己變好看**。那張 10.91% 掛 13 小時沒成交，
  而模型算出的實質年化 15 個小時區間**單調上升 9.03% → 9.46%**。
  成因是 `estimate_wait()` 對右設限的起點記「至少等到窗尾」，
  **離窗尾越近的起點這個數字越小**——乾旱靠自己的新鮮度替自己脫罪。
  它形成一個**對抗降價的正回饋圈**，也解釋了 `repost_comparisons` 全是
  `hold_matched`（M1-c 的驗收條件 2 不是「還沒被觸發」，是被擋住）。
  **刻意先不修**：換一個補值就是換一個拍出來的常數（D032 的死法），是 M2 的題目。

### Added（2026-08-29，PR #49，分支 `feature/report-statistics`）
- **`wait_report` 印出三個統計量的對照**：`estimate_wait()` 一次算出
  平均／中位數／四分之三、`offer_wait_forecasts` 三個都存了，但策略只用
  `mean`、報告也只印 `mean`——**握著三份證據只看了一份，看了兩週**。
  八個樣本並排後 **`median` 8/8 全數較準**（總量比 1.67× 對 4.25×、
  最差 9.0× 對 108.6×）。新增 `WaitSpell.factor_for()`、
  `WaitSummary.overall_factor_for()` ／ `median_factor_for()` ／ `factor_range_for()`。
  ⚠ **只並排，不選**——換統計量會把選中價位移動約 2 個百分點，那是 M2 的題目（D045）。

### Fixed（2026-08-29，PR #49，同分支）
- **`hold_report` 的「還比不出來」會暗示一件不會發生的事**：分組門檻取已結束
  部位的利率中位數 = 9.11%，而 9.11% 同時是眾數（17 筆裡 10 筆），`<` 把整叢
  掃進昂貴組。原本只說「要兩組各 3 筆才算得準」，**讀起來像「再等幾筆就會好」**。
  新增 `RateSplit.degenerate` 講出「這個分界不會自己好」，
  以及 `displayed_at_pivot` 區分「日利率相同」與「年化印起來相同」
  （`0.00024972` 與 `0.00024971` 都印成 9.11%，但 `<` 只看日利率）。

### Changed（2026-08-29，PR #48，分支 `feature/calibrate-funding-credits`）
- **B6：`funding_credits` 補打，credits 那一半不再是猜的**（D027 追記）。
  趁部位 `464812689` 生息中打了一次唯讀查詢——B6 當初抄不到就是因為
  打的當下沒有部位。三件替身不知道的事：**22 欄**、
  `[21]=POSITION_PAIR` 是 `'tBTCUSD'`、
  **`[8]=RATE_TYPE` 真實是 `'FIXED'` 而替身留 `None`**（同一個坑的第三次現身）。
  **這是 B6 第一次核對到整條鏈路**：原始列 → `_parse_positions()` → DB 四欄逐一對上。
  🟡 `funding_loans` 仍抄不到（要「已借走但還沒用掉」的時刻）。


### Fixed（2026-08-29，分支 `feature/calibrate-test-doubles`）
- **等待報告把「還在計時」誤述成「被下一張單取代」**：`describe_spell()` 對所有
  右設限的期間都寫同一句，而那是一個沒有查證過的原因。08-29 那筆年化 10.95%
  的單還躺在場上、才等不到兩小時，報告卻說它被取代了——**偏偏它是 D045 缺了
  兩週的帶外樣本**。把「還在計時」講成「已經結束」，等於把一個會繼續長的下界
  固定成量測值（D026 靜默失效的同一族）。
  新增 `WaitSpell.replaced` 與 `WaitSummary.ongoing`，敘述分兩種講法。

### Changed（2026-08-29，同分支）
- **B6：七支測試替身改用實打抄回來的形狀**（D027 追記）。三個公開端點 ＋
  四個唯讀私有端點各打一次，**七支裡四支跟替身不一樣**：
  ticker 是 17 欄不是 4 欄且 [13][14] 真的是 `None`；
  **funding offer 陣列是 21 欄而替身寫 16**，[10]=STATUS 真實是 `'ACTIVE'`；
  `fetch_balance` 指定了 `type='funding'`，`info` 裡照樣混著別的錢包；
  可用餘額是八位小數。新增五組 `REAL_*` 常數，各配一條直接吃未加工真實列的測試。
- **D027 補第 4 點**：註解裡寫下的真實形狀，要有一條測試直接吃未加工的真實列
  ——`_parse_offers()` 的 docstring 從 08-16 起就寫著「21 欄」，
  **程式碼知道真相卻沒有流進替身，而兩邊沒有人去對過**。
- 測試 688 → **699**（`-m "not live"`）。

### Docs（2026-08-29，同分支）
- **D045 追記：等待高估倍數不是常數，是駝峰，而七個校準樣本全落在峰頂**
  （9.14～9.55% 是 4.2×、10.95% 只有 1.4×、11.3% 回到 1.0×）。
  **這推翻了 D045 本文那句推論的前提**——「W 被系統性放大 → 選出的價位會偏低」
  只有在偏誤均勻時才成立，修正後價位往哪邊移方向相反。進 M2 題目清單。
- 同一節給出 **A2-b 的第一個可算門檻候選**：躺超過 12.6～18.9 小時就輸給簿子底部。
- **M1-c 於 08-29 15:51 第一次被觸發**，`repost_comparisons` 0 → 9 列；
  條件 1 與 3 全數驗過，**條件 2 仍測不到**（9 列全是 `hold_matched`）。

### Added（2026-08-28，分支 `feature/record-repost-counterfactual`，PR #46）
- **M1-c 重掛反事實落地**（D046 第一段）：新表 `repost_comparisons`，
  場上有掛單的每一輪落一列「保住 vs 改掛」的並排比較。**M1 到這裡完成。**
  - **補的是 D046 查出來的兩個缺口**：往上調價**從頭到尾沒有判準**
    （守門檻第一行就 `if candidate.rate >= live_rate: return None`），
    而機會成本**每十分鐘都在算、算完就丟**（`_log_idle_time()` 回傳 `None`）
  - **新增 `ExpectedValueStrategy.evaluate_rate()`**：對**任意**利率用本輪那一窗
    重新評估。場上那張的利率通常**不在候選集裡**（`plan.rate` 經過成交價下限與
    spread 加工），而兩邊必須用**同一把尺**算，比出來的差額才是市場的差、不是尺的差
  - **`_log_idle_time()` 改為回傳算出來的值**，不再算完就丟。
    ⚠ **有出口不等於接上決策**：這一輪只流向反事實資料——閒置久**不是**漲價的理由
  - **「行為零變化」靠位置保證，不靠測試**：兩個判斷的求值提到 `if` 之前，
    順序與短路完全不變（`matched` 為真時守門檻依然不被呼叫，連棄權日誌的行數都一樣），
    於是 `action` 是**讀出來的，不是預測的**。手法同 M1-b 緊貼日誌那一行（D043）
  - **驗證**：舊版與新版引擎在**六個情境**下比對（場上沒單／維持不動／往上調價重掛／
    往下重掛不划算／餘額不足／拿不到簿子），**掛出去的單、取消次數、離開方式完全相同**
    ——D046 驗收條件 3 的直接證據，不只是回歸網。
    另在**正式 DB 副本**上跑過遷移：新表建起來、9 張舊表列數一格沒動
  - 測試 650 → **675**（單元＋功能；整合 26 項另計）。三條 `action` 各有一條測試
  - **成本實測**（寫 500 列再 VACUUM）：一列 **418 位元組**含索引，最壞一年 21 MB
    ——比 `pricing_decisions` 的 4,137 B 小一個數量級（沒有陣列欄位）
  - **不改任何行為**：沒加漲價門檻、沒動 2% 容差、沒動任何策略參數
  - 🔴 **2026-08-29 追記：驗收條件 1 的正向那半與條件 2「測不到」**——部署後
    228 輪，**場上有單的輪次是 0**（三張單全在一個巡檢週期內成交）。
    `repost_comparisons` 至今 **0 列，而那是正確的**。見 D046「驗收狀況」

### Planned（2026-08-26，無程式碼變更）
- **D046 重掛政策**：使用者提案「每十分鐘比一次，該調高就砍掉重掛」。
  查證後**三個事實支持它、一個要修正它**
  - ✅ 每十分鐘確實已經在比了；
    🔴 **但往上調價完全沒有判準**——只管往下的那道守門檻第一行就
    `if candidate.rate >= live_rate: return None`；
    🔴 **機會成本每輪都在算但算完就丟**（`_log_idle_time()` 回傳 `None`）
  - ⚠ **要修正的那一點**：「等太久」是成交比預期慢的證據，**它支持降價不支持漲價**。
    支持漲價的訊號是市場整體成交價往上走。兩者混在一起會在市場走弱時去漲價
  - **排成兩段**：**M1-c 重掛反事實落地**（只落資料、不改行為，驗收第 3 條是
    「行為零變化」）→ **A2-b 重掛政策**（門檻等 M2 回測回答）
  - **本次不動任何程式**：沒有反事實資料就只能拍門檻，那是 `target_queue_usd` 的死法


### Added（2026-08-26，分支 `feature/measure-wait-accuracy`）
- **D045 等待估計校準報告**：`core/wait_time.py` ＋ `scripts/wait_report.py`，
  與 D040 的 `hold_time` ＋ `hold_report` **成對**——算式 `r × P ÷ (W + P)` 裡，
  一個量 `P`（實際借多久），一個量 `W`（實際等多久）。**算式兩項到這裡都有校準報告了**
  - **回答的是「為什麼老是掛同一個利率」**：`W` 正是懲罰高利率的那一項，
    而實測**五筆全部高估**、總預估 ÷ 總實際 = **3.9 倍**（逐筆 1.6×～29.0×）
  - **連續、同利率、中間沒成交過的重掛合併成一段**。
    **第二條規則是實跑正式資料才發現要加的**：只有第一條的話，三張各自成交過的
    9.50% 會被併成一段，**五個校準樣本靜靜地變成兩個**——D026「靜默失效」的又一次
  - **沒成交就被取代的掛單當右設限**分開報（最長那一段 34.20h 單獨印出來）；
    D038 之前的掛單印「沒有留下預估值」而不是填 0；
    `spread_count > 1` 時期的同輪兄弟單不計入右設限
  - **倍數用總量比，且逐筆倍數與利率涵蓋帶一起印**——校準樣本全落在年化
    9.11%～9.50%，**帶外不能外推**
  - `core/hold_time.py` 的 `_parse` 改名為公開的 `parse_moment`（兩支模組共用同一條規則）
  - 測試 660 → **676 項**。依 D025 的教訓，**兩處實作各自反轉過一次確認新測試會紅**
  - **不改任何策略參數**：`ev_window_hours`、`hold_hours`、`minimum_rate` 一個都沒動


### Verified（2026-08-26 00:14）
- **D043 決策落地：五條驗收條件全數通過，M1 完成**。部位 `464372858` 於 00:14:07
  收回（到期 08-25 23:50，逾期 24 分），`pricing_decisions` 第一列就落在那一輪
  ——逐條實測見 D043 的「驗收結果」一節
  - `snapshot_id` 與 `decided_at` 對得回同一輪（字串完全相同）、
    `chosen_rate` 與日誌那一行的五個數字全部對得上、
    **資金 00:30:51 借出後又跑 127 輪仍是 2 列**（D041 的保護在 DB 側被證實）
- **D044／B5 仍未觸發，維持 🟡**：最可能觸發的一刻（00:14 重新掛單）
  當時餘額 344.72436、掛單 344.72，**剛好沒有超**。「沒發生」不等於通過

### Measured（2026-08-26，無程式碼變更）
- **`offer_wait_forecasts` 存了兩週，第一次拿去對答案：五筆全部高估，平均 3.9 倍**
  （預估 7.09h 對實際 1.80h，逐筆 1.6×～28.7×）。完整敘述見 **D045**
  - 起因是使用者問「為什麼掛了幾次都是同樣的利率」。查出**兩個答案**：
    候選集是 168 小時窗內的 K 線 high（一根 K 待滿 7 天，巡檢又比 K 線密），
    以及——**期望值曲線是平的**：8%～11.35% 的實質年化全在 6.9%～7.9% 之間，
    第 1 名贏第 2 名 **0.0005 個百分點**
  - **不改任何參數**：五筆樣本全落在模型自己選出來的 9.11%～9.50% 窄帶，
    唯一的反例正好在高價端（08-19 掛 9.78%，在場 **34.2h 沒成交**）
- **`hold_report.py` 重跑**：已結束 7 筆、平均完成率 **43.6% → 51.8%**。
  D1 的分組門檻跨過，但便宜組三筆是 1.84／45.08／48.39h，離散到中位數沒有代表性
- **`pricing_decisions` 增長速率**：60 小時 2 列（資金 98% 時間鎖在部位裡），一年約 500 列


### Deployed（2026-08-24 23:29，main `7f7a15c`）
- **PR #44（B5）已合併並完成部署**。23:27:54 合併 → 23:29:14 部署流程結束，
  測試 660 項、整合測試、容器化部署三段全綠
- **趁的是同一個零成本視窗**：資金全鎖在部位 `464372858` 裡、每一輪都走餘額守門檻，
  重啟不會打斷任何掛單。PR #43 部署（22:48）之後 41 分鐘

### Fixed（2026-08-24，分支 `fix/b5-insufficient-balance-classification`）
- **B5：餘額不足不再被當成認證失敗而停機**（**完整敘述與驗收條件見 D044**，
  起因是 D025 的附帶發現）。
  ccxt 把 Bitfinex 的
  `Invalid offer: not enough USD balance available in deposit wallet`
  歸類成 `AuthenticationError`，於是它走 `FatalError` 那一支
  ——**機器人永久停止**，日誌還寫「建立放貸掛單認證失敗」，把人引去查 API 金鑰
  - **分類改成看訊息、不看型別**：訊息含 `not enough` / `insufficient` 時歸為
    `SkipCycleError`（本輪略過、下一輪重算金額），日誌寫「融資錢包餘額不足」
  - **刻意不單獨認 `balance`**：「查餘額失敗」之類的訊息也含這個字，
    而把金鑰問題誤判成「本輪略過」會讓機器人拿無效金鑰空轉一整天
    ——**這個方向的誤判比反過來貴得多**，所以判斷條件收窄而不是放寬
  - `AuthenticationError` 與 `ExchangeError` **合併成一個 except**：
    ccxt 下次改版丟哪一種沒有人保證，型別在這裡本來就不可信
  - **回應信封那條路（`status != "SUCCESS"`）也問同一個問題**，
    否則回應形式一改就漏掉一半
  - **引擎照樣落一筆 `failed` 留痕，但不推 LINE**：只改 client 的話這種拒單會從
    「有落帳＋有通知」掉成只剩一行日誌；而它可能連續好幾輪發生，
    額度是每月 200 則（D024）
  - **為什麼趕在今天**：08-25 23:50 部位到期、資金收回重新掛單，
    正是最可能踩到金額問題的時候，而半夜沒有人看著
  - 測試 650 → **660 項**。依 D025 的教訓，兩處實作**各自反轉過一次確認新測試會紅**
    （判斷函式永遠回 `False` → 4 條紅；拿掉引擎留痕分支 → 1 條紅）


### Deployed（2026-08-24 22:48，main `59c087f`）
- **PR #43（D043，M1-b 決策落地）已合併並完成部署**。22:47 合併 →
  **22:48:28 容器重啟**、`healthy`、0 ERROR 0 WARNING
- **新表 `pricing_decisions` 已自動建好**（含 `idx_pricing_decisions_decided_at` 索引），
  22 個欄位全部到位——**不需要遷移腳本**這件事在正式環境確認了第三次（D038、D042、D043）
- ⚠ **`pricing_decisions` 目前 0 列，而這是通過，不是失敗**。
  部位 `464372858` 要到 **2026-08-25 23:50** 才到期，在那之前每一輪都走餘額守門檻
  （22:48:41 第一輪就是「可用餘額 0.08 USD 低於下限 150.00 USD」），
  `choose_rate()` 根本跑不到，**沒有評估過的輪次一列都不該寫**
- **這一次不假裝「部署完成 ＝ 驗收通過」**：五條驗收條件在合併前就寫死在 D043 最後一節
  （照 D041 那條該推廣的做法），要等部位收回那一輪才對得起來


### Verified（2026-08-24）—— 不是變更，是兩條舊決策第一次被真的執行到
- **D040 與 D041 於 2026-08-23 23:04 在正式環境驗收通過**。部位 `464242253` 收回，
  打開了等了三天的窗口
  - **D040**：`_log_hold_times()` 第一次跑到，印出「實際借出 **48.56 小時**，
    佔預定 48 小時的 **101%**——借到期」。完成率統計 32.1% → **43.6%**、
    中位數 6.87h → **13.92h**（六筆已結束）。**模型仍假設 100%，分子被高估沒有被推翻**
  - **D041**：資金回來 → 評估一輪 → 成交 → 掉回門檻，之後 **08-24 全天 130 輪
    每一輪都沒有重播定價決策**。要求的是「下一輪」，拿到的是「130 輪」
  - **關鍵在於這次跟上一次長得不一樣**：`last_evaluation` **先被填滿過**，
    然後才掉回門檻以下。「還沒有機會犯錯」與「有機會犯錯而沒有犯」，
    中間隔的正好是一次驗收
  - 連續五次「部署完成 ≠ 新程式碼被執行過」的紀錄，在 M1-a（08-23）
    與這裡（08-24）連續兩次兌現後結束
- **順帶第一次去算 M1-a 落地的 K 線**（TASKS.md 標著「還沒有人去算它們」）：
  年化 9.11% 這個價位全期命中率 38.8%、**最近 72 小時只剩 9.7%**；
  08-22 之後 9.50% 一次都碰不到。**這組數字是上界不是實況**
  （K 線 high 只說有人以那個價位成交，不說排在我們前面的幾百萬 USD 清光了沒），
  已在文件標明不可以拿它去動 `ev_window_hours`——那是 M2 的題目（D036）

### Added（2026-08-24，分支 `feature/persist-pricing-decision`）
- **決策落地 M1-b**（D043）——**DB 第一次存下「我們怎麼想」**。
  在這批之前，`market_snapshots` 存著市場長怎樣，而「當時為什麼選那個價位」
  只存在該輪的記憶體裡
  - **新表 `pricing_decisions`，每「真的評估過一輪」一列**：選中的價位與它的等待分佈、
    對照組（等最短的那個候選）、候選集的**價位與實質年化兩排**、當時的窗長與最新那根
    K 的 `mts`、以及那個已知錯的 `hold_hours_assumed`（存的是**假設**不是事實——
    M2 換掉它之後，舊決策才比得了），`snapshot_id` 接回同一輪的快照
  - **寫入點緊貼 `_log_pricing_rationale()`**：兩者讀同一份 `last_evaluation`，
    於是**日誌那一行就是 DB 那一列的鄰行**。D041 擋著 M1-b 的理由是
    「DB 裡多一列假資料沒有鄰行會反駁」——**這個保護是靠位置接回來的，不是靠承諾**
  - **沒評估過的輪次一列都不寫**。08-24 那 130 輪（資金全借出、餘額守門檻擋在
    `choose_rate()` 之前）在這張表裡就該是 0 列——寫一列全是 NULL 的決策，
    會讓「這段期間評估過幾次」從 0 變成 130
  - **`last_window` 是新加的第三個「本輪狀態」，放進同一個重置點**。
    每多一個都是會跨輪殘留的成員（D041 已經有兩個），漏掉的那一個不會有人發現
  - **候選集只留兩排，成本是量出來的**：完整評估 17 KB、陣列的陣列 5.3 KB、
    兩排 **2.6 KB**。留 `effective` 是因為它就是排序依據——有它才答得出
    「為什麼是這個價位」（D3）。⚠ **其餘候選的等待分佈沒有存，而且重算不保證重現**
    （最新那根 K 事後被 UPSERT 過），表註解已標明不可以拿重算的東西冒充當時的決策
  - **整列實測 4,137 位元組**（寫 200 列再 VACUUM 量檔案增長）：一年約 50 MB，
    **最壞情況（每輪都評估）207 MB**——那是「市場一直很好、錢一直掛不出去」的日子，
    正好也是最想回頭看的日子
  - **不需要遷移腳本**：新表在啟動時由 `CREATE TABLE IF NOT EXISTS` 自動建好，
    與 D042／D038 走同一條路。已拿正式 DB 副本確認
  - 測試 618 → **650 項**（單元＋功能 624、整合 26）。對照實驗兩組都立刻變紅：
    拿掉 `last_window` 的跨輪重置 → 1 紅；讓沒評估過的輪次也寫一列 → 3 個反向斷言同時紅

### Deployed（2026-08-23 17:20，main `76fe844`）
- **PR #41（D042，M1-a ＋ C2）已合併並完成部署**。17:19:41 合併 →
  **17:20:53 容器重啟**、`healthy`
- ✅ **新程式碼在第一輪就被執行到了**——連續四次「部署完成 ≠ 新程式碼被執行過」
  之後的第一次立刻兌現。17:21:01 的第一輪就寫進 `market_snapshots` 1 列、
  `market_candles` 240 根，**沒有任何一個欄位是 NULL**
- **正式環境第一列的內容**（本身就是這批改動的驗收）：

  | 欄位 | 值 |
  |---|---|
  | 簿子 | 250 檔，年化 5.47%～**8.33%**，可見總額 10,917,130 USD，**`truncated` = 是** |
  | 成交 | 10,000 筆，`span_minutes` = **229.9** |
  | 成交加權中位數 | 年化 **7.13%**（每筆一票版本 7.53%） |
  | 2 天期加權中位數 | 年化 **5.94%**，而 FRR 是 **11.38%** |
  | K 線 | 240 根（**只有 2 天期一種**，`p{period}` 有生效），窗內 high 中位數年化 8.70%、最高 11.85% |

  **`truncated` 第一輪就標成「是」**，而 K 線窗內最高年化 11.85% 遠在可見簿子的
  8.33% 之上——A2／A3 那個「候選價位在簿子之外」的場景，現在有欄位記著了
- ⚠ **D040 與 D041 的正式環境驗收仍未發生**（與本批無關，兩者都在等部位 `464242253` 收回）
  → **2026-08-23 23:04 部位收回，兩者雙雙驗收通過**，見下方 2026-08-24 的條目

### Added（2026-08-23，分支 `feature/market-snapshot-landing`）
- **市場資料落地 M1-a**（D042）——**DB 第一次存下市場長什麼樣**。
  在這批之前有掛單、部位、收益、狀態、等待預估五張表，
  **沒有任何一張存過市場**，每次分析都得重抓即時資料、用完就丟
  - **`market_snapshots`（每輪一列）＋ `market_candles`（一根 K 一列）兩張表，
    不是 TASKS 原本寫的一張**。K 線每小時才換一根、巡檢 600 秒一輪，
    塞同一張表等於一天寫 34,560 列去講 24 根 K 的事。
    UPSERT 的**邊界含等號**是刻意的：最新那根還在成形中，少了等號每根 K 都會被
    凍結在剛出生那一刻的樣子，**而 `high` 正是這個策略唯一在意的欄位**（D035）
  - **這一批只存觀測，不存決策**。`core/market_snapshot.py` 的模組說明第一句就是
    「刻意不認識任何策略物件」——**界線要看得見，否則下次順手就越過去了**。
    M1-b（本輪選中的價位與候選集）等 D041 在正式環境驗收過才進來：
    日誌印錯還有鄰行可以拆穿，**DB 裡多一列假資料沒有鄰行會反駁**
  - **落地點在所有提早 `raise SkipCycleError` 的出口之前**——那些出口正好是
    「市場走弱、單子空掛」的輪次。這是 D038 那一課的重演，
    **差別在於日誌漏印還看得出來，DB 漏一列事後完全無感**。
    唯一例外：FRR 無效那一輪在抓資料前就離開，確實什麼都沒觀測到，
    **寫一列空的比不寫更糟**
  - **有損的地方都有欄位承認**（D039 從日誌延伸到資料表）：`book_truncated`、
    `trade_span_minutes`、沒觀測到的欄位留 NULL 而不是 0
    ——用 0 填會讓一段沒有資料的期間被讀成一段市場死掉的期間
  - **不需要遷移腳本**：`_create_schema()` 每次啟動都跑 `CREATE TABLE IF NOT EXISTS`，
    與 `offer_wait_forecasts`（D038）當初走同一條路。正式環境已確認自動建好

### Fixed（2026-08-23，同分支）
- **C2：`docs_only` 只認 `.project-docs/`，根目錄的文件檔不算**——改為文件白名單
  （`.project-docs/`、`README.md`、`LICENSE`、`.gitignore`、`.gitattributes`），
  **fail-open 維持不變**。這是它第二次現身，兩次都自帶對照組：

  | 推送 | 內容 | 結果 |
  |---|---|---|
  | PR-A `0dcc6e7` | `.project-docs/` 五份 ＋ **`README.md`** | **15:19 容器重啟** |
  | PR-B `e8694ec` | 全部落在 `.project-docs/` | 沒重啟 |

  唯一的變數是根目錄那一個檔案，**所以要修的是涵蓋範圍，不是規則本身**

### Notes（2026-08-23，同分支）
- 測試 587 → **618 項**（新增 31：單元 14 ＋ 資料層 11 ＋ 功能 6）。
  功能測試專門守「**哪些輪次也要留下紀錄**」：掛單成功的一輪、策略不掛單的一輪、
  維持場上既有掛單的一輪、FRR 無效那一輪（不寫列）、K 線列數不跟著輪數長、
  落地失敗不拖垮巡檢
- **成本是量出來的，不是估的**：拿真實公開 API 回應跑過完整落地，
  一列約 **1.2 KB**、一天約 190 KB、一年約 68 MB。
  TASKS.md 原本寫「每輪幾百位元組」，那是還沒把簿子的累積曲線算進去時的估計，
  **兩處程式註解都改成實測值**——一個估計數字留在註解裡，下一個人會拿它當事實
- **驗證 C2 時差點修錯東西**：本機的 `grep` 是包裝 **ugrep 7.8.4** 的 shell function，
  而 ugrep 的 `-q` 會忽略 `-v`，於是「文件＋程式碼」這個案例在本機測出
  「跳過部署」這個**假紅燈**。改用 `/usr/bin/grep`（GNU grep 3.11，也就是
  `ubuntu-latest` 實際跑的那個）重驗，七個案例全部符合預期。
  **用一個沒查過的工具去驗證，得到的紅燈不比綠燈可信**
  ——與 D038 記的「從文件描述推論機制，而沒去讀實作本體」是同一族


### Deployed（2026-08-23 12:20，main `d81121a`）
- **PR #37（D041）已合併並完成部署**。12:19:26 合併 → **12:20:44 容器重啟**、
  `healthy`、`NRestarts=0`，至 14:36 共 14 輪 **0 ERROR 0 WARNING**
- ⚠ **新程式碼仍未被執行到，連續第四次**：部署後 14 輪全部走「可用餘額 0.17 USD
  低於下限 150.00 USD」那個出口，`choose_rate()` 一次都沒跑到
  （日誌裡「期望值定價」出現 **0 次**），於是 `last_evaluation` 的重置
  **從來沒有東西可以重置**。
  **而 D041 的反證正是「新行程的清單本來就是空的」——「那一行沒印出來」
  在修正前後長得一模一樣，證明不了任何事。**
  第一次真正的驗收要等：資金回來 → 策略真的評估過一輪 → 下一輪又掉回門檻以下

### Fixed（2026-08-23，分支 `fix/stale-cross-round-state`）
- **跨輪殘留的狀態被當成本輪的事實報出去**（D041）——兩個成員，同一個形狀，
  D026 靜默失效家族的第七、第八次現身。**它們都不是「忘了講」，是「講了一件沒發生的事」**
  - **`last_evaluation`（`strategies/expected_value.py`）**：`build_offer_plan()` 有六個
    出口會在 `choose_rate()` 之前就 return，而評估結果**只在 `choose_rate()` 內重置**。
    現場代價：**08-21 22:34 → 08-22 19:22 連續 21 小時、87 輪以上**印出位元組完全相同
    的一行定價決策，而同一輪的鄰行寫著「可用餘額 0.01 USD 低於下限 150.00 USD」。
    重置點移到 `build_offer_plan()` 開頭、與 `last_skip_reason` 並排，
    **刻意不在那六個出口各補一次**——那正是這個 bug 當初的成因，
    漏掉任何一條就再犯，而漏掉的那條不會有人發現
  - **`_offers_live`（`core/bot_engine.py`）**：只跟著「我們做了什麼」走，
    而新策略下最常走的是第三條路——**什麼都不做**（08-21 那 36 小時裡 123 輪
    價位一次都沒變）。新增 `_note_offers_unchanged()`，在五個「什麼都沒動」的出口
    照實登記場上狀態，**不推播**——什麼都沒發生，推了就是製造雜訊
- **FRR 讀不到時會無條件推一則「掛單已不在場上」**——但真實流程裡取消發生在
  FRR 檢查**之後**，那一刻單還好端端掛在場上。改成先看一眼場上再決定要不要說。
  **這則比 D2 嚴重：D2 是把真事講錯，這是把假事講成真的**
- **TASKS.md D2 結案，但修法不是換句話說**：原以為要把「啟動後首輪掛單已送出」
  改成「掛單已重新上線」，實際跑過才發現通知推的是**狀態轉換**（D029），
  而場上本來有我們的單、重掛之後還是有——**沒有轉換，所以根本不該送**。
  修正後那一輪安靜通過。**訊息是假的，額度是真的**（每月 200 則，D024）

### Notes（2026-08-23）
- 測試 575 → **587 項**（單元＋功能 561、整合 26）。對照實驗兩次（分別移除兩處修正，
  11 項變紅）、每組各留一項**對照組**確認不是把功能整個關掉、反向斷言釘住
  「不可以印出上一輪那一行」、真實市場資料端到端重演 08-21 22:34 的處境
- **為什麼 575 項測試一項都沒抓到——兩個都是 D027**（測試替身比真實世界乾淨）：
  `test_沒評估過就不硬掰` 從一個**全新的策略物件**出發，而真實運作裡策略物件活得跟
  行程一樣久，**bug 只在第二次呼叫才現形**；`FakeClient.create_loan_offer()` 不把掛出去
  的單放回 `active_offers`，所以替身世界裡「掛單之後場上有單」從來不成立，
  五個漏掉的出口一個都走不到。**新測試一律跑兩輪**，並手動把單放回場上
- **全文件對齊盤查**：六份文件逐項對照程式碼、`config.yaml`、CI workflow、systemd 單元
  與 git 狀態，找到五個落差，**全部是文件落後於程式，沒有一項反向**。
  其中本檔案是唯一有系統性缺漏的一份——D039／D4／D041 三筆已合併的變更都沒進來
  （下面兩則補記即是）。成因是**一件事做完的當下只更新了「最近在看的那份文件」**

### Deployed（2026-08-22 19:22，main `a349a8a`）
- **PR #36（D040）已合併並完成部署**。19:21:18 合併 → 19:22:32 容器重啟，
  連續 16 小時 `healthy`、**0 ERROR 0 WARNING**
- ⚠ **新程式碼未被執行到**：`_log_hold_times()` 要等部位收回，
  而部位 `464242253` 一直開著。**連續第三次「部署完成 ≠ 新程式碼被執行過」**

### Added（2026-08-22，分支 `feature/measure-actual-hold-time`）
- **實際持有時間量測**（D040）——TASKS.md **D1「可以先做的那一半」**。
  材料從 2026-08-16 起就存在 `funding_positions` 的 `opened_at` / `closed_at` 裡，
  只是從來沒有人算過
  - **`core/hold_time.py`**（新）：右設限（仍在借出中）分開報且敘述改口說「至少」、
    偵測延遲與近似起算時間跟數字並排、完成率取代「借滿」的二分類
  - **`scripts/hold_report.py`**（新）：彙總報告，唯讀開 DB，隨時可跑
  - **`core/bot_engine.py`**：部位收回的當下就在日誌講出「實際借了多久、佔預定幾成、
    是提前還款還是借到期」
  - **`db/repository.py`**：新增 `all_positions()`，**刻意不濾掉還開著的部位**
    ——右設限樣本要看得見才算得出蓋掉多少
- **量測結果**：已結束五筆的中位數 **6.87h**、**平均完成率 32.1%**，
  而 `expected_value.py` 假設 **48h**。唯一借好借滿的是最便宜的那筆 5.47%

### Fixed（2026-08-22，同分支）
- **`sync_positions()` 回傳的 `closed` 少了 `closed_at`**：回傳的 dict 是 UPDATE
  **之前**查出來的，於是「剛收回」與「還開著」長得一模一樣。有東西去讀那個回傳值
  之後，**每一筆還款的當下都會被講成「至少借了 N 小時（仍在生息中）」**
  ——講的是還款，話卻說成還在生息。以對照實驗驗收（拿掉修正兩個測試立刻變紅）
- **報告差點用一個虛構的數字宣告假設成立**：第一版對「越貴借越短」做利率分組，
  便宜組只有 1.84h 與 45.08h 兩筆（差 25 倍），`statistics.median` 插值給出
  **23.46h**，報告據此印出「差距 16.59h，方向符合」。改為少於 3 筆不報中位數、
  直接攤開原始值，兩組任一組不足就說「還比不出來」

### Unchanged on purpose（2026-08-22）
- **`expected_value.py` 的 `hold_hours = offer_period * 24` 沒有改**。要換成什麼
  本身就是策略問題（中位數？依利率分層？期望持有時間？），得在 M2 回測工具上
  跑過才知道——**先改參數再建量測正是 D036 記下的錯誤**。那一行旁邊留了註解
  說明落差並指向 `scripts/hold_report.py`

### Notes（2026-08-22，08-21 起的交易盤點）
- **上線以來最好的一段**：36.1 小時走完兩個完整週期，資金使用率 **88.8%**
  （前一段 57.9%）、期間實質年化 **8.44%**（前一段 3.70%），0 ERROR。
  主因是「還款後 5～6 秒就重掛」，全段閒置只剩 4.03 小時
- **PR #34／#35 的新程式碼終於被執行到**，D039 的「至少」語氣與 D4 點名的對象
  都經實錄證實正確
- **D038 的校準拿到第二、三組資料**：預估中位數 3.5h vs 實際 3.93h／0.70h／3.33h，
  無系統性偏差
- **D035 最強的一次證據**：123 輪選中的價位一次都沒變（全部年化 9.50%），
  而同期市場常態價在 5.47%～9.49% 間震盪、平均 6.37%
- ⚠ **好成績不支持「選 9.50% 太高」這個推論**。被證實的只有公式的分子是錯的，
  **不是決策是錯的**——D036 那個病的反面版本：機制錯了但數字很好看

### Deployed（2026-08-21 01:09，main `66e7d3c`）
- **PR #35（D4）已合併並完成部署**。容器 01:09:09 重啟、`Up (healthy)`、`NRestarts=0`，
  **部署後 0 WARNING／ERROR**；容器內 `bot_engine.py`／`orderbook_depth.py` 與 main 的
  sha256 逐一比對一致——**跑的確實是新程式碼，不是舊映像**
- ⚠ **兩次部署（#34／#35）都還沒真正驗證到新程式碼**：改的是排隊位置日誌與往下重掛
  的守門檻，而這兩段都在「本輪有掛單計畫」之後才走得到。資金 08-20 19:10 借出後一直
  沒回來，每一輪都停在「餘額低於下限」那個出口。**（08-21 16:09 終於等到，見上方
  08-22 的 Notes）**

### Fixed（2026-08-21，分支 `fix/abstain-reason-names-the-right-one`）
- **棄權的理由指錯對象**（TASKS.md D4，D026 家族的第六次現身）——
  **這一項是 D039 自己帶進來的**，PR #34 合併後回頭盤查程式碼才發現。
  棄權有三個成因（場上那張單越界、候選價位越界、`queue_clear_usd_per_hour` 設成 0），
  而 D039 的第一版只看候選價位。實錄：**「候選價位年化 9.49% 已超出可見簿子
  （可見最高年化 9.67%）」——9.49 < 9.67，這句話自己就矛盾**
  - 「可見上限落在舊價與新價之間」是**市場走弱時最常見的形狀**，不是角落案例
  - 同批修掉第四個角落：`not book` 原本混在第一道 guard 裡，
    **簿子抓不到時整條判斷靜悄悄跳過**；現在拿不到簿子也會出聲說「拿不到」
  - **決策從頭到尾都是對的，錯的只有理由。** 測試 520 → 524 項
    （四個新測試裡兩個是反向斷言：看得到的那個不可以被點名、
    沒有東西越界時不可以寫「超出可見簿子」）。**行為零變動——只動理由**
- > **一個判斷式改成「會說不知道」之後，「不知道什麼」就成了新的說謊空間。**

### Deployed（2026-08-21 00:34，main `277620f`）
- **PR #34（D039）已合併並完成部署**。容器 00:34:05 重啟、`Up (healthy)`，
  systemd 接管正常，**部署後 0 WARNING／ERROR**；容器內
  `bot_engine.py`／`orderbook_depth.py`／`bitfinex_client.py` 與 main 的 sha256 一致

### Changed（2026-08-20，分支 `fix/honest-queue-position`）
- **A2-a：往下重掛的守門檻，算不出來時改為棄權而不是偷偷否決**（D039）。
  **而且棄權時會在日誌講出來**——如果棄權是靜悄悄的，這一改只是把「偷偷否決」
  換成「偷偷放行」，D026 的靜默失效換個方向再現一次
- **A3：排隊位置越界時改口說「至少 N USD」並點出可見簿子的上限**（D039）。
  拿去比較的那條路（`_queue_ahead()`）越界直接回 `None`，照實描述的那條
  （`_describe_queue()`）照樣印下界——**同一份資料，兩種責任分開**。
  **下界不是沒有資訊，會騙人的是把它講成量測值的語氣**
- `truncated` 定為**整本簿子的性質、不分桶**：簿子是「利率由低往高的前 250 檔」，
  可見範圍內完整、之上一無所知，所以一個旗標就夠。空簿子也算越界
  ——「前方 0 USD」同樣是編出來的
- **B9 順帶清掉**：`api/bitfinex_client.py` 那句「250 檔約 500 萬 USD，足以蓋過整個
  供給側」與 2026-08-19 實測（只有 1,306,715 USD、可見最高年化 9.04%）矛盾，改成
  明講「可見範圍內完整、之上一無所知」；`strategies/orderbook_depth.py` 的模組
  docstring 補上「已被 D035 取代但仍是父類別」。
  `_queue_ahead_of_live()` 併進 `_log_live_queue_position()`——A3 之後它只剩一個
  呼叫端，**留著就是下一個死碼**

### Validated（2026-08-20）
- **D037 的推論變成量測**：10:12～15:05 **連續 30 輪**被守門檻擋下，
  每一輪日誌的兩個排隊金額**完全相同**（`前方 3,535,093 → 3,535,093 USD`）。30/30。
  而那天唯一放行的一次也不是判斷——**是簿子剛好變深造成的巧合**
- ⚠ **今天成交了，不等於守門檻是對的**：它被一個假訊號放行，而成交是市場常態價
  從 5.47% 漲回 9.12% 掃上來的結果。**把它讀成守門檻的背書就是 D036 再犯一次**
- ⚠ **這一改鬆開了目前唯一擋著往下調價的東西**：從此往下重掛只剩 `_plans_match()`
  的 2% 利率容差與期望值策略自己在擋。真正的重掛政策是 **A2-b**，要在 M1／M2 的
  回測工具上跑過才定——**不要拍一個「超過 N 小時就降價」的常數**
  （`target_queue_usd` 就是這樣死的）

### Deployed（2026-08-19 23:44，main `da546c2`）
- **PR #33（D038）已合併並完成部署**。容器 23:44:55 重啟、healthy，
  部署後無任何 WARNING／ERROR，新表 `offer_wait_forecasts` 已建立
- **場上那張已排隊 18.7 小時的掛單沒有被取消**：部署後第一輪就走
  「掛單條件與場上 1 筆一致，維持不動」，與部署前的實跑驗證一致。
  新算法選出的價位與場上完全相同（0% 漂移），而舊算法選 9.96%、
  與場上差 1.8%——**卡在 2% 容差邊緣，市場再動一點就會觸發重掛**。
  換句話說這次部署反而降低了重掛風險
- 新舊版同一分鐘的日誌對照：

  ```
  23:35 舊：選中年化 9.96%（平均等待 3.2h、窗內命中 40 次、實質年化 9.33%）
  23:45 新：選中年化 9.78%（進場等待 平均 6.1h／中位數 3.5h／四分之三在 10.0h 內、
            窗內命中 53 次、實質年化 8.68%、11% 的起點在窗內沒等到「真實等待更長」）
  23:45 新：場上掛單已閒置 18.7 小時（機會成本約 0.0719 USD），沒有留下當初的等待預估。
  ```

### Changed（2026-08-19 稍晚，文件對齊，直接在 main 更新）
- **B7 `README.md`**：從「現階段以 dry-run 為主」改為「真金運作中（2026-08-15 起）」，
  並補上「目前的定價策略」（白話講單位時間報酬與陣發掃單，含已知限制）與「部署」兩節
- **B8 `ARCHITECTURE.md`**：架構圖、目錄樹、元件說明三處補上 `expected_value.py`
  與 `get_rate_candles()`；`orderbook_depth.py` 標明**已被取代但仍是父類別**
  （共用邏輯住在那裡，不是死碼）
- `PLAN.md` 新增「目前所在位置（2026-08-19）」，記下第 0 期為何比原訂的半天長很多：
  **兩天各冒出一個原本不在清單上、但比清單上任何一項都根本的問題**，
  而且兩次都是動手修既有項目時讀程式本體才發現的


### Fixed（2026-08-19，分支 `fix/honest-wait-estimate-and-idle-tracking`）
- **等待估計問錯了問題**（D038）：`estimate_wait_hours()` 逐根走訪算出命中間隔後
  用 `fmean()` 取平均，而 `[6,0,0]` 與 `[2,2,2]` 的平均都是 2——**它的 docstring
  宣稱處理了陣發性，那一行卻把陣發性抹掉了**。改為 `estimate_wait()`：
  從窗內每一個小時各出發一次，算「我這個時候進場要等多久」
  - 同一份 168 小時真實 K 線：9.96% 這個價位舊算法說等 3.2h，實際 10.8h（低估 3.4 倍）；
    最佳解由 9.96% 改為 **9.78%**
  - **修正不是單向的**：陣發時算出更長（測試中 3.75 倍），
    命中規則時反而算出更短（47.7h → 29.9h）。修的是「算對」，不是「調高」
  - 右設限（等到窗尾還沒等到）改為**計入而非丟棄**——丟掉的正是最長的那些等待
- `_parse_offers()` 補上 `created_at_ms`（funding offers 索引 2 = MTS_CREATE），
  **已對正式帳號實打驗證**：`'1787087004000'` → `2026-08-19 05:03:24 +0800`

### Added（2026-08-19，同分支）
- **閒置時間量測**（D037 順位 1）：每輪印出場上掛單已閒置多久、機會成本（USD），
  並與掛單當初的預估對照（在中位數內／已超過中位數／已超出 p75）
  - 量測點放在所有「要不要動這張單」的判斷**之前**：閒置最久的輪次走的正是
    「維持不動」那條提早 return 的路徑
- 新表 `offer_wait_forecasts`：掛單當下對「要等多久」的預估，一張單一列。
  **實際等待事後算得出來，「當初以為要等多久」才是不存就永遠消失的那一半**
- `describe_decision()` 的日誌行補上中位數、p75 與右設限比例——
  只印平均會讓「平均 6 小時」看起來像「大概 6 小時會成交」
- 測試 493 → 509 項；測試替身校正（`make_offer_array()`／`live_offer()` 帶
  MTS_CREATE 字串，`FakeClient` 補 `get_rate_candles()`）

### Observed（2026-08-19）
- **05:03:19 借款人提前還款**（原定 08-20 22:11 到期，提前 41 小時），
  部位 `464047005` 實際只借 6 小時 52 分，年化 9.96%
- 05:03:24 以年化 9.78% 重新掛單，**至 23:09 已閒置 18.1 小時未成交**
- 掃到 9.78% 的兩根 K（03:00、04:00）都在掛單**之前**；
  當下 250 檔可見最高只有 9.04%，掛單越出可見範圍（A3 現場重現、B9 順帶證實）


### Deployed（2026-08-18 22:39，main `205a757`）
- PR #32（盤查與路線圖重排、D036）與 A1（定價決策進日誌）**合併進 main 並完成部署**。
  CI 四個 job 全綠，容器 22:39:02 重啟並確認採用 `expected_value` 策略
- **合併時機是挑過的**：22:11 成交後資金已借出、訂單簿上沒有我們的單，
  重啟不會取消任何掛單、不損失排隊位置——**部署成本為零的視窗**
- 機器上沒有 `gh` CLI 也沒有 token，改以本機合併後推 main；
  GitHub 偵測到 head commit 已可從 main 到達，**PR #32 自動標示為 Merged**

### Validated（2026-08-18）
- **期望值定價（D035）第一次拿到真實市場的正面證據**：344.36 USD 以年化 9.96%
  掛出，期間市場常態價跌到 5.47%，**22:11:22 被一波掃單成交**。
  等待 3.6 小時，**含閒置的實質年化 9.27%**，對照立刻成交的 5.47%
- ⚠️ **但 A2 那個退化的守門檻整天沒被觸發**（擋下重掛的是 2% 利率容差），
  這筆成交**不構成 A2 的背書**，見 D037

### Added（2026-08-17 深夜，分支 `fix/log-expected-value-reasoning`）
- **定價決策寫進日誌**（TASKS.md A1）：`ExpectedValueStrategy.describe_decision()`
  ＋ `BotEngine._log_pricing_rationale()`。輸出含候選價位數、選中的利率、平均等待、
  窗內命中次數、實質年化，**並對照「最快成交的候選」**——後者正是舊策略會選的價位，
  兩者並排才看得出取捨換到了什麼

### Fixed（2026-08-17 深夜）
- **「本輪不掛單」的理由不再寫死成「可放貸金額不足」**。策略有六個出口會回傳空計畫，
  **其中五個跟金額無關**；最糟的是「價格低於年化 8% 地板」——帳上有 344 USD 卻寫
  「可放貸金額不足（目前 344.3 USD）」，自相矛盾又把人指向錯的方向，
  **而那正是市場走弱時最可能出現的情況**。這是 D026「靜默失效」的第三次現身
- `Strategy` 基底新增 `last_skip_reason` 與 `_skip()`，**三個策略的每個出口都要交代理由**。
  只修 `expected_value` 的話，之後切回 `orderbook_depth` 做對照就會踩回同一個坑。
  策略答不出來時退回中性的「未提供原因」——講「不知道」遠比講一個具體但錯誤的原因好

### Changed（2026-08-17，分支 `fix/market-floor-goes-stale`）
- **定價基準由「訂單簿排隊位置」改為「單位時間報酬的期望值」**（TASKS.md P1-5、D035）。
  `strategy.mode` 預設改為 `expected_value`。掃過候選價位，挑
  `r × 借出期間 ÷ (等待 + 借出期間)` 最大者（沿用 D034 的算式），
  等待時間每輪從 1 小時 K 重新估，不再是人手填進設定檔的常數
- **排隊定價之所以要換掉**：它假設需求會穩定地從簿子前端吃過來，實測不成立——
  這個市場的成交是**陣發掃單**（最近 30 天 **90.0% 的小時曾觸及年化 8% 以上**，
  單根 1 小時 K 振幅動輒 5 個百分點），而簿子底端長期被大額低價單佔住。
  **站在隊伍最前面不會更快成交，只保證用最低價成交**——2026-08-16 那筆年化 5.47%
  的成交，發生在一個曾漲到 11.77% 的小時裡
- `strategies/base.py` 新增 `requires_candles` 旗標與 `build_offer_plan(candles=...)` 參數

### Added（2026-08-17）
- `api.get_rate_candles()`：利率 K 線查詢（`/v2/candles/trade:{tf}:f{ccy}:p{period}/hist`）。
  **不用 `/v2/trades` 代替**——後者一次最多 10000 筆、活躍時段只涵蓋約 4 小時，
  而**樣本窗太短正是 D035 第一個錯誤結論的成因**
- `strategies/expected_value.py`：新策略，繼承 `OrderBookDepthStrategy`，
  只換掉「怎麼決定 base_rate」那一步
- 設定 `strategy.ev_window_hours`（168）／`ev_min_hits`（5）／`ev_min_candles`（48）／
  `candle_limit`（240）／`candle_timeframe`（1h）／`candle_hours`（1.0）

### Unchanged（2026-08-17，刻意保留）
- **`minimum_rate` 維持年化 8.00%**。一度打算調低它（因為當下 30 分鐘內「年化 8% 以上
  成交佔 0.0%」），查完 K 線後推翻——那個判斷用一個時間切片代表市場，
  與 D030 記過的爆發桶陷阱同類。年化 8% 的平均等待只有 0.6 小時，保留成本接近零
- `market_floor_pct`（0.85）與 D033 的成交價下限語意完全不動

### Known gaps（2026-08-17）
- **天期仍寫死 2 天**：D032／P1-2 指出天期與利率是同一個期望值問題的兩個維度，
  本次只做了利率那一維。比較天期要一輪打三次 K 線端點（`p2`／`p7`／`p30`）
  並各自估等待，列為 P1-5 的剩餘部分
- `get_funding_book()` 的 `len=250` 註解已過期：可見的 250 檔最高只到年化 8.33%，
  簿子在 8% 以上的部分看不到。改用期望值定價後訂單簿不再是主要資料源，
  影響隨之改變，但註解要修

### Changed（2026-08-16 夜間第三段，分支 `fix/requeue-queue-position-guard`）
- **往下調價的重掛要先證明划得來**（TASKS.md P1-4、D034）。判準是
  `利息 ÷ (等待 + 借出期間)`：新價位比場上那張低時，排隊位置的改善要補得回少收的
  利息才重掛。**往上調價不受這條限制**——那個方向多賺的利息是確定的，
  而往下調時「換來的速度」是估的，估的那半邊目前只有一個校準樣本
- **取消掛單後改為確認場上狀態，不再用餘額回推**（D031 的第二個缺口）。
  19:31 證明兩者會分岔：餘額沒回來不代表取消沒生效，也可能是那張單根本沒被取消。
  仍有單就整輪不重掛——**再掛一筆就是雙倍曝險**
- 日誌分出「候選價位」與「場上掛單」兩個排隊位置。原本只有前者，
  而 19:31 的誤讀正是把前者讀成後者
- `describe_queue()` 新增 `period` 參數：問「場上那張單排在哪」時天期要取自那張單，
  不能取自設定檔（分桶實驗一開始，同時掛 2 天與 30 天就是常態）

### Added（2026-08-16 夜間第三段）
- 設定 `engine.queue_clear_usd_per_hour`（預設 540000）：排隊的錢被吃掉的速率。
  取自唯一一筆真實成交（前方 73 萬 USD、1.35 小時後成交）。
  **遠低於訂單簿成交流量**（當日 2 天期約 415 萬 USD/小時）——成交量不等於隊列消化速度

### Rejected（2026-08-16 夜間第三段，實測否決）
- **D031 原本指定的「排隊位置守門檻」沒有實作**，理由見 D034：排隊金額對利率單調，
  「前方金額少」幾乎等同「這張單掛得比市場便宜」，固定門檻鎖住的正好是最該往上調價
  的那些。實測 2% 容差擋不住的 64 個往上調價位，重掛期望值**全部為正**
- 中間版本「利率更差、隊伍又更長就不動」同樣被否決：因為單調性，那種情況除了完全
  平手之外不可能發生，判準是空的

### Verified（2026-08-16 夜間，實單驗證，無程式變更）
- 🎉 **第一筆成交**：2026-08-16 19:31:31，344.30 USD、日利率 0.000250（年化 9.12%）、
  2 天期。從新策略首次掛單（18:10:24）到成交歷時 1.35 小時。
  **上方「定價基準改為訂單簿排隊位置」的變更由此得到實單驗證**
- **P2-1 的已知缺口部分關閉**：`credits` 端點的欄位索引已用真實回應核對，
  解析完全正確（金額／利率／天期／時間全對）。**`loans` 端點仍未驗證**
- **「不再每輪無條件取消重掛」實際生效**：18:20、19:20 兩輪日誌顯示
  「掛單條件與場上 1 筆一致（利率容差 2.0%），維持不動以保住排隊位置」
- 🔴 **同時發現缺陷（D031，待修）**：19:31:02 機器人送出取消，
  25 秒後那張單才成交——**取消沒趕上，純屬運氣**。
  觸發原因是排隊位置掉到 0 而策略想掛更高的利率；
  排隊位置只寫進日誌、沒有餵回重掛判斷。詳見 TASKS.md P1-4

### Changed（2026-08-16，分支 `feature/orderbook-pricing-and-fill-detection`）
- **定價基準改為訂單簿排隊位置**（TASKS.md P1-1、D030）。新增
  `strategies/orderbook_depth.py` 並設為預設（`strategy.mode`）。
  **根因**：舊策略掛 0.000272，而當時簿子頂端就是 0.000272——單子一直落在
  整個供給側的最後面，78 輪掛空。新演算法只有一句：在「前方排隊金額 ≤
  `target_queue_usd`」的前提下挑利率最高的一檔。實測會掛 0.000250（年化 9.12%）。
  **刻意不用 trades 百分位**：那會被爆發桶汙染，等於 FRR 落後問題換個來源重演
- **`minimum_rate` 語意改變**：從「算出來太低就拉高到這裡」改成
  「低於這裡就整輪不掛」。舊寫法會把價格拉到簿子外，掛一張永遠不會成交的單
- **不再每輪無條件取消重掛**：同利率下先掛先成交，每 600 秒重掛等於一天把自己
  送回隊伍末端 144 次。改為先查場上現況再比對，實質相同就什麼都不做
  （容差 `engine.rate_tolerance_pct`，預設 2%）
- **`spread_count` 由 3 改為 1**：344 USD 最多拆 2 筆，而第 2 筆會被乘到 0.000288、
  簿子頂端才 0.000270——一半的錢會變死單。**觸發條件正是「把資金全部投入」**

### Added（2026-08-16，分支 `feature/orderbook-pricing-and-fill-detection`）
- **成交偵測**（TASKS.md P2-1、D030）：機器人終於知道自己借出去了。
  新增 `funding_positions` 表、`Repository.sync_positions()` 對帳，
  以及「資金已借出」「借出的資金已收回」兩則 LINE 通知。
  查 credits 與 loans **兩個端點**——只查一個會漏掉一半。
  **已知缺口**：那兩支的欄位索引取自官方文件、尚未經真實回應核對（至今零成交），
  解析已寫成防禦式，第一筆成交後要回來核對
- `api/base.py` 新增 `get_funding_book()`／`get_active_offers()`／`get_active_positions()`
- `config.yaml` 新增 `strategy.mode`、`strategy.offer_period`、
  `strategy.target_queue_usd`、`engine.rate_tolerance_pct`
- 測試 347 → 437 項

### Added（2026-08-16，分支 `feature/notify-format-and-trade-events`）
- **交易面通知**（TASKS.md P2-4、D029）：掛單上線／掛單消失／掛單被拒三種事件會推 LINE。
  推的是**狀態轉換**不是每輪結果——原規劃的「內容有變才推」擋不住 FRR 漂移，
  等於每輪都推。現在一天通常 0～2 則。**「掛單已不在場上」是目前唯一能察覺
  「錢可能借出去了」的訊號**，但刻意不寫死成「成交」（也可能是資金被搬走）
- **統一訊息格式**（P2-3、D029）：新增 `notify/messages.py`，三段式——
  結論行／`欄位：值`／「需人工介入」或「無需處理」二選一。
  分類分成【系統】【交易】【收益】【風控】，圖示為正常看分類、異常看等級。
  日誌維持單行（`grep ERROR` 才抓得到），只有推播用三段式
- `config.yaml` 新增 `line.push_trade_events`（預設 true）：額度安全閥，
  關掉的是通知不是紀錄
- 測試 308 → 347 項

### Changed（2026-08-16，分支 `docs/tasks-notify-and-ci-deploy-skip`）
- **只動 `.project-docs/` 的推送不再觸發部署**（TASKS.md P1-3、D017 的 2026-08-16 補充）。
  先前每次文件同步都會 `systemctl --user restart`，等於**把交易所上正在排隊的掛單
  取消後重掛一筆新的**——空窗加上時間優先權歸零，白白拉長成交的等待。
  新增 `changes` job 比對變更路徑，`deploy` 整個 job 被條件擋下；
  `test`／`integration` 照跑。判斷不出來的情況一律照常部署（fail-open）

---

## 更早的變更

真金上線前的 M1～M4 建置期（2026-07-26 ～ 2026-08-15）完整保留在
**[archive/CHANGELOG-M1-M4.md](archive/CHANGELOG-M1-M4.md)**，包含四個 milestone
的 Added／Fixed／Changed／Known Issues，以及 2026-08-15 那一則
「M4 完成並切換為小額真金運作」的上線紀錄。

